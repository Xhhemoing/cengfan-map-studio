# Round 5 Briefing (from the Round-4 SOTA review)
[Model: claude-fable-5-thinking-xhigh]

## Direction

Round 4 wired the Round-3 producers into consumers and bounded snapshot serialization. Round 5 is **calibration, honesty, and de-duplication of state** — no lock-free / zero-copy work, no new subsystems. The centerpiece is R5-3: the app currently runs three independent `createIndexedDbProjectStore()` instances (`workbenchStore` in main.tsx, `editorProjectStore` in src/lib/editor-project-store.ts, and AppErrorBoundary's lazy own-store). Healthy they agree; degraded they split-brain — a project created in the degraded workbench is invisible to the editor route (separate memory fallback → projectMissing), each instance runs its own R4-10 recovery probe, and the crash screen consults a store whose health is not the health that matters.

Quality bar unchanged: **no metric, no merge; red repro first; targeted vitest only** (`npx vitest run <file>`); `npm run typecheck` must stay exit 0 after every task. One task = one exclusive writable file set; the sets below are disjoint — do not touch another task's files.

## Tasks (exactly 10, disjoint writable sets)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R5-1 | `server/collaboration.ts`, `server/collaboration.test.ts`, `scripts/perf-room-snapshot-bench.ts`, `tsconfig.app.json` | Persist cap 5 MiB < 8 MiB API accept limit (`DEFAULT_MAX_ROOM_TRANSACTION_BYTES`, index.ts:123): 5–8 MiB rooms accepted live, silently restart-mortal. Per-room cap doesn't bound total flush occupancy (N rooms × ~15ms each). Interval flush queues snapshots onto `persistQueue` while a slow sink holds the previous one (each retains a full snapshot). Dead `clientId` in `setAccess` forced the R4-6 tsconfig exclusion → convergence proof type-checked by no project. | Red: test a 6 MiB-record room → skipped at current cap. Fix: raise `MAX_PERSISTED_ROOM_BYTES` to a bench-derived value ≥ accept limit + envelope/history margin (start at 12 MiB ≈ 40ms/room by R4-4's slope; re-measure with new bench cells at 6/8/12 MiB, median-of-5, and record the table); add a total-snapshot byte budget skipping largest records first, deterministically, with ids in the warn; skip scheduling a new flush while a persist is in-flight (coalesce, re-arm after settle); rename `_clientId` and delete the tsconfig `exclude` line. Metric: bench table (all kept cells ≤50ms occupancy), `npm run typecheck` exit 0 with the proof file included. |
| R5-2 | `server/index.ts`, `server/index.test.ts` | Boot logs "restored 0 collaboration room(s) from snapshot" even with no snapshot file (false provenance, R4-1 nit). Prior envelope's `skippedRoomCount` is never surfaced at boot. Snapshots that parse but have an invalid envelope (version ≠ 1 / rooms not array) are silently ignored and then overwritten — quarantine misses them. `.bad` sidecars accumulate unboundedly. | Red: boot-with-no-snapshot test asserting no "restored" log line (fails today). Fix: log restored count only when a snapshot loaded; add a boot line for prior `skippedRoomCount` ("上次关停有 N 个房间超过持久化上限，未恢复"); quarantine invalid-envelope files via the existing `roomSnapshotQuarantinePath`; keep at most K (suggest 5) newest `.bad` sidecars. Metric: log-content tests for all four paths. |
| R5-3 | `src/lib/editor-project-store.ts`, `src/main.tsx`, `src/main.test.tsx`, NEW `src/components/StudioRoutes.tsx` (+ its test file) | Three store instances split-brain when degraded (review headline). `onRecoverError` has zero consumers — quota-blocked recovery is silent everywhere. Crash boundary never receives the live store. `main.tsx` react-refresh warning from defining `WorkbenchRoute` in an entry file. Editor route shows no degraded notice (deferred from R4-3). | Red: degrade the store, `put` a project via the workbench store, `get` it via `editorProjectStore` → null (split-brain repro). Fix: ONE shared instance exported from `src/lib/editor-project-store.ts` (move the health-subscriber slot + `onHealthChange`/`onRecoverError` wiring there); main.tsx imports it; extract `WorkbenchRoute` + a new `ProjectRoute` into `StudioRoutes.tsx` — `ProjectRoute` renders the memory-mode notice banner **around** `<App/>` (do NOT edit App.tsx — R5-6 owns it) and surfaces the latest recover error (quota) in that banner; pass `projectStore={sharedStore}` to `AppErrorBoundary`. Metric: split-brain test green; lint react-refresh warning count drops by 1; editor-route notice appears when degraded and clears on recovery. |
| R5-4 | `src/components/AppErrorBoundary.tsx`, `src/components/AppErrorBoundary.test.tsx` | Dishonest copy (R4-7 nits): `store-degraded` claims "磁盘上没有可导出的工程内容" when the truth is the database wouldn't open; `project-missing` says "已经不在本机项目库里了" when a mid-enumeration degrade returned null from empty memory. With an injected degraded store that HOLDS memory projects (reachable after R5-3), the flow must offer them with in-memory-only urgency copy. | Red: inject a degraded store with 2 memory projects → current code renders them under disk-implying copy; inject an empty degraded store → copy asserts disk emptiness it cannot know. Fix: separate "无法打开本机数据库" from "库为空"; memory-held projects listed with "仅存在于本次会话内存中，请立刻导出"; `project-missing` distinguishes degraded-during-read. Metric: red-first tests per copy path; read-only invariant preserved (no put/remove — keep the existing spy test). |
| R5-5 | `src/components/ProjectWorkbench.tsx`, `src/components/ProjectWorkbench.test.tsx`, `src/styles.css` | After memory→persistent recovery the notice clears but the list stays the memory-era view until the next user op (cross-task 2 residue). `.workbench-storage-notice` has zero CSS and inherits `.workbench-resume`'s hover/`:active { transform: scale(0.985) }` — a warning that animates like a button. `exportBackup` fires N programmatic downloads under one gesture — Chromium blocks the 2nd+ (multi-project backup silently truncated; injected-spy tests can't see it). | Red: flip health prop memory→persistent in a test → assert `store.list` re-invoked (fails today). Fix: effect keyed on the health prop re-runs `refresh()` on the persistent flip; dedicated `.workbench-storage-notice` rules (no hover transform, warning palette); replace the download loop with per-project export buttons inside the notice (crash-screen pattern) or a single combined trigger per gesture — document the browser constraint in-code. Metric: refresh-on-recovery test; DOM test for per-project affordances. |
| R5-6 | `src/App.tsx`, `src/App.test.tsx`, `src/lib/useCollaborationRoom.ts`, `src/lib/useCollaborationRoom.test.tsx` | A submit that rejects with a terminal code (ROOM_NOT_FOUND / FORBIDDEN / ROOM_CLOSED) only paints `error.message`; the room stays live-looking until the stream independently discovers expiry — user can keep editing into a dead room between heartbeats (R4-2 observation). | Red: App test — submit rejection with ROOM_NOT_FOUND leaves `roomExpired` false and the send effect re-armable. Fix: hook exports a `reportTerminalRejection(code)` (delegating to the existing `markRoomExpired`/`markRoomClosed`, `isTerminal()`-idempotent); App's submit catch calls it for terminal codes before painting the message. Preserve R4-2 invariants: no heal-count advance, no offline flag on protocol rejections. Metric: red-first tests both layers; existing 125 App tests stay green (targeted run). |
| R5-7 | `src/lib/agent-session.ts`, `src/lib/agent-session.test.ts` | `run()` pushes `{role:"user", content:message}` unconditionally (line 664), so R4-8's resume re-appends the identical unanswered request left by the transport-failed round — duplicated message, token waste, re-plan nudge. | Red: transport-fail a run, `continue()` with the same text, assert the conversation contains the user message once (fails today: twice). Fix: in `continue` mode, skip the push iff the conversation tail is the identical user message with no assistant reply after it; a *different* resume text must still append. Metric: red-first test + a different-text-appends test; existing session tests green. Note: `agent-session.deadline.test.ts` exists — do not touch it; coordinate nothing, your change is upstream of transport. |
| R5-8 | `server/production.ts`, `server/production.test.ts` | Drain errors still swallowed by `.catch(() => undefined)` — only flush failures report (R4-5 scope edge). A throwing `onDraining` propagates out of `shutdown()` (pinned at production.test.ts:219) → in production that throws inside a signal handler → `uncaughtException` mid-shutdown while close/drain/flush never ran. | Red: drain rejection currently invisible — test asserting a report hook fires goes red. Fix: report drain errors through `onFlushError` with a phase discriminator (`{phase:"drain"|"flush"}`) or a parallel `onDrainError` — pick one, document it; contain `onDraining` throws (report, continue shutdown) and **deliberately flip** the pinned throw test with the rationale in the test name (signal handlers must not throw). Metric: red-first tests; idempotence + deadline tests stay green. |
| R5-9 | `src/components/AgentAssistant.tsx`, `src/components/AgentAssistant.transport.test.tsx` | While failed+resumable, two competing affordances render: the main button (labeled 继续对话, disabled until text is typed) AND 网络恢复后重试 — ambiguous which continues what; main button uses the *typed* message, retry uses the *original* request (R4-8 nit). | Red: DOM test asserting exactly one continue affordance in failed+resumable state (fails today: two buttons). Fix: single primary affordance — 网络恢复后重试 resends the original request; typing new text switches the primary button to continue-with-new-text and hides the retry (state machine in one place, tested). Metric: red-first DOM tests for both sub-states; resume-still-calls-`session.continue` test preserved. |
| R5-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律 (AGENTS.md): user-facing changes need a recorded acceptance path; breaking-ish changes need rollback plans. The R4 record lives in commit messages only; PR [#14](https://github.com/Xhhemoing/cengfan-map-studio/pull/14) description doesn't carry the durability band or rollback map. | Deliverable (docs, no code): PR description gains (a) acceptance evidence per round (suite/lint/typecheck numbers), (b) behavior changes: R4-4 persist band (5 MiB cap until R5-1; rollback = revert `0613660`), R3-6 refusal identity, R4-10 recovery (rollback = revert `1287f4f`), R4-5 shutdown contract; (c) known follow-ups = this table. PROGRESS.md Round-5 section mirrors it. After R5-1 merges and gates are green, flip PR #14 to **ready-for-review** (review verdict: no blocker). |

## Sequencing

R5-1 first (it owns the only calibration blocker and unblocks flipping the PR). R5-3 before R5-4's end-to-end verification (R5-4's own tests inject stores directly, so it need not wait to start). Everything else is order-free. R5-10 last.

## Saturated — leave alone this round

`src/lib/project-store.ts` (two rounds of work; remaining findings are theoretical — delete-resurrection impossible while memory starts empty, `localeCompare` cosmetic), `src/lib/useCollaborationRoom.ts` heal/terminal core (R5-6 adds one seam only), `src/lib/collaboration-client.ts`, workbook import/export paths, `src/lib/agent-session.ts` transport deadlines (R5-7 is one push-site change). Do not add a browser-side Worker bench: R4-9's Node band check plus its printed "exact-browser-claim-verified=no" is the correct resting state.

## Frozen (unchanged)

`server/ai/*`; `src/lib/collaboration-operations.ts` (consume only); layout stack; `src/lib/project-package.ts`; project-document rejection identity; SSE wire format (drain still must not emit `closed`); DataWorkspace product code (bench only).
