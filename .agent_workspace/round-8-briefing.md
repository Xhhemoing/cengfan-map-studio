[Model: claude-fable-5]
# Round 9 Briefing (from the Round-8 SOTA review)

## Direction

Round 8 closed both of its headline defects and, for the first time, held red-first discipline on every row. Round 9 finishes the failure-visibility chain at its last dark surface, fixes the disk-litter defect the round filed (plus the second instance this review found), and starts paying the file-size debt that is now the largest hygiene item — we are at round 9 of 20, and every task below carries evidence from the Round-8 review (`.agent_workspace/round-8-review.md`). The headline is a three-step chain (R9-1 → R9-2 → R9-3): **the transactions POST (`server/index.ts:892–920`) answers with the raw `roomStore.apply` result and no persistence fields — and an actively-editing member's only steady-state server contact is that ack** (the SSE wire is frozen and silent on persistence), so a persist-failure streak that starts mid-session never reaches the one member with the most undurable work until a heal or version conflict forces a refetch. Second: on a rename failure, **two** writers orphan `<file>.<pid>.tmp` forever — the room-snapshot writer (`index.ts:1568–1575`, filed by R8-8) and the workspace PUT writer (`index.ts:773–776`, found by this review) — while `pruneRoomSnapshotSidecars` (`:1502–1506`) only ever prunes `.bad`. Third theme: size debt with the extraction playbook proven three times — App.tsx 1729, `server/index.ts` 1637 (baseline 1076), and test files up to 2978 lines against the repo's 400-line rule — plus a permanent guard so the 38-file unmount sweep can never silently regress.

Quality bar unchanged: **no metric, no merge; red repro first — as a separate red commit or with the observed red state recorded verbatim in the commit message; targeted vitest only** (`npx vitest run <file>`); `npm run typecheck` must stay exit 0 after every task; CI must be green on the merged tree. One task = one exclusive writable file set — except where this table *explicitly* sequences two tasks on the same file (`server/index.ts` and `src/App.test.tsx` this round); a sequenced successor starts only after its predecessor's merge and must not rewrite the predecessor's additions. If your fix forces an edit in a cross-owned file, stop and file it.

## Tasks (exactly 10; writable sets disjoint except the two pre-authorized sequences)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R9-1 | `server/index.ts`, `server/index.test.ts` | Both atomic writers leak their temp file on rename failure: room-snapshot writer (`index.ts:1568–1575`) and workspace PUT writer (`index.ts:773–776`) write `<file>.<pid>.tmp` then `rename`, with no cleanup on the rename path; `pruneRoomSnapshotSidecars` (`:1502–1506`) matches only `.bad`. Filed by R8-8 (`edab090`) for the room writer; the workspace instance found by the R8 review. A recurring rename failure (final path squatted by a directory — the inverse of R8-8's mechanism) litters `dataDir` with one orphan per pid, unreclaimed across restarts. | Red: per writer, occupy the *final* path with a directory → write succeeds, rename rejects (real errno) → pin that the `.tmp` remains in `dataDir` (readdir). Fix: best-effort removal of the temp file when rename fails (both writers; failure of the cleanup itself must not mask the original error), the write→rename order and modes untouched (`0o600` on the room writer). Metric: red flips for both writers; the original rename error still propagates (pinned); clean-path pins green unmodified; R8-8's journey (cross-owned) green untouched. |
| R9-2 | `server/index.ts`, `server/index.test.ts` — **sequenced after R9-1's merge** | The transactions ack (`index.ts:892–920`) sends `roomStore.apply` raw — no `persistedAtLastFlush`, no `persistence` — while create/join/snapshot carry the full tri-state + `lastFailureAt` (R8-2). The actively-editing member is the only one with zero streak-visible responses (R8 review cross-check 1). Also: `/api/health.rooms.lastFlush.at` still publishes `0` raw for a never-succeeded store — 1970 to a naive parser (R7-2 nit 2, open for a third round). | Red: during an injected streak, POST a transaction → pin the ack today carries no persistence trace, then flip. Fix: the ack (both full and `Prefer: return=minimal` shapes) gains the same `roomPersistenceFields(roomId)` spread — one seam, no new computation; key-omission semantics identical to R8-2. Health: map `at: 0 → null` on `lastFlush` (same 那不是 1970 年 reasoning as R7-2), `lastFailure` pass-through untouched — this is a shape change on an ops surface: record the rollback (revert this merge) in the commit. No SSE changes. Metric: red flips on the ack with exact-shape pins (minimal and full); streak + success-clears over a real flush cycle re-pinned on the ack; health `at: 0` red flipped; existing R8-2 pins green unmodified except any that exact-match a never-succeeded health shape (flip = evidence, per R8-2's precedent — name them in the red commit). |
| R9-3 | `src/lib/collaboration-client.ts`, `src/lib/collaboration-client.test.ts`, `src/lib/collaboration-send.ts`, `src/lib/collaboration-send.test.ts`, `src/lib/useCollaborationRoom.ts`, `src/lib/useCollaborationRoom.test.tsx` — sequenced after R9-2 | Client side of the chain: `submitRoomOperations`/`submitRoomSnapshot` return the ack unnormalized (`collaboration-client.ts:483–507` — no `persistenceFlagOf`/`persistenceOutcomeOf` applied), and `commitAcknowledgement` (`collaboration-send.ts:214`) discards everything but version/ids. Even after R9-2 the hook's `roomPersistFailureAt` only updates on create/join/snapshot fetches. | Red: hook + send loop today leave `roomPersistFailureAt` null when an ack carries `persistence.lastFailureAt`. Fix: client normalizes ack persistence with the exact R8-3 discipline (three-literal outcome, absence-as-no-streak, unknown extras dropped); the hook exposes a display-only ack-note entry applying the *same* `notePersistence` rules (tri-state clears, silent-boolean keeps); `commitAcknowledgement` threads the ack to it — controller is already the whole hook result, so **no `App.tsx` edit and no `ProjectMenu` edit** (prop landed in R8-3b). Do NOT touch heal/terminal/offline logic or `collaboration-operations.ts` (frozen). Metric: red flips; old-server ack (no field) keeps prior state (pinned); streak-ends-via-ack clears the menu state (pinned at hook level); existing R8-3 pins green unmodified; send-loop convergence pins green unmodified. |
| R9-4 | `server/durability-flush-failure.integration.test.ts`, `server/durability-helpers.ts` — sequenced after R9-1 and R9-2 | Two unproven claims over real files: (a) R9-1's cleanup has no real-disk proof, and the rename-failure branch (write succeeds, rename fails) has no journey at all — R8-8's EISDIR mechanism fails at `open(2)`, before any temp file exists; (b) `persistence.lastFailureAt` (and after R9-2, the transaction ack's) is pinned only with injected throwing persists — never against a kernel errno (R8 review leftover 4). | Deliverable (proof task, test-only): a second journey in the flush-failure file — boot real `dataDir`, clean flush; squat the *final* snapshot path's rename target (directory over `collaboration-rooms.json` after saving its bytes, or an equivalent rename-blocking mechanism — document the choice and restore in `finally`); edit over HTTP; explicit flush rejects with the rename errno → health `lastFlush.lastFailure` carries it; **the transaction ack and a snapshot GET both carry `persistence.lastFailureAt` over real HTTP**; `dataDir` contains no orphaned `*.tmp` after the failure (R9-1's cleanup, the leak this journey exists to catch); SIGTERM contained; restore path in `finally`, reboot restores the saved good envelope. Keep every file ≤ 400 lines (split further if needed — the file is at 148). Any product defect found is filed for R10, not fixed. Metric: journey green on the merged tree ×3 consecutive runs; the no-orphan assert demonstrably red against the pre-R9-1 tree (record the observed red verbatim). |
| R9-5 | `src/App.tsx`, `src/App.test.tsx` (additions only), `src/lib/app-initialization.test.ts`, NEW `src/lib/*` modules (+ tests) | App.tsx at 1729 vs the 400-line rule; playbook proven three times (−329/−334/−261, suites green). Remaining cohesive clusters located by the R8 review: save-on-leave lifecycle (`saveWorkspaceNow` + latest-ref pattern + `subscribePageLeave` wiring + `handleBackToWorkbench` + `overwriteBrowserStorage`, App:655–698), custom-template capture wiring (:766–767), project-record load/save refs (:242–259). Plus R8-7's nit: `loadBrowserState`'s SSR fallback branch (`app-initialization.ts:63–66`) is unpinned anywhere — jsdom always has `window`. | Deliverable: extract ≥200 further lines into pure-seam modules with their own tests; **behavior-preserving only** — save-pipeline call order, hash navigation, and 描述文案 verbatim; the latest-ref discipline (refs synced in effects, not render) must survive extraction with its rationale comments. Add the direct `loadBrowserState` test (SSR branch via a scoped `window` shim or extracted guard). No cross-owned test edits; no WIP messages in merged history. Metric: line-count delta in the commit; App.tsx ≤ 1530; all existing App tests pass unmodified; typecheck 0. |
| R9-6 | `vite.config.ts` (test block only), NEW `src/test/leaked-root-guard.ts` (name indicative) | Two rounds of sweeps fixed 38 files (R7-7: 2, R8-5: 21, R8-6: 14, R8-3: 1) and the fresh R8-review scan is clean — but nothing stops a new test file from reintroducing inline-only unmounts; the probe that measured 165→0 was temporary. R8-5's metric commit (`5de889e`) proved the mechanism: setup-file `afterEach` hooks run outermost, after the file's own `afterEach`. `vite.config.ts` has no `setupFiles` today. | Red: temporarily strip one file's net (locally, not committed) → the guard fails naming the leaking case; record the observed output verbatim in the commit message. Fix: a `setupFiles` entry that, in jsdom environments only (node suites must be untouched — guard must no-op there), counts still-mounted `__reactContainer$` containers after each test and fails with the container's id/tag when non-zero. Must not double-fail an already-failing test in a way that masks the original assertion (report as teardown error or suppress when the test already failed — document the choice). Metric: full suite green with the guard active (orchestrator gate); the deliberate-leak red recorded; no timing regression beyond noise on the suite wall-clock. |
| R9-7 | `server/index.ts`, `server/index.test.ts` (move-only edits), NEW `server/room-snapshot-store.ts` (+ NEW test file) — **sequenced last among the index.ts tasks, after R9-1 and R9-2** | `server/index.ts` is 1637 lines (baseline 1076) — the growth is a cohesive, module-scope room-snapshot persistence cluster: `loadRoomSnapshot`, `quarantineRoomSnapshot`, `pruneRoomSnapshotSidecars`, `roomSnapshotSidecarSequence`, `isRestorableRoomSnapshot`, `createRoomSnapshotWriter` (~150 lines, `:1490–1580`), all pure module functions with no request-handler coupling. | Deliverable (move-only refactor): relocate the cluster verbatim into `server/room-snapshot-store.ts`; move (not rewrite) the tests that pin it — quarantine sidecars, envelope rejection, writer atomicity, R9-1's cleanup — into the new test file; `index.ts` keeps only imports. Zero behavior change, zero assertion rewrites (verify by `-U0` diff on `expect(` lines, the R8-8 discipline). Metric: `server/index.ts` ≤ 1480; moved tests green in isolation; full `server/` targeted run green; typecheck 0. |
| R9-8 | `server/collaboration.test.ts` (split), NEW `server/collaboration-*.test.ts` files + NEW shared fixture helper (names indicative) | `server/collaboration.test.ts` is 1532 lines against the 400-line rule — the same debt R8-8 paid for durability, four times over. It mixes at least four separable domains (membership/access, apply/convergence, persistence outcomes + warn text, trim/eviction geometry) sharing seed helpers (R8-1 added three more). | Deliverable (the R8-8 pattern): split into cohesive per-domain files + one shared fixture module; **zero assertion changes in the move** (machine-check: `-U0` diff shows `expect(` lines only relocated, and `it` counts identical before/after — quote both in the commit message); every resulting file ≤ 400 lines. Metric: all split files green in isolation and together; the skipped test stays skipped (not silently dropped); typecheck 0. |
| R9-9 | `src/App.test.tsx` (split), NEW `src/App.*.test.tsx` files + NEW shared harness module (names indicative) — **sequenced after R9-5's merge** | `src/App.test.tsx` is 2978 lines / 18 describes — 7.4× the rule and the biggest file in the repo. R8-3b/R8-7/R9-5 keep appending because there is nowhere else for App-level pins to live. Cohesive domains are already visible in the describes (collaboration send/recovery, persistence copy, history/undo, navigation, workspace save). | Deliverable: same discipline as R9-8 — split by domain with a shared render-harness module (the `renderApp`/`stubStream`/scripted-fetch fixtures), zero assertion changes, machine-checked, every file ≤ 700 lines (App harnesses are heavier than server fixtures; 400 stays the target where achievable — state the per-file counts in the commit). Include R9-5's additions in the split (it merged first). Metric: all split files green in isolation; total `it` count unchanged; typecheck 0. |
| R9-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律: R8-10 was sequenced after the Round-8 review by design; PR [#14](https://github.com/Xhhemoing/cengfan-map-studio/pull/14)'s description must now carry Rounds 6–8. | Deliverable (docs, no code): description gains (a) Round-8 evidence (197 files / 1846 passed / 2 skipped; lint 0 err / 1 warn; typecheck 0; CI green on `553d5c1` runs 32787959232 + 32787965224; review 9 ACCEPT + 1 NITS, no blocker), (b) behavior changes + rollbacks: warn legend scoping (revert `a72bf30`), `lastFailureAt` on room responses (revert `d9d77ef`), member failure copy (revert `4f1b13c` + `4f70e66`), banner export errors in-notice (revert `8fefafc`), App extraction + WorkbenchBackButton (revert `56c2d7b`), react-refresh moves (revert `4d2a887`), plus the Round-6/7 items from the R8-10 spec, (c) known misses stated plainly: transactions ack silent on streaks until R9-2/3; `.tmp` orphan on rename failure until R9-1; durable capacity ceiling 12 MiB. PROGRESS.md Round-9 section mirrors it. |

## Sequencing

`server/index.ts` queue (strict): R9-1 → R9-2 → R9-7. Chain: R9-2 → R9-3 (client consumes R9-2's ack fields). Proof R9-4 after R9-1 + R9-2. `App.test.tsx` queue: R9-5 → R9-9. R9-6 and R9-8 are order-free and can start immediately. R9-10 last. Parallel-safe starts at round open: R9-1, R9-5, R9-6, R9-8.

## Saturated — leave alone this round

`server/collaboration.ts` (R8-1 closed the last evidenced wording defect; trim/eviction geometry pinned since R6-1 and proven E2E twice), `src/components/ProjectMenu.tsx` (three-note precedence complete and pinned; only a new evidenced defect reopens), `src/components/ProjectWorkbench.tsx` + `StorageNotice.tsx` (R8-4 closed the last open notice item), `src/lib/editor-project-store.ts` + `src/lib/project-store.ts`, `scripts/perf-room-snapshot-bench.ts` (honest since R7-6; next bench work needs a new workload), the 38 swept test files' unmount pattern (R9-6 guards it; don't churn the files), `.github/workflows/ci.yml`, `useCollaborationRoom` heal/terminal/offline core (R9-3 touches ack-note display state only).

## Frozen (unchanged; campaign runs to round 20)

`server/ai/*`; `src/lib/collaboration-operations.ts` (consume only — R9-3 verified feasible without it: the ack is consumed in `collaboration-send.ts`); layout stack internals (`card-layout.ts`, layout worker, solver) unless a new evidenced defect; SSE wire format (drain still must not emit `closed`; R9-2/R9-3 use HTTP response fields only); project-package format; project-document rejection identity; DataWorkspace product code (its exhaustive-deps warning stays — the repo's one remaining lint warning is intentional); payment/套餐/模板手续费 boundaries per `docs/开源与收费边界.md` — no payment or billing code in this repo.
