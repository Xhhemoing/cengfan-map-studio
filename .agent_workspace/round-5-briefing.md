[Model: claude-fable-5-thinking-xhigh]
# Round 6 Briefing (from the Round-5 SOTA review)

## Direction

Round 5 closed the split-brain store, calibrated the persist cap to the API accept limit, and made every degraded surface tell the truth. Round 6 is **band elimination, visibility to the people affected, and proving the merged system under disaster** — no lock-free / zero-copy work, no new subsystems. We are at round 5: tasks stay at the level of concrete, evidenced defects, not system-level extremes. The flagship is R6-1: the persist band still exists at the exact limit (record envelope + history on top of the package; snapshot envelope on top of the record; aggregate budget = per-room cap so two heavy rooms can never both persist), and the safe elimination — persisting oversized rooms with trimmed operation history — is already guaranteed safe by `getOperations`' trimmed-interval `VERSION_CONFLICT` answer (collaboration.ts:721–724).

Quality bar unchanged: **no metric, no merge; red repro first; targeted vitest only** (`npx vitest run <file>`); `npm run typecheck` must stay exit 0 after every task. One task = one exclusive writable file set; the sets below are disjoint — do not touch another task's files.

## Tasks (exactly 10, disjoint writable sets)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R6-1 | `server/collaboration.ts`, `server/collaboration.test.ts`, `scripts/perf-room-snapshot-bench.ts` | Exact-limit mortality band (R5 review cross-check 3): the persisted record wraps the package with accessRecords + up to 256 `operationHistory` entries + metadata, so a package the API accepts near 8 MiB yields a record > `MAX_PERSISTED_ROOM_BYTES` → skipped; a record of exactly 8 MiB passes the per-room check and is evicted by the total budget (+~24 B envelope). Aggregate budget = per-room cap ⇒ whole-server durable capacity is 8 MiB (two 5 MiB rooms can't both persist). Interval flush swallows persist rejections (`void flush().catch(() => undefined)`, collaboration.ts:881) — steady-state disk-full is silent until shutdown. | Red: a room whose create/transaction body the API accepts but whose record exceeds the cap → currently absent after restart. Fix: **history-first degradation** — when a record misses the per-room cap or loses budget eviction, retry it with `operationHistory` trimmed (empty) before skipping the whole room; safe because `getOperations` already answers a trimmed interval with `VERSION_CONFLICT`/"请重新获取完整快照" and clients re-snapshot (pin that restore-path behavior at the store level). Record trimmed ids separately in the envelope (`trimmedRoomIds`, additive) and in the warn. Report interval persist failures (`console.error`, deduped per failure streak, cleared on success). Expose a `lastPersistOutcome()` accessor (skipped ids, trimmed ids, at-timestamp) for R6-2. Add an estimator-fidelity corpus test (quotes/backslashes/C0/paired+unpaired surrogates) asserting `jsonByteLengthWithin` == `Buffer.byteLength(JSON.stringify(record))` exactly. Metric: red test flips (trimmed room survives restart and serves its snapshot; a fresh joiner at an old baseline gets the conflict-then-resync path); 2×5 MiB rooms both survive; bench cell for the trim path stays within budget. Non-goal, on the record: the kept 8 MiB cell measures 56.6 ms > the ~50 ms occupancy target — do NOT chase it with off-thread serialization this round; the miss is documented and bounded. |
| R6-2 | `server/index.ts`, `server/index.test.ts` | Boot warn reports the skipped-room **count but not the ids** (`readSkippedRoomCount` ignores the `skippedRoomIds` the envelope has carried since R5-1). Nothing outside the server console can observe persistence mortality: no ops surface, and room HTTP responses carry no signal for R6-6 to consume. | Red: boot with a snapshot carrying `skippedRoomIds` → current log has no ids. Fix: boot warn lists ids; extend the existing liveness/health endpoint payload (additive) with `rooms: { restoredAtBoot, skippedAtLastShutdown: { count, ids }, lastFlush: <R6-1 lastPersistOutcome> }`; add an additive `persistedAtLastFlush: boolean` field to room create/join/snapshot HTTP responses, computed from `lastPersistOutcome()` (lag ≤ one flush interval is acceptable and documented). No SSE changes. Metric: log-content test with ids; endpoint test; response-field tests (true for small rooms, false after a flush skipped/trimmed the room). Sequenced after R6-1. |
| R6-3 | NEW `src/components/crash-disaster.integration.test.tsx` (test-only; no product files) | The R5-3+R5-4+R4-7 combination is proven only unit-by-unit. No test walks the disaster the campaign built for: corrupt IndexedDB at boot + projects created in the degraded session + a crash. | Deliverable (proof task, like R4-6): stub a failing `IDBFactory` before module load (`StudioRoutes.test.tsx` pattern, `vi.resetModules`), drive the real shared store: workbench-create two projects → editor route `get`s one (shared memory) → crash a throwing child under `AppErrorBoundary` with the mirror empty → assert the boundary lists **both** memory projects under the in-memory urgency copy, per-project export produces both files across two gestures, and no `put`/`remove` hits the store; then push a quota `onRecoverError` through the channel and assert the route banner shows it. Metric: the journey test green on the merged tree. Any defect found is filed for R7 — do not fix cross-owned files. |
| R6-4 | `src/components/StudioRoutes.tsx`, `src/components/StudioRoutes.test.tsx`, `src/components/ProjectWorkbench.tsx`, `src/components/ProjectWorkbench.test.tsx`, `src/styles.css`, NEW `src/components/StorageNotice.tsx` (+ test) | R5-3's editor banner still uses `className="workbench-resume workbench-storage-notice"` + `workbench-resume-icon/body`: `.workbench-resume:hover` (accent-green) and `:active { transform: scale(0.985) }` are pseudo-class rules that outrank the later `.workbench-storage-notice` base rule, so the editor-route warning animates like a clickable card — the exact defect R5-5 fixed on the workbench, re-introduced on the new surface. The banner is implemented twice with diverging markup. The editor banner urges "请及时导出" but offers no export affordance. The workbench notice ignores `recoverError` (quota) that the editor banner shows. | Red: DOM test asserting the editor banner's classList excludes `workbench-resume` (fails today). Fix: one shared `StorageNotice` component (dedicated classes only, warning palette, `role="status"`), consumed by both `MemoryModeNotice` (StudioRoutes) and the workbench notice; it takes `recoverError` and an optional export-affordance slot — workbench passes the per-project button list, editor passes at least a current-project export. Delete the duplicated markup. Metric: red test flips; a single-implementation assertion (grep-level or shared-component render); quota copy visible on both routes; existing refresh-on-recovery and one-download-per-gesture tests stay green. |
| R6-5 | `src/App.tsx`, `src/App.test.tsx`, NEW extracted `src/lib/*` module(s) | `App.tsx` is 2651 lines (repo rule: split beyond 400; baseline was 2466 — it *grew* 185 lines over five rounds despite "App.tsx 只做组合"). Honesty defect as the red anchor: `editorProjectStore.get(projectId)` → null while the store is degraded renders "项目不存在" (App.tsx:519 path), asserting a deletion it cannot know — the same defect class R5-4 fixed in the crash boundary, unfixed on the editor route. | Red: degraded store + unknown id → current copy claims nonexistence. Fix: health-aware missing-project copy ("本机数据库已降级，无法确认该工程是否还在磁盘上", link back to workbench); then extract ≥300 lines of cohesive non-visual orchestration (import/export handlers, project persistence effects, or collaboration submit wiring — pick the most self-contained cluster) into `src/lib` modules with pure seams; after R6-6 merges, thread its `roomPersistenceDegraded` value to `ProjectMenu` (one prop). Metric: copy red test flips; line-count delta recorded in the commit; all existing App tests pass **unmodified** (only additions allowed in the test file). |
| R6-6 | `src/lib/collaboration-client.ts` (+ its test), `src/lib/useCollaborationRoom.ts`, `src/lib/useCollaborationRoom.test.tsx`, `src/components/ProjectMenu.tsx` (+ test) | Room members never learn their live room will not survive a restart — the skip/trim decision exists only in the server console (R5-1 warn) and the next boot's log (R5-2). The people who lose data are the only ones not told. | Red: client drops R6-2's `persistedAtLastFlush` field today (no parse, no state). Fix: `collaboration-client` parses the additive field from create/join/snapshot responses; the hook exposes `roomPersistenceDegraded` as display-only state (one seam — do NOT touch the heal/terminal core); `ProjectMenu` renders a non-dismissible note ("该房间体量超过服务器持久化上限，服务器重启后将无法恢复，请及时导出备份") with precedence terminal > offline > persistence preserved. Metric: red-first parse test, hook state test, menu DOM test with precedence assertions. Sequenced after R6-2. |
| R6-7 | `src/components/AppErrorBoundary.tsx`, `src/components/AppErrorBoundary.test.tsx` | The crash screen's "返回项目列表" button calls `window.location.hash = "#/"; window.location.reload()` (AppErrorBoundary.tsx:327–330) — a hard reload that **destroys the memory fallback** the R5-4 copy directly above just said dies on reload; degraded users are one click from losing everything they were told to export. Also: the lazy `ownStore ??= createIndexedDbProjectStore()` fallback (line 117) survives as a second-instance vector. | Red: degraded store with listed memory projects → the return button today reloads without warning. Fix: when `listedHealth === "memory"` and projects are listed, the return affordance requires an explicit confirm naming the loss ("内存中的 N 个工程将永久丢失，确定离开？") or navigates without `reload()` (hash change alone keeps the heap alive — prefer this, and say why in-code); soft "重新加载" (state reset) stays as-is since it keeps the instance. Remove the lazy own-store fallback: the prop is always injected (main.tsx) and tests inject; delete the `createIndexedDbProjectStore` import. Metric: red DOM test flips; no-second-instance assertion; existing outcome-taxonomy and read-only tests stay green. |
| R6-8 | NEW `.github/workflows/ci.yml` | The repo has **no CI**: suite/lint/typecheck are orchestrator-manual, the R3 blocker was precisely a check nobody ran, and PR #14 is going ready-for-review with no reviewer-visible green (交付纪律: 本地测试通过 + agent 口头完成不是交付证据). | Deliverable: one workflow on `pull_request` + push to `agent/opt-continuous`: `npm ci` → `npm run typecheck` → `npm run lint` → full vitest — one job, serial steps, mirroring `scripts/run-heavy.mjs`'s no-parallel-validation rule. Pin the Node major to what the repo develops on. Metric: a green run visible on PR #14. Rollback: delete the file. |
| R6-9 | NEW `server/durability.integration.test.ts` (test-only; no product files) | R4-1/R5-1/R5-2 are each unit-tested, but no test walks the full server durability lifecycle end to end — the seams (shutdown flush → restore → quarantine → sidecar cap → re-persist) are exactly where multi-round work regresses. | Deliverable (proof task): one journey against real `dataDir` files: boot → create a small and an oversized-record room → lifecycle SIGTERM shutdown → assert snapshot file contents + skip warn with ids → reboot → assert restored count + prior-skip boot warn → corrupt the snapshot on disk → reboot → assert `.bad` quarantine + cold-boot silence → repeat corrupt boots past the sidecar cap → assert exactly 5 newest kept → final boot creates and persists again cleanly. Metric: the journey test green on the merged tree; defects found are filed for R7. |
| R6-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律: PR [#14](https://github.com/Xhhemoing/cengfan-map-studio/pull/14) flips to ready-for-review on the Round-5 verdict, so its description must carry the acceptance evidence and rollback map reviewers need. | Deliverable (docs, no code): PR description gains (a) Round-5 acceptance evidence (172 files / 1568 passed / 2 skipped; lint 0 errors / 6 warnings; typecheck 0; review verdict 7 ACCEPT + 2 ACCEPT-WITH-NITS, no blocker), (b) behavior changes + rollbacks: persist band as constituted (rollback = revert `d17e913` + `45e8a12`), shared store (revert `c709c77`), shutdown contract — `onDraining` throws no longer propagate (revert `9994b1f`), terminal submit rejections (revert `87e7be8`), (c) known follow-ups = this table. PROGRESS.md Round-6 section mirrors it. |

## Sequencing

R6-1 → R6-2 → R6-6 (persistence-visibility chain; each consumes the previous task's landed seam). R6-5's prop thread waits for R6-6; its extraction and copy fix can start immediately. R6-4 and R6-7 are order-free. R6-3 and R6-9 run **after** the code tasks merge — they prove the merged tree. R6-8 anytime. R6-10 last.

## Saturated — leave alone this round

`server/production.ts` (R4-5 + R5-8 complete the shutdown contract; every phase now reports), `src/lib/agent-session.ts` (R4-8 + R5-7 close the resume path; deadline file untouched as before), `src/components/AgentAssistant.tsx` resume UX (R5-9's state machine is done), `server/index.ts` quarantine/boot-log core beyond R6-2's additive scope, `src/lib/editor-project-store.ts` (the channel is one round old and correct), workbook import/export paths, `src/lib/collaboration-client.ts` heal/backfill core (R6-6 adds one parse site only).

## Frozen (unchanged)

`src/lib/project-store.ts` internals; layout stack; `src/lib/collaboration-operations.ts` (consume only); `server/ai/*`; SSE wire format (drain still must not emit `closed`; R6-2/R6-6 use HTTP response fields only); project-document rejection identity; DataWorkspace product code (bench only).
