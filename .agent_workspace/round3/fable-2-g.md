MODEL: claude-fable-5-thinking-xhigh

# Round 3 · R3-F2 · 项 G + 存活内容合规 — 完成报告

状态：已实现，未 commit / 未 push（按指令）。零支付内容。改动仅落在 4 个许可文件，`src/`、`server/`、README、CONTRIBUTING、宣发脚本零触碰（git status 复核：本人 diff 仅 USER_GUIDE.md + 国际部-12人.md 修改、CHANGELOG.md + docs/社区/ 新增；其余 src/* 改动属同轮其他 agent）。

## 1. 新增 `CHANGELOG.md`（仓库根）

- **头部自包含**：记录规则四要素（新增 / 修复 / 明确不做+原因 / 感谢）直接写在头部，未链接 `docs/宣发/反馈收集SOP.md`（PR #8 可能删除）。仅链接稳定文件：LICENSE、docs/开源与收费边界.md、docs/社区/贡献者致谢.md。
- **隐私规则入头部**：致谢只用 GitHub ID/昵称、禁「学校全名+个人」组合、转述意见不得含名单内容、无数据不虚构「收到 N 条」。
- **结构**：`[Unreleased]`（空，占位说明）+ `[0.1.0] - 2026-08-24` 快照，以产品口径记录 Round 3 全部 7 个用户可见项：提意见入口（A）、模板交换格式（B）、协作昵称+角色标签（E）、导出文件名（F）、模板下载恢复（D）、载入示例（H）、致谢规则（G）。每条附带隐私边界表述（零遥测、students 拒收、昵称不出本机、文件名无 token 等），与 round2 验收规格对齐。
- **「明确不做」节**：世界地图/海外地图模板（仅中国省级地图，海外去向=无锚点卡片）、PDF/小程序/账号/云端、遥测上报、VIP/会员/付费模板——与能力清单口径一致。
- **AGPL-3.0-only 声明 + 无支付承诺**在头部固定。
- **「感谢」节**：明确「规则先行、不放空表」，无虚构条目。

## 2. 新增 `docs/社区/贡献者致谢.md`

- **同等致谢原则**：文档 / 模板 / 脱敏数据 / 答疑 / 代码同等致谢（呼应 CONTRIBUTING「什么样的贡献都算」），一句劝退点反馈也算。
- **致谢载体**：CHANGELOG 点名（附 Issue/PR 链接）+ 本文名录 + 社区模板署名。
- **社区模板可选署名**：对齐 B 项格式规范（`author` ≤30 字符、仅昵称、可留空匿名、再分发不得移除署名、AGPL 随仓分发、投稿确认素材授权且无真实名单）。
- **命名与隐私**：GitHub ID/自选昵称、禁真实姓名、禁学校+个人组合、禁转述名单、本人可要求移除。
- **明确不做**：无 VIP/会员/专属付费模板；无虚构名单（名录节现为「暂无外部贡献者」纯文字，无假表格）；不承诺仓外实体回报（覆盖验收词表中「实体海报/证书」类越权承诺，措辞规避敏感词本身）。

## 3. 编辑 `docs/案例模板/国际部-12人.md`（合规修复）

删除的违规宣称（round2 附录 A1.4 第 2 条全覆盖）：
- 「含世界地图切换」「切换『世界地图』模板」「世界地图 + 手动微调」「切换世界地图」→ 全部改为真实能力口径：中国地图版式 + 海外去向标记「去向类型 = 海外去向」+ 无锚点卡片手动排布（与 USER_GUIDE 案例 C「海外去向以无锚点卡片展示」一致，且「海外去向」是 `binary-import.ts` 真实枚举值）。
- 「平均录取学校排名：Top 50（QS）」→ 删除（排名口径违规），数据特征仅保留去向国家并标注虚构。
- 衍生用途「留学咨询案例」→ 删除（A1.4 点名违规项）；保留毕业纪念册、校友录封面。
- 复核：文件内 0 命中 世界地图/升学率/录取率/QS/Top N/排名/留学咨询（rg 验证，唯一残留命中在 CHANGELOG「明确不做」节的合规否定句）。

## 4. 编辑 `USER_GUIDE.md`（3 处外科手术式行级修改，未重写）

真实模板表头来源：`src/lib/binary-import.ts` `createImportTemplateSheets()` = 学生姓名 / 录取院校 / 城市 / 去向类型（前三必填，去向类型可选、留空按中国去向）。

1. FAQ「Excel 导入失败？」：原答案「姓名/去向/省份」→ 改为真实表头 + 必填/可选说明 + 指引数据阶段下载 XLSX 模板 + 别名自动识别提示（所列别名 姓名/录取学校/所在城市/去向 均逐一核对存在于 `HEADER_ALIASES`）。
2. 二.1「必填字段：姓名、去向、省份/城市」→ 「学生姓名、录取院校、城市（去向类型可选：中国去向/海外去向）」——消除与 FAQ 的自相矛盾。
3. 快速上手第 4 步「（姓名、去向、省份）」→ 「（学生姓名、录取院校、城市）」。

## 验证纪律（failure → cause → fix → recheck）

- **failure**：USER_GUIDE FAQ 与案例模板宣称与代码事实不符（表头错误、世界地图/排名能力漂移）。
- **cause**：文档先于产品口径写就，未对 `createImportTemplateSheets()` 与能力清单校准。
- **fix**：上述 3+1 处最小改写，全部宣称落到可在 `src/lib/binary-import.ts` 验证的事实。
- **recheck**：`rg "世界地图|升学率|录取率|QS|Top ?[0-9]|留学咨询|排名"` 对 4 个改动文件 → 案例模板 0 命中（唯一命中为 CHANGELOG 合规否定句）；表头与别名逐一比对源码通过。按指令未跑测试（本项纯文档，无代码面）。

## 与验收规格的偏差（有意，按 Round 3 锁定范围）

- 未加 README「贡献与致谢」节（README 在禁改清单）。
- 未改写用户群运营手册 §四、未给 promo:check 加存在性检查（宣发脚本禁改；C 项已 DROP）。
- 文件名用任务指定的 `贡献者致谢.md`（验收稿曾写 `贡献者名录.md`）。
- 回滚方案：G 为纯新增/纯文档——删 CHANGELOG.md + docs/社区/ 即回滚功能面；国际部-12人.md 与 USER_GUIDE.md 属合规修复，按验收纪律永不回滚。
