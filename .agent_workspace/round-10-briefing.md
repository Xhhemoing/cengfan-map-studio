[Model: claude-fable-5]
# Round 11 Briefing (from the Round-10 SOTA review)

## Direction

Round 10 closed the disk-hygiene tail (AI-state `.tmp` + `.corrupt-*` cap, boot sweep) and cut the two biggest source files to 1146/1066 with every move machine-verified; the ratchet now makes shrinkage one-directional. What remains is (a) **two small evidenced defects** this review found in code: the AI state store's *load-path* errors bypass `safeError` and drop the underlying errno entirely (`server/ai/ai-state-store.ts:112` readFile failure, `:114` quarantine-rename failure — contradicting the module's own `:50` contract that the errno rides `cause`; R10-1 fixed only the persist path), and the boot sweep never visits a state file configured outside `dataDir` (`AI_STATE_FILE` / `config.aiStateFile`, `server/index.ts:1092` — the sweep reads `dataDir` only, verified); (b) the **size debt with honest measurements**: `server/index.ts` 1146 (AI route cluster `:834–1006` = 173 measured lines; after it the file is near its floor — setup `:245–525`, frozen SSE `:618–835`, health/workspace `:526–616` left alone because extracting ~91 lines would relocate the frozen persist-failure health surface), `server/index.test.ts` 2378 (74 `it(` under one describe at `:275` behind a 274-line prelude), `src/App.tsx` 1066 (legacy topbar `:811–870` = 60 lines, export-project dialog `:872–910` = 39 lines, both behind the existing `posterExport`/handler seams), and the four biggest un-split test files: `PosterCanvas.test.tsx` 983, `card-layout.test.ts` 958, `agent-session.test.ts` 696, `project-store.test.ts` 655 — the first two were explicitly deferred by the R9 freeze note ("their test files may be split in a later round"); that round is now.

Not tasked, with reasons: the idle-SSE-member remainder **cannot be fixed over HTTP** for a genuinely idle client (documented since R9; unfreeze bar unmet); the 12 MiB durable ceiling was **not newly evidenced** in Round 10 and stays a known-miss for the PR description only; the health/workspace route extraction is measured but **deliberately left** (frozen-surface risk outweighs ~91 lines); `styles.css` and the `src/data` files are listed debt that must not be hand-mangled into compliance.

Quality bar unchanged: **no metric, no merge; red repro first — as a separate red commit or with the observed red state recorded verbatim in the commit message; targeted vitest only** (`npx vitest run <file>`); typecheck (`tsc -p tsconfig.app.json` and `-p tsconfig.node.json`, `--noEmit`) exit 0 after every task; CI green on the merged tree. **New, from the Round-10 finding — ratchet merge discipline:** every merge commit must be ratchet-green on its **own** tree: run `npx vitest run scripts/file-size-ratchet.test.ts` on the merge result before pushing; allowlist updates (lower/delete your own file's entry) ride the merge itself, never a chore commit afterwards; a branch cut before someone else's allowlist change rebases before merging. Five of nine Round-10 merges were ratchet-red on their own trees and needed orchestrator healing — that stops here. One task = one exclusive writable file set — except the explicitly sequenced `server/index.ts` queue (R11-2 → R11-3 → R11-4) and the shared `scripts/file-size-allowlist.json`, where each task edits **only its own file's entry** and coordinates via rebase, not rewrite. A sequenced successor starts only after its predecessor's merge and must not rewrite the predecessor's additions. If a fix forces an edit in a cross-owned file, stop and file it.

## Tasks (exactly 10; writable sets disjoint except the sequenced index.ts queue and own-entry allowlist edits)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R11-1 | `server/ai/ai-state-store.ts`, `server/ai/ai-state-store.test.ts` — **narrow re-unfreeze of this one file pair; the rest of `server/ai/*` stays frozen** | Found by the R10 review in code: both load-path failures construct `StateStoreFailure` directly and bypass `safeError`, dropping the underlying errno — `:112` (readFile failure other than ENOENT → bare `AI_STATE_LOAD_FAILED`) and `:114` (quarantine-rename failure → bare "AI 状态文件隔离失败"). This contradicts the module's own `:50` contract ("原始 errno 挂在 cause 上") which R10-1 established for the persist path. | Red: directory-squat the state file path itself (`mkdir` at `filePath` before `createFileAiStateStore(...).load()`) → readFile rejects with real EISDIR (not ENOENT) → pin that the load rejection carries `cause.code === "EISDIR"` — today `cause` is absent. Fix: route both branches through `safeError` (or pass `{ cause }` explicitly); external `code`/`message` unchanged — this is observability, not shape. The `:114` branch is hard to force on a real fs under root (its rename target embeds `Date.now()`); fixing it rides the same `safeError` route and is covered by the `:112` pin plus the existing quarantine pins passing unmodified — do not invent a mock-fs red for it. Metric: red flips; all 7 existing pins green unmodified; typecheck 0. |
| R11-2 | `server/index.ts` (boot wiring only), `server/boot-tmp-sweep.test.ts`, `server/room-snapshot-store.ts`, `server/room-snapshot-store.test.ts` — **first in the index.ts queue** | Confirmed by the R10 review: `AI_STATE_FILE` / `config.aiStateFile` may resolve outside `dataDir` (`index.ts:1092`), and `sweepStaleTemporaryFiles` reads only `dataDir` — a crash-orphaned `<stateFile>.<pid>.tmp` beside a configured-elsewhere state file is never collected. | Red: boot `createReadyAiServer` with `AI_STATE_FILE` pointing into a second temp directory; seed `<basename>.4.tmp` beside it → pin it survives boot today. Fix: when `dirname(stateFile)` differs from `dataDir`, additionally sweep **only** `^<basename(stateFile)>\.\d+\.tmp$` in that directory — never the generic any-name shape there (an operator may point at a shared directory; the sweep must not eat another program's temporaries; say this in a comment). Cohesion: extend `room-snapshot-store.ts` (it owns the sweep) with the narrow-prefix variant; boot wiring calls both, still before any reader. Metric: red flips; the existing before-first-reader pin green unmodified; a foreign-named `.tmp` in the state-file directory pinned to survive; typecheck 0; allowlist untouched unless `index.ts` crosses its entry (it must not — the wiring is ~3 lines; if it does, lower/keep the entry honest in the same commit). |
| R11-3 | `server/index.ts`, NEW `server/ai-routes.ts` (+ NEW `server/ai-routes.test.ts`) — **sequenced after R11-2's merge; `server/index.test.ts` must not be edited** (the R10-4 discipline) | `server/index.ts` 1146. The AI route cluster is measured at **173 lines** (`:834–1006` on the current tree: `POST /api/ai/agent` `:834–915`, `parse-data` `:916–946`, `propose-edits` `:947–977`, `explain` `:978–1006`) — cohesive, no room/SSE coupling, and everything it needs from `server/ai/*` is already imported by index.ts (consume-only; the frozen modules are not edited). | Deliverable (seam extraction, the R10-4 discipline): thread an explicit deps struct (`sendAi`, `readJson`, byte caps, agent + otherAi limiters, ai backend, agent loop, budget receipts/ledger, `aiLogger`, `agentRuntime`) into `createAiRoutes(deps).handle(ctx)` returning true/false back to index.ts's matcher. Response shapes, status codes, abort/499 and 502 mapping, and rate-limit behavior byte-identical — quote the normalized-diff residue in the commit like `fad35fc` did (declare every mechanical delta). Existing AI pins in `index.test.ts` pass **unmodified**; new seam pins live in `ai-routes.test.ts`. Metric gate: named cluster relocated; all existing pins green unmodified; typecheck 0. Expected count (report, don't gate — R11-2 lands first in this file): index.ts ≈ 985 ± 25; state actual before/after; **lower the allowlist entry in this same merge**. |
| R11-4 | `server/index.test.ts` (split), NEW `server/index.*.test.ts` domain files + NEW shared fixture module (names indicative) — **sequenced after R11-3's merge; last in the index.ts queue** | 2378 lines, the largest file in the repo after the css/data listings — 74 `it(` in one `describe("unified application server")` at `:275` behind a 274-line prelude of fixtures and helpers. | Deliverable (the R9-8/R10-6 pattern): carve the prelude into a shared fixture module; split by domain (indicatively: auth/config, health+lifecycle, workspace, room lifecycle residue, SSE stream, AI routes, static/integration residue); zero assertion changes machine-checked (sorted `expect(` multiset byte-identical including the fixture module, `it` count 74 preserved — quote both in the commit); every file ≤ 400; keep the server-close/tmp-dir `afterEach` nets in the fixture as file-level hooks. Metric: all split files green in isolation and together; typecheck 0; **delete or lower the `server/index.test.ts` allowlist entry in the same merge**. |
| R11-5 | `src/App.tsx`, NEW `src/components/editor/*` components (+ tests), `src/App.shell-layout.test.tsx` (additions only) | App.tsx 1066 after R10-5. Two named branches remain cleanly severable, measured: the legacy topbar `:811–870` (60 lines: brand, workflow stepper slot, toolbar groups, export buttons) and the export-project confirmation dialog `:872–910` (39 lines incl. backdrop; all state flows through the existing `posterExport` seam). | Deliverable (behavior-preserving, the R10-5 discipline): extract both branches into `src/components/editor/*` with props typed at the seam; DOM structure, class names, roles, aria labels, and 文案 verbatim — all existing App split-file pins pass **unmodified**; latest-ref and effect wiring stay in App. Metric: App.tsx ≤ 990 (no queue predecessor — this is a gate, measured from today's 1066 with the named branches summing ~99 lines); existing App tests green unmodified; new files ≤ 400; typecheck 0; **lower the App.tsx allowlist entry in this same merge** — do not repeat R10-5's stale-entry lapse. |
| R11-6 | `src/components/canvas/PosterCanvas.test.tsx` (split), NEW `src/components/canvas/PosterCanvas.*.test.tsx` + NEW shared harness (names indicative) — **PosterCanvas product code stays frozen; test-only** | 983 lines against the 400 rule; the R9 freeze note explicitly deferred this test split to a later round. | The R10-6 discipline: domain split + shared harness (keep the tracked-roots `afterEach` drain so the R9-6 guard stays a reporter); zero assertion changes machine-checked (`expect(` multiset + `it` counts quoted in the commit); every file ≤ 400. Metric: split files green in isolation and together; typecheck 0; **delete the allowlist entry in the same merge**. |
| R11-7 | `src/lib/card-layout.test.ts` (split), NEW `src/lib/card-layout.*.test.ts` + NEW shared fixture (names indicative) — **card-layout.ts, worker, and solver stay frozen; test-only** | 958 lines; same deferred-split note as R11-6. Pure-function node-env tests — the easiest split in the round. | Same discipline: domain split + shared fixture; zero assertion changes machine-checked; every file ≤ 400; do not touch the layout benches' gating semantics (the 400-card median discipline stays as-is). Metric: split files green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**. |
| R11-8 | `src/lib/agent-session.test.ts` (split), NEW `src/lib/agent-session.*.test.ts` + NEW shared fixture (names indicative) — **agent-session.ts product stays untouched** | 696 lines against the 400 rule; the largest remaining `src/lib` test after the queue. | Same discipline: domain split + shared fixture, zero assertion changes machine-checked, every file ≤ 400. Metric: green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**. |
| R11-9 | `src/lib/project-store.test.ts` (split), NEW `src/lib/project-store.*.test.ts` + NEW shared fixture (names indicative) — **project-store.ts product stays untouched (saturated list)** | 655 lines against the 400 rule. | Same discipline: domain split + shared fixture, zero assertion changes machine-checked, every file ≤ 400. Metric: green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**. |
| R11-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) — **last** | 交付纪律: the deferred R10-10 plus Round-10 evidence must reach the PR surface ("R10-10 在 fable 之后收口" is on record in the description). | Deliverable (docs, no code): description gains (a) Round-10 gates: 238 files passed / 1 skipped; 1942 passed / 2 skipped (93.28s); lint 0 err / 1 frozen warning; typecheck 0; review 5 ACCEPT + 4 NITS, no blocker; (b) rollbacks per merge: ratchet (revert `5b1e35c`), AI-state hygiene (revert `964a641`), boot sweep (revert `bd65d04` — note the sweep deletes matching `.<pid>.tmp` on next boot by design), static-files extract (revert `b5e17df`), room-routes extract (revert `96b353d`), App extract (revert `acce6f6`), test splits (revert `7d2717f` / `4010acc` / `05e72d2`); (c) known misses stated plainly: idle SSE member (SSE frozen, documented); 12 MiB ceiling (unchanged); ai-state load-path `cause` until R11-1; out-of-dataDir sweep gap until R11-2; index.ts 1146 / index.test.ts 2378 / App.tsx 1066 with the R11 queue filed; (d) the ratchet merge-discipline rule written into PROGRESS.md's Round-11 notes verbatim so the next orchestrator inherits it. |

## Sequencing

`server/index.ts` queue (strict): R11-2 → R11-3 → R11-4 (R11-4 touches only `index.test.ts` + new files, but sequences after R11-3 so a discovered pin-move becomes a stop-and-file, not a race). R11-1 independent (server/ai pair only). R11-5 independent of the server queue. R11-6, R11-7, R11-8, R11-9 order-free. R11-10 last. Parallel-safe starts at round open: R11-1, R11-2, R11-5, R11-6, R11-7, R11-8, R11-9. Allowlist coordination: each task edits only its own file's entry, in the merge itself, and rebases onto whatever entries changed under it — five Round-10 merges landed ratchet-red on their own trees; zero is the Round-11 number.

## Saturated — leave alone this round

The persist-failure chain end to end (closed R7-1→R9-3, errno-proven R9-4; only a new evidenced defect reopens); `server/index.ts`'s health/workspace routes (measured ~91 lines but entangled with the frozen health surface — documented, not tasked); `server/collaboration.ts`; `server/room-routes.ts` and `server/static-files.ts` (fresh, tested); `src/components/ProjectMenu.tsx`, `ProjectWorkbench.tsx`, `StorageNotice.tsx`; `src/lib/editor-project-store.ts` + `project-store.ts` product; the R9-5 seams and R10-5 editor components (fresh); `server/durability-*.ts` journeys (extend only with new mechanisms); the R9-6 guard and the 38-file unmount pattern; `.github/workflows/ci.yml`; `App.collaboration-terminal.test.tsx` at 402 (ratchet holds it; still not worth churn); `src/styles.css` and `src/data/*` (listed debt; splitting generated/asset-like files is churn without safety yield).

## Frozen (campaign runs to round 20)

`server/ai/*` **except the R11-1 narrow re-unfreeze of `ai-state-store.ts` + its test** (one evidenced defect: load-path `cause` loss at `:112`/`:114`; the agent loop, schemas, providers stay frozen — R10-1's unfreeze is otherwise closed); `src/lib/collaboration-operations.ts` (consume only); SSE wire format **still frozen** — R11-3 must not touch the events/events-ticket block (byte-verified identical this round; keep it that way), drain must not emit `closed`; the idle-member remainder stays documented-not-tasked (cannot be HTTP-fixed); layout stack internals (`card-layout.ts`, worker, solver) — R11-7 splits only the tests; PosterCanvas product (R11-6 splits only the tests); project-package format; project-document rejection identity; DataWorkspace product code (the exhaustive-deps warning at `DataWorkspace.tsx:257` stays the repo's one intentional lint warning); the persist-failure chain unless newly evidenced; payment/套餐/模板手续费 boundaries per `docs/开源与收费边界.md` — no payment or billing code in this repo.
