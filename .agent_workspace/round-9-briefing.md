[Model: claude-fable-5]
# Round 10 Briefing (from the Round-9 SOTA review)

## Direction

Round 9 closed the failure-visibility chain at its last actively-editing surface and proved it over a kernel errno; red-first held on every row that demanded it. What remains is (a) the **disk-hygiene tail** the round itself filed and this review confirmed in code: the AI state store writes `<file>.<pid>.tmp` → `rename` with no cleanup (`server/ai/ai-state-store.ts:73`, the exact pre-R9-1 pattern) and quarantines corrupt state to uncapped `.corrupt-<ts>` sidecars (`:71`), and nothing anywhere sweeps a `.tmp` orphaned by a crash *between* write and rename — R9-1 only cleans up in-process; (b) the **size debt**, now with honest numbers: `server/index.ts` 1547 (module-scope static-file cluster `:255–424` = 170 movable lines; `createAiServer` `:425–1457` ≈ 1033), `src/App.tsx` 1484 (the JSX render branches: loading shell, missing-project shell, global-settings screen `:1034`, stage layout, legacy editor return `:1056–1456`), and test files `DataWorkspace.test.tsx` 1362, `useCollaborationRoom.test.tsx` 1265, `collaboration-client.test.ts` 948. The R9-7 lesson is folded into every metric below: **shared-queue line targets are stated net of the queue's earlier deltas, cluster sizes are measured (cited with line ranges), and the merge gate is the named scope moving verbatim — the absolute count is reported, not gated, wherever a queue predecessor can shift it.**

Not tasked, with reasons: the idle-SSE-member remainder is documented in the review and **cannot be fixed over HTTP** for a genuinely idle client (zero steady-state requests) — it does not meet the SSE unfreeze bar; the 12 MiB durable ceiling was **not newly evidenced** in Round 9 and stays a known-miss for the PR description only.

Quality bar unchanged: **no metric, no merge; red repro first — as a separate red commit or with the observed red state recorded verbatim in the commit message; targeted vitest only** (`npx vitest run <file>`); typecheck (`tsc -p tsconfig.app.json` and `-p tsconfig.node.json`, `--noEmit`) exit 0 after every task; CI green on the merged tree. One task = one exclusive writable file set — except the explicitly sequenced `server/index.ts` queue (R10-2 → R10-3 → R10-4) and `src/App.tsx` pair (R10-5 after nothing, but R10-5's App test additions must not touch R10-6/7/8 files). A sequenced successor starts only after its predecessor's merge and must not rewrite the predecessor's additions. If a fix forces an edit in a cross-owned file, stop and file it.

## Tasks (exactly 10; writable sets disjoint except the sequenced index.ts queue)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R10-1 | `server/ai/ai-state-store.ts`, `server/ai/ai-state-store.test.ts` — **scoped unfreeze of this one file pair; the rest of `server/ai/*` stays frozen** | Confirmed by the R9 review in code: the AI state writer (`ai-state-store.ts:73`) is the exact pre-R9-1 pattern — `writeFile(<file>.<pid>.tmp)` then `rename`, no cleanup on the rename path; and corrupt-state quarantine (`:71`) renames to `.corrupt-<Date.now()>` with no cap, the same unbounded-sidecar shape `.bad` had before `pruneRoomSnapshotSidecars`. | Red ×2, separate red commit or verbatim: (a) squat the state file's *final* path with a directory → write succeeds, rename rejects with real EISDIR → pin the `.tmp` remains (readdir); (b) seed 7 `.corrupt-*` sidecars → boot → pin all 7 remain. Fix: rename-failure best-effort unlink with the original error rethrown (R9-1's exact contract — cleanup failure must not mask the errno; copy the 6-line catch/`rm` inline rather than importing from `server/room-snapshot-store.ts`, which already imports from `server/ai` — do not create the tangle); cap `.corrupt-*` at 5 keeping the newest (mirror `MAX_ROOM_SNAPSHOT_SIDECARS`), prune best-effort. Metric: both reds flip; original errno propagation pinned; existing state-store pins green unmodified; typecheck 0. |
| R10-2 | `server/room-snapshot-store.ts`, `server/room-snapshot-store.test.ts`, `server/index.ts` (boot wiring only), `server/index.test.ts` — **first in the index.ts queue** | R9-1 cleans up only in-process. A crash between write and rename (SIGKILL, power loss) orphans `<file>.<pid>.tmp` forever: nothing matches `.tmp` at boot (`pruneRoomSnapshotSidecars` prunes `.bad` only). Filed by R9-1's ledger, confirmed by the R9 review. | Red: seed stale orphans in `dataDir` (`workspace.json.12345.tmp`, `collaboration-rooms.json.999.tmp`, `ai-state.json.4.tmp`) → boot a server → pin all remain today. Fix: a `sweepStaleTemporaryFiles(dataDir)` in `room-snapshot-store.ts` (cohesive: it owns `writeFileAtomically`), called **once at boot before any writer arms** — at that instant even own-pid names are stale from a previous boot. Match only `^<name>.<digits>.tmp$` shapes; never touch `.bad`/`.corrupt-*`/live data; deletion best-effort (a sweep failure must not block boot — log and continue). Metric: red flips; a freshly-written good envelope and existing sidecars survive the sweep (pinned); R9-4's journeys green untouched; typecheck 0. |
| R10-3 | `server/index.ts`, `server/index.test.ts` (move-only edits), NEW `server/static-files.ts` (+ NEW test file) — **sequenced after R10-2's merge** | `server/index.ts` 1547. The module-scope static-file cluster is measured at **170 lines** (`:255–424`: `contentTypeFor`, `securityHeaders`, `cacheControlFor`, `acceptsGzip`, `statOrUndefined`, `serveStatic`) with no room/AI coupling — the honest next extraction the R9-7 writable set didn't authorize. | Deliverable (move-only, the R9-7/R9-8 discipline): relocate the cluster verbatim into `server/static-files.ts`; move (not rewrite) the tests that pin it; machine-check with `-U0` diff on `expect(` lines quoted in the commit, `it` counts identical. Metric gate: the named cluster relocated verbatim, moved tests green in isolation, full server targeted run green, typecheck 0. Expected count (report, don't gate — R10-2 lands first in this file): index.ts ≈ 1385 ± 15; state the actual before/after in the commit. |
| R10-4 | `server/index.ts`, `server/index.test.ts`, NEW `server/room-routes.ts` (+ NEW test file if splitting pins) — **sequenced after R10-3's merge; last in the index.ts queue** | `createAiServer` ≈ 1033 lines. The non-SSE room HTTP handlers are a measured cohesive block (`:808–1041` on the current tree, ≈ 233 lines: rooms POST, room GET/DELETE, invitations, join, transactions, members, leave, access, operations). The SSE events handler (`:1042` onward) is **excluded** — the wire is frozen and it stays in index.ts untouched. | Deliverable (seam extraction, the R9-5 discipline — not move-only, since handlers close over server state): thread an explicit deps struct (roomStore, `roomPersistenceFields`, limiter/CORS/send helpers, byte caps) into `server/room-routes.ts`; response shapes, error codes, and header behavior byte-identical — existing exact-shape pins (R8-2, R9-2) must pass **unmodified**; no SSE edits, no `Prefer` semantics change. Metric gate: named block relocated, all existing room pins green unmodified, typecheck 0. Expected count (report, don't gate): index.ts ≈ 1160 ± 30; state actuals. Any behavior difference discovered is a stop-and-file, not a drive-by fix. |
| R10-5 | `src/App.tsx`, NEW `src/components/editor/*` components (+ tests), `src/App.shell-layout.test.tsx` / `src/App.lifecycle-seams.test.tsx` (additions only) | App.tsx 1484 after R9-5; what remains is mostly JSX: the project-loading shell, missing-project shell, global-settings screen (`:1034`), stage-layout branch, and the ~400-line legacy editor return (`:1056–1456`). R9-5's seam playbook doesn't apply to JSX; the component-split playbook (R8-7's `WorkbenchBackButton`) does. | Deliverable (behavior-preserving): extract ≥ 2 of the named render branches into `src/components/editor/*` with props typed at the seam; DOM structure, class names, roles, and 描述文案 verbatim — all existing App split-file pins pass **unmodified** (they render through the same DOM); latest-ref and effect wiring stay in App. Metric: App.tsx ≤ 1250 (this file has no queue predecessor — the number is a gate, measured from today's 1484 with the named branches summing ≥ 280 lines); existing App tests green unmodified; typecheck 0. |
| R10-6 | `src/lib/useCollaborationRoom.test.tsx` (split), NEW `src/lib/useCollaborationRoom.*.test.tsx` files + NEW shared fixture module (names indicative) | 1265 lines against the 400 rule — the biggest `src/lib` test. Visible domains: subscribe/reconnect/heal, persistence notes (R7-3/R8-3/R9-3 pins), terminal rejection, offline/online, invitation/access. | Deliverable (the R9-8 pattern): split by domain + one shared harness (the mount/flushSync/unmount net — keep the tracked-roots `afterEach` drain so R9-6's guard stays a reporter); zero assertion changes, machine-checked (`-U0` `expect(` multiset + identical `it` counts quoted in the commit); every file ≤ 400. Metric: all split files green in isolation and together; typecheck 0. |
| R10-7 | `src/components/DataWorkspace.test.tsx` (split), NEW `src/components/DataWorkspace.*.test.tsx` files + NEW shared fixture (names indicative) — **DataWorkspace product code stays frozen; test-only** | 1362 lines, the largest test file in the repo after R9-9. | Same discipline as R10-6: domain split + shared fixture, zero assertion changes machine-checked, every file ≤ 400, unmount net preserved, the frozen exhaustive-deps warning untouched (no product edits). Metric: split files green in isolation and together; typecheck 0. |
| R10-8 | `src/lib/collaboration-client.test.ts` (split), NEW `src/lib/collaboration-client.*.test.ts` files + NEW shared fixture (names indicative) | 948 lines. Domains visible: request/retry/timeout transport, room CRUD parsing, persistence normalization (R8-3/R9-3 pins), operations/conflict. | Same discipline: domain split + shared fixture, zero assertion changes machine-checked, every file ≤ 400. The scripted-fetch fixtures move to the shared module so R9-3's ack-discipline pins and the transport pins drive one script. Metric: split files green in isolation and together; typecheck 0. |
| R10-9 | NEW `scripts/file-size-ratchet.test.ts`, NEW `scripts/file-size-allowlist.json` (names indicative) | Three rounds of splits (R8-8, R9-8, R9-9, R10-6/7/8) have no guard: nothing stops a new 1500-line test file, exactly the gap R9-6 closed for unmounts. Vitest already includes `scripts/**/*.test.ts`. | Red: add a temporary oversized file locally (not committed) → the ratchet fails naming it; record verbatim. Fix: a node-env test that walks `git ls-files` source/test files and fails any file > 400 lines unless listed in the allowlist **at or below its recorded count** (ratchet: counts may only shrink; shrinking updates the list in the same commit). Seed the allowlist with today's violators and counts (index.ts 1547 — note R10-2/3/4 will shrink it in-queue, whichever lands later updates the entry; App.tsx 1484; data files; PosterCanvas; card-layout; the test files above). Metric: guard green on the merged tree; deliberate-oversize red recorded; no full-suite timing regression beyond noise. |
| R10-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律: Round-9 evidence must reach the PR surface (R9-10 covered through Round 8). | Deliverable (docs, no code): description gains (a) Round-9 gates: 222 files / 1905 passed / 2 skipped (88.47s); lint 0 err / 1 frozen warning; typecheck 0; review 8 ACCEPT + 1 NITS, no blocker; (b) behavior changes + rollbacks: `.tmp` cleanup both writers (revert `baff4c9`), ack persistence + health `at: 0 → null` (revert `2334a3d`, ops shape), client ack chain (revert `d6af547`), App extract (revert `dfb7f37`), leaked-root guard (revert `b1a398a`), snapshot-store extract (revert `eac6d7b`), test splits (revert `a789835` / `51f9ed0`), rename journey (revert `a9f3087`); (c) known misses stated plainly: idle SSE member dark on streaks (SSE frozen, documented); AI state store `.tmp`/`.corrupt-*` hygiene until R10-1; no boot `.tmp` sweep until R10-2; 12 MiB durable ceiling (unchanged, not newly evidenced); index.ts 1547 / App.tsx 1484 with the R10 queue filed. PROGRESS.md Round-10 section mirrors it. |

## Sequencing

`server/index.ts` queue (strict): R10-2 → R10-3 → R10-4. R10-1 independent (server/ai only). R10-5 independent of the server queue. R10-6, R10-7, R10-8, R10-9 order-free. R10-10 last. Parallel-safe starts at round open: R10-1, R10-2, R10-5, R10-6, R10-7, R10-8, R10-9. R10-9's allowlist entry for `server/index.ts` is updated by whichever queue task lands after it — coordinate via rebase, not rewrite.

## Saturated — leave alone this round

The persist-failure chain end to end (server fields, ack, client normalization, hook notes, menu copy — closed R7-1 through R9-3 and errno-proven by R9-4; only a new evidenced defect reopens), `server/collaboration.ts`, `src/components/ProjectMenu.tsx`, `src/components/ProjectWorkbench.tsx` + `StorageNotice.tsx`, `src/lib/editor-project-store.ts` + `project-store.ts`, the R9-5 seam modules (fresh, tested), `server/durability-*.ts` journeys (R9-4 just landed; extend only with new mechanisms), the 38-file unmount pattern (R9-6 guards it), `.github/workflows/ci.yml`, `App.collaboration-terminal.test.tsx` at 402 (2 lines over aspiration; not worth churn).

## Frozen (campaign runs to round 20)

`server/ai/*` **except the R10-1 scoped unfreeze of `ai-state-store.ts` + its test** (confirmed defects only; the agent loop, schemas, providers stay frozen); `src/lib/collaboration-operations.ts` (consume only); SSE wire format **still frozen** — R10-4 must not move or edit the events handler, and drain must not emit `closed`; the idle-member remainder does not meet the unfreeze bar (documented in the R9 review; it cannot be HTTP); layout stack internals (`card-layout.ts`, worker, solver) unless a new evidenced defect — their *test* files may be split in a later round, not this one; project-package format; project-document rejection identity; DataWorkspace product code (the exhaustive-deps warning stays the repo's one intentional lint warning — R10-7 splits only its tests); ProjectMenu unless a new evidenced defect; payment/套餐/模板手续费 boundaries per `docs/开源与收费边界.md` — no payment or billing code in this repo.
