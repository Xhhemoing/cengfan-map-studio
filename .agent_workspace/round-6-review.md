[Model: claude-fable-5]
# Round 6 SOTA Review

Reviewed tree: `/tmp/opt-merge`, branch `cursor/r6-fable-review-187a` cut from `origin/agent/opt-continuous` @ `5b7fe2a` (includes the required R6-3 merge `aaddff2`, R6-9 merge `9d19bc9`, and ledger `5b7fe2a`; origin gained one docs-only ledger commit `f0977d5` after this review's fetch — outside scope, contents consistent with what was verified here). Round-6 scope (against R5 close `23a03e2`): 40 files, +3823/−597. Every merge diff was read against its first parent with per-merge file-ownership checks. No product code was modified by this review. `npm test` / lint were NOT run (orchestrator serializes; recorded: **181 files, 1656 passed / 2 skipped; lint 0 errors / 7 warnings**). This review DID run `tsc -p tsconfig.app.json --noEmit` and `-p tsconfig.node.json --noEmit` — both exit 0. Unlike prior rounds, the gate numbers are no longer trust-only: R6-8's CI exists and this review verified via `gh run list` that the run on exactly `5b7fe2a` is green (run 32778740759, success, typecheck→lint→test serial).

## Headline

**No merge blocker.** All eleven rows accepted: 6 ACCEPT, 4 ACCEPT-WITH-NITS, 0 REJECT (the `b7ed418` hotfix is accepted as an orchestrator test-only fix, outside task verdicts). The Round-5 flagship defect is genuinely closed the way the briefing prescribed: an API-accepted near-limit room now survives restart with its operation history trimmed instead of dying whole, the trim is exercised at both the per-room gate and the aggregate budget (trim-before-evict), and the store-level restore behavior — stale client at an old baseline gets `VERSION_CONFLICT`/"增量历史已被裁剪，请重新获取完整快照" — is pinned. The visibility chain R6-1→R6-2→R6-6→R6-5b verifiably reaches the DOM (cross-check 2). The two proof tasks are real journeys, and R6-9 doubles as the end-to-end proof that the residual band (snapshot-only records over 8 MiB, nothing to trim) is still skipped **by design and no longer silently**: created 201, live 200, skipped with ids at flush, warned with ids at next boot, 404 after restart.

The largest finding of this review is again a cross-task seam, not a task failure: `persistedAtLastFlush: false` covers both *skipped* and *trimmed* rooms (R6-2 defines it as "完整写下", and its tests pin trimmed → false), but the R6-6 member-facing copy asserts "服务器重启后将无法恢复" — which R6-1's own tests prove is **false for trimmed rooms** (they restore with snapshot, access, and version intact; only stale clients must re-snapshot). Since R6-1 made trim the common degraded outcome and skip the rare one, the most common warning a member will ever see now overstates the loss. That, plus the persist-failure staleness of `lastPersistOutcome` (nit R6-1.1), heads Round 7.

## Per-task verdicts

### R6-1 history-first persist degradation (`5bc3b29`) — ACCEPT-WITH-NITS
- All briefed claims verified in code. Per-room gate: a record over `MAX_PERSISTED_ROOM_BYTES` with non-empty history is retried with `operationHistory: []` before being skipped; the boundary is `usedBytes <= maxBytes` so an exactly-8-MiB record passes. Aggregate: budget raised 8→12 MiB with the two-5-MiB-rooms rationale in-code; the eviction loop trims the largest retained room's history first and only evicts whole rooms when no retained room has anything left to trim (pinned: `BUDGETA` trimmed, both rooms retained, output ≤ budget). Envelope gains additive `trimmedRoomIds` (sorted, byte-accounted in the estimator like `skippedRoomIds`); the flush warn names both groups with ids and states the different consequences. Restore path pinned at the store level exactly as demanded: trimmed room restores snapshot + authorization + version 64, and `getOperations(…, 0)` throws `VERSION_CONFLICT` with "增量历史已被裁剪，请重新获取完整快照" and `currentVersion` — the safety argument (clients re-snapshot) is inherited from the pre-existing trimmed-interval contract, not asserted by fiat. `lastPersistOutcome()` returns defensive copies and is recorded per successful persist. Interval flush rejections now `console.error` once per failure streak, cleared on success (four-phase mock pins report → silence → clear → re-report). Estimator-fidelity corpus test asserts `jsonByteLengthWithin` equals `Buffer.byteLength(JSON.stringify(record))` **exactly** (quotes, backslashes, all 32 C0 controls, paired and unpaired surrogates, mixed) and goes undefined at exactly one byte less. Red-first discipline held (`e8040f3` red → `4d404eb` fix).
- Nits (fold to R7): (1) **`latestPersistOutcome` is only written on persist success** — during a disk-failure streak, `lastPersistOutcome()` keeps reporting the last good flush, so R6-2's `/api/health.rooms.lastFlush` and `persistedAtLastFlush` are stale-optimistic exactly when persistence is broken; the only signal is the deduped console.error (R7-1/R7-2). (2) **The bench no longer measures the new worst case.** The "standard 12 MiB" single-room cell now exercises the *skip path* (record > 8 MiB per-room cap, empty history → omitted), and no cell measures a multi-room aggregate near the 12 MiB budget; by the R5 calibration table that worst-case flush would be ~86 ms on the calibration machine (~50 ms here, where 8 MiB = 33.19 ms). The known 8-MiB > 50 ms miss is honestly documented in the bench header, and the bench does assert output ≤ budget + trimmed-room count — but the budget raise's occupancy cost is undocumented (R7-6). (3) Trim is all-or-nothing (whole history), which is the simple/safe choice; partial trim is not worth pursuing.

### R6-2 persist mortality over HTTP (`be82abb`) — ACCEPT
- All four surfaces verified. Boot warn now lists the skipped ids (deduped, sorted for cross-restart log comparison; count = `max(skippedRoomCount, ids.length)` for backward compat with count-only, ids-only, and pre-R5-1 envelopes). `/api/health` gains `rooms: { restoredAtBoot, skippedAtLastShutdown: { count, ids }, lastFlush }` with the boot facts captured once at construction ("日志滚走之后它就是唯一能被外部观测到的证据") and cold boot reporting zeros. `persistedAtLastFlush` rides create (sibling field, 201), join (sibling, 200), and snapshot (on the room projection): never-flushed rooms report `true` with the ≤-one-interval lag documented in-code; skipped and trimmed rooms report `false`; matching is case-insensitive against the store's uppercased ids (pinned with lowercase outcome ids); a store without `lastPersistOutcome` degrades to `true`/`null` instead of crashing the endpoint (pinned by injecting a stripped store). Tests drive a real shutdown→restore cycle for the health payload, not fixtures only. Red-first held (`2a8b3e9` → `e0c7857`).
- Observations, no verdict impact: the health endpoint exposes room ids unauthenticated — briefed, ops-facing, and ids alone grant no access (join needs an invite token, snapshot needs the room token); the boolean's skip/trim conflation is documented honestly here ("完整写下") — the copy defect it enables is charged to the chain (cross-check 2), with the split as R7's headline.

### R6-3 crash-disaster integration proof (`aaddff2`) — ACCEPT
- Exclusive-file discipline exact: one new test file, no product edits. The journey is the real thing, not a re-enactment: broken `IDBFactory` stubbed before module load, `vi.resetModules`, and the **actual module graph** (`editor-project-store`, `StudioRoutes`, `AppErrorBoundary`, `main`) — with the R5-3 identity assert `workbenchStore === editorProjectStore` up front. Steps verified: workbench seeds + user-created project both land in the shared memory copy; the editor route `get`s the same project (the split-brain regression would render 项目不存在 here); crash under `AppErrorBoundary` with an empty mirror lists **both** memory projects under the 仅存在于本次会话内存中 urgency copy with the degraded prefix and a negative match against disk-implying phrases; two gestures produce two `cengfan-recovery-*` files; `put`/`remove` spies stay at zero through both the rescue and the quota phase; then the phased factory flips to quota mode, the store's real background recover probe runs, and the genuine `onRecoverError` reaches the workbench banner through the channel (`quota-exceeded`, health stays `memory`, both projects still listed).
- Both filed R7 items are **confirmed from code** (cross-check 3): `createProject` does `put` + navigate with no `refresh()` (ProjectWorkbench.tsx:155–163, while `renameProject` at :173 awaits `refresh()`), and `editor-project-store.ts:23` constructs the singleton without passing or exposing the `scheduleRecover` option that `createIndexedDbProjectStore` already accepts (project-store.ts:595/622) — forcing this test to hijack global `setInterval` and pattern-match the 20 s period, which it documents as an R7 ask in-code.

### R6-4 unified StorageNotice (`4d7e3a2`) — ACCEPT-WITH-NITS
- The red test is exactly the briefed one (editor banner classList excludes `workbench-resume`, plus no `workbench-resume-icon/body/cta` descendants) and it now passes against a single shared `StorageNotice` (dedicated `workbench-storage-notice-*` classes only, `role="status"`, warning palette) consumed by both routes; the duplicated markup in `ProjectWorkbench` and the old `MemoryModeNotice` is deleted; grep confirms `workbench-resume` survives in those files only inside an explanatory comment. Quota copy is now symmetric: the workbench receives `recoverError` from the channel-subscribed route and the typed message takes precedence over the generic detail on both surfaces (pinned both in `StorageNotice.test.tsx` and route tests). The editor banner's 请及时导出 finally has an exit: a current-project export button (`aria-label="导出当前项目"`) with an in-banner `role="alert"` failure row; one-download-per-gesture is re-pinned on the editor route with the download-stub filename ledger.
- Nits: (1) lint went 6→7 warnings — `projectPackageFileName` (a non-component export) moved into the component file and trips react-refresh; recorded in PROGRESS, cheap to fix by moving the helper to `src/lib` (R7-4). (2) The workbench's own export failures still surface in the generic error section rather than the banner's `StorageNoticeActionError` row — cosmetic asymmetry, noted only.

### R6-5 honest missing-project copy + App split (`937337d`) — ACCEPT-WITH-NITS
- The honesty fix mirrors R5-4's crash-boundary taxonomy on the editor route: `loadStoredProject` captures `healthAtRequest` before `get` and `health` after; `resolveMissingProjectNotice` asserts deletion only for persistent→persistent not-found, distinguishes degraded-throughout, degraded-during-read, and read-failed ("读取失败…这不等于它已经被删除"), and every unconfirmed variant warns against clearing browser data. The DOM pin is adversarial in the right way: `data-missing-project="unconfirmed"`, `data-store-health="memory"`, and a negative assert on the old 项目不存在或已删除 copy. Extraction: App.tsx 2651→2322 (−329, ≥300 as briefed), six cohesive `src/lib` modules (collaboration-send, editor-workspace-persistence, layout-health-input, missing-project-notice, student-transactions, template-capture) each with their own tests; `src/App.test.tsx` additions-only (+36/−0), and the merged tree's full suite green on CI pins the extraction behaviorally.
- Nits: (1) **file-discipline deviation**: the merge also edits `src/components/AppProjectMode.test.tsx` (outside the briefed set), renaming and re-pointing the rejected-`get` expectation from the old dishonest copy to 无法读取这个项目, and giving the store stub `health: "persistent"`. The change is *forced by the fix* (the old test pinned the defect) and the substance is right — but it modifies a pre-existing assertion in a cross-owned file, which the briefing's "existing tests pass unmodified" rule existed to prevent; recorded so R7 briefings pre-authorize such collisions explicitly instead of leaving implementers to improvise. (2) App.tsx at 2322 lines is still ~5.8× the repo's 400-line rule — direction is right, keep extracting (R7-9).

### R6-5b thread `roomPersistenceDegraded` (`50d5755`) — ACCEPT
- Red-first (`54ff54e`) then the one-line prop thread. The added App test is the end of the chain done properly: a real create response with `persistedAtLastFlush: false` as the sibling field drives client parse → hook state → App prop → the rendered `data-collaboration-persist="degraded"` note, with negative asserts that neither terminal nor offline state was disturbed.

### R6-6 room persist degradation to members (`9f93022`) — ACCEPT-WITH-NITS
- Wiring verified at all three layers. Client: `persistenceFlagOf` accepts only JSON booleans — a missing key stays missing (old servers can't be misread as a verdict), and both response shapes are parsed (sibling on create/join, on-room for snapshot) with the rationale in-code. Hook: `roomPersistenceDegraded` is display-only (no contact with heal/terminal/offline logic — the diff confirms one state + one recorder + call sites); `undefined` keeps the previous statement rather than clearing it ("快照没带字段不代表房间忽然又安全了"); state resets on room switch and on leave; join-then-snapshot ordering lets the newer statement win. Menu: precedence terminal > offline > persistence with a per-case pin for each, non-dismissible pinned (no button inside the note), and the note stays out of the disconnected join form. Red-first held (`5c18d02` → `b09ed7b`).
- Nit (the round's main carried defect, filed as R7's headline): the copy 该房间体量超过服务器持久化上限，服务器重启后将无法恢复 is wrong for **trimmed** rooms — R6-1's near-limit test proves a trimmed room restores with snapshot, access, and version intact. The implementers followed the briefed copy and the briefed single boolean; the conflation is a chain defect (the brief predates R6-1's trim-first design making trim the common case). Also inherent and accepted per brief: the client only learns at create/join/snapshot, so a room that degrades mid-session is told at the next snapshot fetch (reconnect), not immediately.

### R6-7 crash-return keeps memory projects (`41d475a`) — ACCEPT
- Exactly the briefing's preferred design: `holdsMemoryOnlyProjects()` (= `listedHealth === "memory"` and projects listed) → `navigate("#/")` **without** `reload()`, with the soft state reset deferred via `setTimeout(…, 0)` and the reason in-code (hashchange dispatches asynchronously; a same-task reset would re-render the crashed subtree). All other cells of the matrix still reload and are each pinned: healthy-store listing, plain crash with nothing listed, degraded-but-empty listing. The lazy `ownStore ??= createIndexedDbProjectStore()` fallback and its import are deleted; the missing-prop path gets an honest new `store-unavailable` outcome (message + structured log) instead of a silent second store, pinned. The strongest test is the heap-survival one: after the no-reload return, an export through the same store still downloads a real package — the shared instance demonstrably outlived the navigation.

### R6-8 CI workflow (`cfdc8c5`) — ACCEPT
- `.github/workflows/ci.yml` matches the brief: one job, serial `npm ci` → typecheck → lint → full vitest (with the run-heavy no-parallel rationale as a comment), Node 22 pinned with npm cache, `pull_request` + push to `main`/`agent/opt-continuous`, 30-min timeout, `contents: read`, per-ref concurrency with cancel-in-progress. The metric is met and verified first-hand: green runs exist on the branch including on exactly `5b7fe2a`, and PR #14's `pull_request` runs are exercising it. The one red run (32777897030, after the R6-5 merge) was **not a product failure**: all 179 test files passed and Vitest caught one unhandled `ReferenceError: window is not defined` in teardown — the CI paid for itself within hours by exposing a real flake class the local serialized runs never surfaced.

### hotfix `b7ed418` (StudioAssistantRail unmount) — accepted, orchestrator-owned
- Test-only, correct, and carries the full failure→cause→fix→recheck chain in the commit message (CI failure on `ecac0ed`, armed persist timeout racing jsdom teardown, roots now unmounted in `afterEach`, subsequent runs green). Follow-up found by this review: at least two more test files carry the same latent leak — `UniversityEmblem.test.tsx` renders into `document.body` and never unmounts anything; `src/lib/local-workspace-entry.test.tsx` declares a `roots` array but its `afterEach` only clears localStorage, and it mounts `ProjectWorkbench`, which runs async store effects. Filed as R7-7. (`DataOverview.test.tsx` was checked and is clean — its helper-based counts just look asymmetric.)

### R6-9 server durability journey (`9d19bc9`) — ACCEPT
- Exclusive-file discipline exact (one new node-environment test; helpers duplicated from `index.test.ts` because that file is cross-owned — acceptable, noted). All nine briefed legs run against a real `dataDir`: cold boot silence (no "restored N"); small room + an API-accepted 8-MiB-body room whose record cannot fit (201, live-readable 200); SIGTERM with an open SSE stream — and the frozen-wire invariant re-pinned where it matters most (`event: closed` absent from the drain frames); on-disk envelope asserted (small room persisted, oversized in `skippedRoomIds`) plus flush warn with ids; restart restores 1 with the prior-skip boot warn naming the id, oversized 404; corruption → `.bad` quarantine with content intact and cold-boot silence; repeated corrupt boots past the cap keep exactly the 5 newest sidecars (mtime staggered via `utimes`, exact content-set assert); final boot persists cleanly with no skip metadata. No product defects found, consistent with the ledger.
- Notes, no verdict impact: `MAX_ROOM_SNAPSHOT_SIDECARS = 5` is duplicated because `index.ts` doesn't export it — drift would fail the test loudly, so this is acceptable; the trim path is not exercised over real HTTP + files (the oversized room here has empty history, so it proves the *skip* band) — that is the natural R7 extension of this journey (R7-8).

## Cross-task checks

### 1. The persist band after R6-1 — narrowed, proven, and visible; residue is by-design and on the record
- What died: the R5 band's two worst constituents. Rooms oversized *because of history* now persist trimmed (both gates); and the "exactly 8 MiB record evicted by the envelope's ~24 B" absurdity is gone — the per-room gate passes ≤ 8 MiB exactly and the aggregate budget is 12 MiB, so a lone 8 MiB record plus envelope fits with room to spare. Two 5 MiB rooms both survive (pinned; the R5 "whole-server durable capacity = one room" defect is fixed).
- What remains, deliberately: a room whose record **with empty history** still exceeds 8 MiB — i.e. the document plus access records plus metadata lands within envelope-scale of the 8 MiB HTTP accept limit (`min(maxJsonBodyBytes, DEFAULT_MAX_ROOM_TRANSACTION_BYTES)`, index.ts:778) — is still skipped: the live document cannot be trimmed. R6-9 proves this lifecycle end to end on real files, and R6-1/R6-2/R6-6 make it observable at flush (ids), at boot (ids), on `/api/health`, and in the room member's menu. The aggregate ceiling is now 12 MiB total — two 8 MiB rooms still cannot both persist (trim first, then largest evicted, pinned order). This is bounded, honest residue; the correct next moves are honesty refinements (trim ≠ skip in the client copy) rather than bigger constants.
- Occupancy leftover, confirmed as the user asked: the 8 MiB cell's ~50 ms-target miss stays on the books (56.6 ms on the R5 machine; 33.19 ms measured here, header documents the miss and rejects off-thread serialization as out of scope — correct). New leftover from the budget raise: the worst-case *aggregate* flush is now 12 MiB, ≈86 ms by the R5 calibration table on that machine, and **no bench cell measures a multi-room aggregate at budget** — the old single-room 12 MiB cell silently became a skip-path measurement when the per-room cap landed at 8 MiB. Documentation task, not a code rollback (R7-6).

### 2. Visibility chain R6-1 → R6-2 → R6-6 → R6-5b — reaches the DOM, every joint pinned
- Store: `lastPersistOutcome()` records skipped + trimmed ids per successful flush (pinned in three collaboration tests). Server: `persistedAtLastFlush(roomId)` consumes it case-insensitively on create/join/snapshot and `/api/health` republishes the raw outcome (pinned over real HTTP with an injected outcome). Client: strict-boolean parse at both response shapes; missing stays missing. Hook: display-only state, kept on `undefined`, reset per room. App: one prop (R6-5b). Menu: the note renders with precedence terminal > offline > persistence. The chain is proven end to end by R6-5b's App test (real create response → rendered note) — the R5 finding "the people who lose data are the only ones not told" is closed.
- The chain's one defect: the boolean erases the trim/skip distinction and the menu copy asserts restart-death for both. For trimmed rooms — now the *common* degraded case by construction — the true consequence is "重启后房间还在，但离线期间的增量历史会丢，落后的客户端要重新拉全量快照". A false death warning trains users to distrust the banner exactly where R6-6's non-dismissibility makes it loud. R7-1/2/3 split the tri-state through the same chain.
- Chain staleness under failure: because `lastPersistOutcome` updates only on success (R6-1 nit 1), a disk-failure streak leaves every downstream surface reporting the last good flush. Same R7 batch.

### 3. R6-3's filed R7 items — both CONFIRMED from code
- **createProject no refresh**: ProjectWorkbench.tsx:155–163 (`put` → `openProject`, no `refresh()`), while rename/delete both await `refresh()`. Consequence today: the degraded banner's per-gesture export list under-reports a just-created memory project for as long as the workbench stays mounted (navigation usually masks it; R6-7's no-reload return path remounts `ProjectWorkbench`, whose mount effect refreshes — so the exposure is the mounted-workbench window, real but narrow). One-line fix + pin: R7-4.
- **No scheduleRecover seam**: `createIndexedDbProjectStore` already accepts `scheduleRecover` (project-store.ts:595, default at :622) — no frozen-internals change needed — but `editor-project-store.ts:23` doesn't pass or re-expose it, so R6-3 had to hijack global `setInterval` and match the 20 s constant to reach the real recover→`onRecoverError` path. Seam in the singleton module only: R7-5.

### 4. R6-5 × R6-7 × R6-4 — the degraded-session UX now composes
- A degraded user can: see the banner (both routes, quota-aware), export from either route (per-gesture), crash and export from the boundary (read-only), return to the list without losing the heap (R6-7), and open a bad link without being lied to (R6-5). Verified end to end by R6-3's journey on the shared singleton. Remaining composition gap is only the createProject staleness above.

## Gates

- Typecheck: run by this review, both projects, exit 0. Full suite + lint: orchestrator numbers (181 files / 1656 / 2 skipped; lint 0 errors / 7 warnings) — independently corroborated by the green CI run on `5b7fe2a` that this review checked directly. Lint 6→7 is a real regression this round (R6-4's react-refresh warning; R7-4).
- **Merge blocker: none.** Recommended PR #14 description updates (R7-10 / orchestrator): the persist contract as now constituted (records > 8 MiB with empty history are restart-mortal, warned with ids; history trimmed above that line with `VERSION_CONFLICT` re-snapshot on restore; aggregate 12 MiB; rollback = revert `5bc3b29`), the additive HTTP surface (`/api/health.rooms`, `persistedAtLastFlush`; rollback = revert `be82abb`, `9f93022`, `50d5755`), the crash-return behavior change (no reload with memory projects; rollback = revert `41d475a`), and CI (rollback = delete the workflow file). Known misses to state plainly: trimmed-room copy overstates loss until R7; `lastFlush` stale-good under persist failure until R7; durable capacity ceiling 12 MiB.
