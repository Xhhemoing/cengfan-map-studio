# Round 3 SOTA Review
[Model: claude-fable-5-thinking-xhigh]

Reviewed tree: `/tmp/opt-merge`, branch `agent/opt-continuous`, HEAD `7d958e9`. Round-3 diff scope (against R2 close `69d54ba`): 37 files, +2952/−182. Every task diff was read commit-by-commit; ownership was verified per commit (`--name-only`). No product code was modified by this review. `npm test` / `npm run lint` were NOT run (orchestrator owns the serialized full suite). This review DID run two targeted typechecks — `tsc -p tsconfig.node.json --noEmit` and `tsc -p tsconfig.app.json --noEmit` — because the campaign's gate (vitest + eslint) is provably blind to type errors: vitest strips types via esbuild, and `eslint.config.mjs` sets `projectService: false` (no type-aware linting). That blind spot matters this round.

## Headline

**One merge blocker: the merged tree does not compile.** `tsc -p tsconfig.node.json` fails with 3 errors, all one root cause — the R3-4 × R3-5 type seam (`server/index.ts:45` TS2430, `server/index.ts:469` TS2345, `server/index.test.ts:262` TS2345). `npm run build` runs `tsc -b` (scripts/build.mjs), so **production builds are broken on `7d958e9`**, and neither the full test suite nor lint will detect it. The app project (`tsconfig.app.json`) typechecks clean — the other eight src-side tasks are unaffected. The fix is a ~5-line type-seam hotfix, not a revert (runtime behavior is correct and well-tested); it is Round-4 task R4-1 and must land before the full-suite gate and before PR #14 moves.

Verdicts: 6 ACCEPT, 3 ACCEPT-WITH-NITS, 1 REJECT-AS-LANDED (R3-5, compile break only — design and tests are good). The recurring theme, louder than in Round 2: **producer tasks keep landing capabilities that their consumer task cannot wire in the same round** — `health`/`onHealthChange` have zero consumers, `roomExpired`/`connectionHealCount`/`setCollaborationOffline` are threaded nowhere, `canContinue`/`retriable` never reach the assistant UI, and ProjectMenu's new terminal/offline panels are prop-gated dead code. Round 4 is mostly consumer-side wiring plus the gate fix.

## Per-task verdicts

### R3-1 import size gate + ProjectGrid metadata types — ACCEPT-WITH-NITS
- Both read sites now gate on `File.size` before materializing text: `ProjectWorkbench.tsx:182` (`assertProjectPackageSize(file.size)` before `file.text()`) and `usePosterExport.ts:126` (before `readAsText`, with the error routed to `reportStatus`). Gating on `file.size` (UTF-8 bytes) is strictly conservative versus the parser's `raw.length` (UTF-16 units ≤ bytes), so no false accepts.
- Red repros are exactly as demanded: `ProjectWorkbench.test.tsx` spies `file.text` and asserts it is never called for a 256MB `File`, with the R2-7 typed "工程包过大…128.0 MB" message rendered; `usePosterExport.test.tsx` spies `FileReader.prototype.readAsText` likewise, plus a within-cap import success test.
- The `sorted as unknown as StoredProject[]` double cast is dead: `ProjectGrid` now takes `ProjectGridItem = Pick<ProjectListItem, "id" | "name" | "updatedAt" | "pack">` (ProjectGrid.tsx:9) and `ProjectCard` widened to `Pick<ProjectListItem, "name" | "pack">` — `StoredProject` still satisfies both, so no caller regressed. New test pins that cards render counts from `list()` metadata with `store.get` never called.
- Nits: (1) commit `7b61877` touched `workbench/ProjectCard.tsx`, outside the declared writable set — necessary for the prop chain, no ownership conflict (nobody else owned it), but it should have been declared; (2) the optional R3-8 `health` notice was not rendered (allowed — "must not block on it") — see cross-task 3.

### R3-2 partition-heal auto-resend — ACCEPT-WITH-NITS
- Heal detection (`App.tsx:600-617`): a dedicated effect, declared before the send effect so it runs first in the same commit, watermarks `collaborationOffline` and `roomVersion` per room (room switch resets the watermark — a new room's version is never a "heal") and sets a one-shot `collaborationHealPendingRef`.
- The send effect (App.tsx:619-628) consumes the flag on every run — the resend is one-shot per heal, never a loop — and uses it to bypass the self-echo suppression exactly once, with the correct argument written in-code: by heal time the baseline has already folded the remote event in, so the re-diff yields only the stranded local ops.
- **Budget stacking is prevented at the right layer**: `advanceRoomVersion` (App.tsx:~645) pre-raises the heal watermark before `setRoomVersion` for every send-side version advance (ack commit, conflict backfill), so R2-3's conflict retry cannot masquerade as a heal and pull an extra resend. Verified against both call sites (`commitAcknowledgement`, `rebaseOnLatestVersion`).
- Transport failures on submit now say "网络异常，本地修改已保留，恢复后会自动续传" via `isCollaborationTransportError` (App.tsx:731-737) instead of a raw error.
- Tests are the briefing's repro verbatim: stranded diff resent on stream-event heal with NO further edit (`uploads[1].baseVersion === 2`, students-only diff), resend on offline-flag clear without version advance, and never-heals → exactly one upload.
- Nits: (1) the `TODO(R3-3)` at App.tsx:733 was never resolved even though R3-3 landed the `setCollaborationOffline` setter later the same round — submit-path failures still cannot set the offline flag (see cross-task 1); (2) every remote event advances `roomVersion` → arms the heal flag → the next send-effect run re-diffs even when nothing is pending; the diff is cheap and yields no upload when empty, but it is one extra `diffCollaborationDocument` per remote event in hot collaboration — acceptable, worth remembering if profiling ever flags it.

### R3-3 terminal expiry / offline taxonomy — ACCEPT-WITH-NITS
- `collaboration-client.ts`: `subscribeRoom` gains only the `onTerminal` callback (fired before `onError` so the caller lands terminal state before the error-driven backfill can repaint "正在自动重连"); `SubscribeTerminalReason` is exactly the members of `TERMINAL_TICKET_ERROR_CODES` (collaboration-client.ts:493), so the `error.code as SubscribeTerminalReason` cast is sound. Backoff sequence and wire format untouched, as required.
- `useCollaborationRoom.ts`: terminal `roomExpired` state reached from all four entry points — ticket `onTerminal` (:440), backfill ROOM_NOT_FOUND/FORBIDDEN (:341), the VERSION_CONFLICT-retry error path (:372), and join failure (:~589, forgetting only the target room's credentials). `isTerminal()` short-circuits backfill, `markOffline`, and `markReconnecting`, so nothing repaints over the terminal message from the hook's own paths; `markRoomExpired` drops the stored room credential (a token that opens nothing must not keep the join button lying).
- `window online` listener (:467) bumps `reconnectNonce` → remounts the subscription (immediate ticket, backoff untouched) and defers the `online` backfill until after the new subscription is installed (`healPendingRef`, :447) so the old effect's cleanup abort cannot kill it. Guarded: healthy connections and terminal rooms are never nudged.
- `connectionHealCount` increments only on true offline→online transitions (`markOnline` early-returns when not offline, :279); `leaveRoom` clears offline without incrementing (a voluntary disconnect is not a heal — the doc-comment says why). `setCollaborationOffline` delegates to markOffline/markOnline, and the terminal state is out of its reach (tested).
- `ProjectMenu.tsx`: `roomExpired`/`collaborationOffline` are optional with defaults that preserve existing rendering; terminal beats offline (`isOffline` requires no terminal), closed beats expired; expired collapses the now-useless invite/readonly controls. 6 panel tests + 8 new hook tests including "never resurrects a terminal room from an online event".
- Nits: (1) **all of the new panel UI is dead code in production** — App threads none of the new props (cross-task 1); users do still see the taxonomy because the hook writes it into `collaborationMessage`, which App already renders, so the messaging goal is half-delivered; (2) `markRoomExpired` on join failure sets the hook-global terminal flag; if a join could race an active healthy room, that room's subscription would be abandoned by `shouldReconnect` — currently unreachable (ProjectMenu renders the join form only when `roomId` is null) but worth a guard when App wiring lands.

### R3-4 room store persist/restore — ACCEPT
- `snapshot()` (server/collaboration.ts:642) captures rooms, **hashed** access records (`publicParticipant` also strips any extra fields), invitations (hash+role+expiry), transaction ids, operation history, `lastActivity`, legacy flags, under a versioned envelope (`version: 1`). Raw tokens never touch the snapshot; the test asserts serialized output contains no raw secret.
- Restore validates per record: 64-hex token hashes, role whitelists, `areValidCollaborationOperations` on history, TTL threshold (`lastActivity <= now - roomTtlMs` → room stays dead — tested), `maxRooms` cap, duplicate-id skip. Every skipped fragment calls `markDirty()` so the next flush rewrites the cleaned state. Unknown snapshot versions are ignored wholesale.
- Dirty tracking is precise: all nine mutation paths mark dirty; `purgeExpired` early-returns before marking when nothing expired, so an idle server never rewrites its snapshot; `touch` no-ops on same-timestamp.
- `flush()` (:664): purge first (expired rooms are never persisted), snapshots synchronously, serializes persists through `persistQueue` (order preserved, prior failure isolated by `.catch`), advances `persistedVersion` monotonically only after the sink resolves; follow-up `062cc14` makes `flush()` await the queued op so callers see sink failures. The interval timer is `unref()`d and skips when clean.
- Injected-clock tests cover the briefing's full round-trip: transact → snapshot → new store → same tokens authorize, version/history/invitations intact, expired rooms stay dead, interval persistence.
- Note for Round 4 (not a defect): `CollaborationRoom.snapshot` holds the full project package (collaboration.ts:36-50), so a snapshot of a few large rooms is a multi-MB `JSON.stringify` on the event loop every dirty interval — unmeasured; R4-4 measures it first.

### R3-5 graceful shutdown drain + boot restore — REJECT-AS-LANDED (compile break; fix forward, do not revert)
- **The blocker.** `RoomStoreFactoryOptions extends RoomStoreOptions` redeclares `restore?: unknown` (server/index.ts:45-50) against R3-4's `restore?: RoomStoreSnapshot` (collaboration.ts:135) → TS2430; the factory call at index.ts:469 and the test at index.test.ts:262 fail assignability with the same root cause. The `unknown` opacity was authored as a pre-R3-4 compatibility shim ("它还没落地时这两个字段会被现有实现忽略") — but commit `ada36f4`'s parent already contained R3-4's merge (`642bcff`), so the commit broke `tsc` the moment it landed, and no gate caught it: vitest compiles per-file via esbuild, eslint has `projectService: false`, and nobody ran `tsc -b`. Reproduced in this review: 3 errors, exit ≠ 0; `tsconfig.app.json` clean.
- Everything else is right, which is why the remedy is a type-seam hotfix (R4-1), not a rollback:
  - `createServerLifecycle` (production.ts:93): shutdown completes only when BOTH `server.close` and the drain→flush chain finish (`pending = 2`); the deadline path runs `onTimeout` (→ `closeAllConnections`) and force-finishes; flush is ordered AFTER drain so writes produced during drain reach the snapshot — the reasoning is documented in-code.
  - `attachServerLifecycle` (index.ts:1348): drain ends every registered SSE stream (each stream registers its own `endStream` and deregisters in `teardownStream`) **without emitting `closed`** — deliberately, since `closed` is the room-is-gone terminal signal and these rooms now survive restart; then releases idle keep-alive sockets so `close` can complete. AI-state and room flushes run under `Promise.allSettled` so one failure cannot starve the other.
  - Boot restore: `createReadyAiServer` reads `collaboration-rooms.json` (missing/corrupt → start with no rooms, warn — a bad file must not block boot, index.ts:1379), and persists via temp-file + `rename` at mode 0600 (index.ts:1395). The direct-run entry now uses `attachServerLifecycle`.
  - Tests: SIGTERM with live SSE → streams end + flush ran + port freed; in-flight request bounded by the deadline; snapshot round-trip across two boots; drain-waits-for-both; deadline hook fires.
- Fix sketch for R4-1: now that R3-4 is landed, type the seam honestly — `RoomStoreFactoryOptions` drops its `restore`/`persist` redeclarations (inherit `RoomStoreSnapshot` types); keep `unknown` only at the outermost file-read boundary (`loadRoomSnapshot`) with one commented cast at the seam, which is safe because `createRoomStore` fully validates the restore payload at runtime anyway. Update the index.test.ts:262 shape accordingly.

### R3-6 refused landing stops polluting history — ACCEPT
- The prescribed root fix, exactly: `applyTransaction` keeps the `cloned` reference and returns `project` untouched when `applied === cloned` (project-document.ts:256); the rejection contract is documented at `cloneProjectForTransaction` (identity, not equality — an unchanged spread copy still commits, and a test pins that boundary from both sides).
- The producer actually satisfies the contract: `transactionForSteps.apply` returns `current` by identity on replay failure (agent-session.ts:759). Bonus: the zero-steps fallback transaction (`apply: (current) => current`, :774) now also commits nothing.
- R2-0's integration expectations flipped in the same commit: `expect(landed).toBe(live)`, version and history unchanged. New project-document tests include the subtle one — a refusal must not clear the redo branch (`history.future` preserved, redo still works).
- The user-facing message and the semantic change landed in the SAME commit (`50f858a`), per the briefing's never-split requirement: `AgentAssistant.tsx:landingFailureMessage` now says "画布内容与历史记录都没有变化" and the "撤销栈里会多出一条空操作" apology is gone, with landing tests updated.
- Observable-semantics change (no more version bump on refusal) — must be recorded in the PR description with rollback note (revert `50f858a` restores the old contract; no data format touched).

### R3-7 AI client deadlines — ACCEPT
- `requestRound` (agent-session.ts:~503-545): per-request `AbortController` + a **self-settling deadline** that wins the `Promise.race` even if the transport ignores abort — and the deadline covers the body read (`response.json()` failure re-checks `timedOut`), closing the headers-arrive-body-stalls partition shape. The orphaned attempt gets a `catch(() => {})` so a late rejection can't become an unhandled rejection.
- Default 90s, injectable via `requestTimeoutMs` (non-finite/non-positive falls back — tested). Typed outcome: `{ kind: "failed", reason: "timeout" | "network", retriable: true }` with distinct Chinese messages; user cancel stays `cancelled` in both race orders (cancel-during-fetch rethrows and the outer catch at :718-719 maps AbortError → cancelled; cancel-racing-deadline is re-checked in `run` before `failTransport`).
- `canContinue` is now `(completed || retriableFailure) && !activeRun`; `retriableFailure` resets on the next `run()`. Budget, `taskId`, `budgetReceipt`, metrics untouched by a transport failure (tested: resume after deadline keeps granted budget and task identity).
- 11 tests in the new `agent-session.deadline.test.ts`, including the two adversarial ones that matter: abort-ignoring transport still fails at deadline, and malformed response body is NOT misclassified as a network failure. Did not touch `agent-session.test.ts` (R3-6's file) — ownership split held.
- Nit (consumer gap, not this task's fault): `AgentAssistant.tsx` never reads `canContinue`/`retriable`/`reason` — a failed conversation is "fresh" (AgentAssistant.tsx:447) so the user's retry restarts the conversation instead of resuming it. R4-8.

### R3-8 store degraded mode + quota taxonomy + reconcile — ACCEPT-WITH-NITS
- All three demanded behaviors, cleanly layered:
  - Degraded mode: open retries (default 1 extra, injectable delay) exhaust → typed `open-failed` → `withFallback` lazily instantiates one memory store, `health` getter flips `"persistent"→"memory"`, `onHealthChange` fires once; degrade is sticky (no reopen storm — tested via open-call count). Only `open-failed` degrades; write/quota errors propagate typed.
  - Quota taxonomy: request-level errors captured at `tx.onerror` (where quota is visible) and reported at terminal `onabort` (`abortFailure`), preserving R2-6's multi-store abort correctness; `isQuotaError` covers `QuotaExceededError`, the legacy Firefox name, and Safari's code 22. Message is the user-actionable "本机存储空间不足…".
  - Reconcile (`reconcileMissingMetadata`, project-store.ts:~397): cheap `getAllKeys` set-diff on every open; only orphaned `projects` rows are read and re-projected via `metadataFromStoredValue`; unparseable rows are left in place (user data is never deleted), failures are swallowed (a broken reconcile must not block open). Closes R2-6's stale-tab hole; both tests present (orphan reappears, unparseable stays out).
- 10 new tests including first-call degradation and health-flag reporting for all three store flavors.
- Nits: (1) `health`/`onHealthChange` have **zero production consumers** — `main.tsx:12` and `editor-project-store.ts:3` call `createIndexedDbProjectStore()` with no options (cross-task 3); (2) sticky degrade means one transient open failure (~2 attempts, 120ms apart) silently forfeits persistence for the whole session and hides previously-persisted projects from `list()` — the designed trade-off is defensible but recovery deserves a Round-4 look (R4-10).

### R3-9 workbook worker reuse + parse deadline — ACCEPT-WITH-NITS
- Reuse: `workbookWorkerRef` holds one worker across imports; `acquireWorkbookWorker` cancels idle teardown and lazily creates; 30s idle timer tears down module + heap. All failure paths (`onerror`, `onmessageerror`, `postMessage` throw, deadline) terminate the shared worker so the next import provably spawns fresh (tested).
- Deadline: 30s → terminate + reject with the exact briefing message "解析超时，文件可能已损坏"; the settle path clears the timer and the active-import slot with requestId matching so a stale deadline can't kill a successor.
- Correctness preserved by construction: `beginImport` (DataWorkspace.tsx:175-179) still bumps the generation and cancels the active import before every new one, so two imports can never share `onmessage` handlers; the 25MB pre-read cap, transfer semantics, `WORKBOOK_IMPORT_CANCELLED` sentinel, and requestId guard are all unchanged; the worker file itself needed no change (its `parseWorkbookImport` handler was already multi-message-safe).
- Both demanded tests exist: reuse-then-idle-teardown, and wedged-worker → timeout message → next import works on a fresh worker.
- Nit: the merge commit records "repeat-import median 62.3% faster" but **no bench artifact exists in the repo** — unlike every R2 perf task (`scripts/perf-*-bench.ts` landed before product code), the number is unreproducible. The claim is mechanistically plausible (skips re-parsing the ~1MB xlsx module) and the task was primarily defect-driven, so this is a nit, not a blocker — but it is a discipline slip the campaign should not repeat (R4-9 lands the bench).

### R3-10 error-boundary backup export — ACCEPT
- "导出工程备份" on the crash screen: reads the mirror via the existing `createSafeLocalStorageMirror`/`loadBrowserWorkspaceMirror` (frozen R1 file consumed, not edited), downloads via the existing `downloadProjectPackage`, filename stamped from the pack's export date. Existing recovery actions and markup untouched (pinned by a test); `main.tsx` untouched (allowed — injection points are optional props).
- Failure taxonomy is complete and honest: `mirror-empty` (no fake empty file), `mirror-corrupt` (keeps the page, asks for the console diagnostics, includes byte count + 120-char head for bug reports), `mirror-unreadable` (storage threw), `download-failed`. Every outcome logs a structured record including the captured crash detail (`componentDidCatch` stores message + componentStack).
- The demanded round-trip repro is verbatim: boundary triggered → click → blob text through `parseProjectPackage` → deep-equals the seeded pack. 6 tests total.
- Residual (scoped out correctly): the export covers only the editor's localStorage mirror; a user whose crash is in the workbench (IndexedDB projects, no mirror) still has no extraction path — R4-7.

## Cross-task couplings (the three required checks)

1. **R3-2 × R3-3 — App threads NONE of it; budgets verified non-stacking.** Direct answer to the review question: App consumes `collaborationOffline` and `roomVersion` (App.tsx:617/775 heal deps) but does NOT thread `roomExpired`, `connectionHealCount`, or `setCollaborationOffline` anywhere — the ProjectMenu render site (App.tsx:1575-1605) passes neither new prop, and the send-effect TODO (App.tsx:733) is still open even though the setter it waits for landed the same round. Consequences: (a) ProjectMenu's terminal/offline panels are unreachable (users get only the hook's `collaborationMessage` text — truthful but unstructured, and the invite/readonly controls stay visible on an expired room); (b) an asymmetric partition where only the submit POST fails never sets the offline flag, so the stranded diff still waits for a remote event or user edit — the exact hole R3-2 was chartered to close is closed for stream-visible partitions only; (c) **doomed resend**: the send effect gates on `roomClosed` but not `roomExpired` (App.tsx:624), and `markRoomExpired` clears the offline flag — which App's watermark reads as a heal — so an expiry during a partition triggers one pointless POST whose ROOM_NOT_FOUND error then overwrites the hook's terminal message via the raw `setCollaborationMessage` (the hook's `isTerminal` guard protects its own paths, not App's raw setter writes). Bounded (one shot per heal, one per edit), no loop, no data corruption — but wrong messaging. Retry budgets DO hold: heal resend is one-shot per heal (flag consumed every send-effect run), `advanceRoomVersion` pre-raises the watermark so R2-3's conflict retry (still 1) never reads as a heal, healCount increments only on true transitions, and client-level POST retries remain zero (R2-8 unchanged). All three wiring gaps → R4-2.
2. **R3-4 × R3-5 — functionally wired end-to-end, broken at the type seam.** The full chain exists and is tested: boot `loadRoomSnapshot(collaboration-rooms.json)` → `roomStoreOptions.restore` → `createRoomStore` validation/TTL; mutations → dirty-gated 30s interval + shutdown `flushRooms` → `flush()` → atomic 0600 temp+rename write; drain ends SSE without `closed` so restored rooms greet reconnecting clients; index.test.ts proves a room created before SIGTERM answers `fetchRoom` after reboot. But the seam types (`restore?: unknown` vs `RoomStoreSnapshot`) fail `tsc` — 3 errors, build broken, invisible to the vitest+eslint gate. R4-1 hotfixes the types AND R4-9 fixes the gate so this class of break cannot land silently again.
3. **R3-1 × R3-8 — health notice NOT wired, confirmed.** `ProjectStoreHealth`/`onHealthChange` exist with tests, but both store construction sites (`main.tsx:12`, `editor-project-store.ts:3`) pass no options and no component reads `health` — grep over `src/` non-test files finds zero consumers. A Firefox-private-mode user gets working-but-ephemeral storage with no warning, which is arguably worse than the old hard failure because data loss is now silent. Was explicitly optional for R3-1 this round; becomes mandatory as R4-3.

Additional cross-task findings: R3-7's retriable state has no UI consumer (AgentAssistant restarts instead of resuming — R4-8); ownership audit otherwise clean — the only writable-set stretch is R3-1's `ProjectCard.tsx` (declared post-hoc by the orchestrator, conflict-free); R3-6/R3-7's file-level split of `agent-session.*` held exactly as designed (verified per commit).

## PR #14 recommendation

**Keep draft. Two hard gates before ready-for-review, one of them new:**
1. **R4-1 type-seam hotfix merged, then `tsc -b` (or `npm run build`) green on the merged tree.** Non-negotiable: `7d958e9` cannot build for production, and the existing full-suite + lint gate will report green anyway. Until the gate itself runs a typecheck (R4-9), the orchestrator must run `tsc -b` manually at every merge point.
2. Orchestrator's serialized full `npm test` + `npm run lint` on the post-hotfix tree (PROGRESS.md shows MERGED_PENDING_FULL_SUITE; only the coupled R3-2/R3-3 slice, 174 tests, has run since these merges).
Also fold into the PR description: R3-6's observable-semantics change (refused landings no longer bump version/history) with revert-commit rollback note; R3-4/R3-5's new `collaboration-rooms.json` artifact (additive; rollback = delete file + revert, hashed-only credentials, 0600); R3-9's 62.3% claim flagged as unreproduced-in-repo until R4-9's bench lands.

## Residual gaps → Round 4

Ten tasks sketched in `round-3-briefing.md`. Directional call: Round 3 closed the big durability holes (rooms survive restarts, refusals stop polluting history, storage degrades instead of dying, crashes can export). What remains is (a) the compile/gate blocker, (b) **consumer-side wiring** for every producer capability this round stranded (App taxonomy threading, workbench health notice, assistant resume), (c) the last protocol unknown — ack-lost submit convergence has no test, (d) disaster-recovery breadth (workbench-crash export, degraded-store recovery), and (e) measurement debts (room-snapshot serialization cost, workbook bench). **Lock-free/zero-copy remains out of scope: no measurement anywhere in R3 shows contention** — the only new unmeasured hot path is room-snapshot `JSON.stringify`, and R4-4's charter is to measure it before touching anything.
