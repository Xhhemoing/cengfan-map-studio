[Model: claude-fable-5]
# Round 7 Briefing (from the Round-6 SOTA review)

## Direction

Round 6 eliminated history-caused restart mortality, put the skip/trim facts on HTTP, and walked both disaster journeys on the merged tree. Round 7 is **finishing the honesty of what Round 6 built** — no new subsystems, no lock-free / zero-copy work; we are at round 7 of 20 and every task below is a concrete, evidenced defect from the Round-6 review. The headline is a chain fix in three sequenced steps (R7-1→2→3): `persistedAtLastFlush: false` conflates *skipped* (dies at restart) with *trimmed* (survives; stale clients re-snapshot), and the member-facing copy asserts restart-death for both — false for the common case, since R6-1 made trim the default degradation. Second theme: `lastPersistOutcome` only updates on success, so every downstream surface reports stale-good exactly when the disk is failing. Third: close the two confirmed R6-3 items, the bench's dead cell, and the CI flake class the new workflow exposed.

Quality bar unchanged: **no metric, no merge; red repro first; targeted vitest only** (`npx vitest run <file>`); `npm run typecheck` must stay exit 0 after every task; CI must be green on the merged tree. One task = one exclusive writable file set; the sets below are disjoint — do not touch another task's files. Lesson from R6-5 recorded: if a fix necessarily breaks a pinned expectation in a cross-owned test file, the collision is pre-authorized **only** where this table says so; otherwise stop and file it.

## Tasks (exactly 10, disjoint writable sets)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R7-1 | `server/collaboration.ts`, `server/collaboration.test.ts` | `latestPersistOutcome` is written only in the persist-success path (`startPersist`'s `operation.then`), so during a disk-failure streak `lastPersistOutcome()` keeps answering with the last good flush — R6-2's `/api/health.rooms.lastFlush` and `persistedAtLastFlush` are stale-optimistic precisely when persistence is broken; the only live signal is one deduped `console.error`. | Red: persist rejects twice after one success → `lastPersistOutcome()` still reports the old success with no failure indication. Fix: additive failure record on the outcome, e.g. `lastFailure?: { at: number; message: string }` kept alongside the last success (or a discriminated shape — implementer's choice, but the last *successful* skipped/trimmed ids must remain readable), set on every persist rejection (interval and explicit flush), cleared on the next success. Keep the console dedupe behavior and all R6-1 trim semantics untouched. Metric: red test flips; streak → visible failure with timestamp; success clears it; existing 12 collaboration persist tests green unmodified. |
| R7-2 | `server/index.ts`, `server/index.test.ts` | One boolean erases the trim/skip distinction R6-1 created: R6-2's tests pin trimmed → `persistedAtLastFlush: false`, identical to skipped, though the consequences differ (skip = gone at restart; trim = survives, stale clients must re-snapshot). `/api/health.rooms.lastFlush` also cannot show a persist-failure streak (needs R7-1). | Red: create/join/snapshot responses today carry no way to tell a trimmed room from a skipped one. Fix (additive, keep the boolean for compat): a sibling field `persistence: { outcome: "persisted" \| "trimmed" \| "skipped", at: number \| null }` computed from `lastPersistOutcome()` on the same three responses, and `/api/health.rooms.lastFlush` gains R7-1's failure record. Same ≤-one-interval lag note, same case-insensitive matching, same tolerance for stores without the accessor. No SSE changes. Metric: response-shape tests for all three outcomes on all three endpoints; health failure-streak test; existing R6-2 tests green unmodified. Sequenced after R7-1. |
| R7-3 | `src/lib/collaboration-client.ts`, `src/lib/collaboration-client.test.ts`, `src/lib/useCollaborationRoom.ts`, `src/lib/useCollaborationRoom.test.tsx`, `src/components/ProjectMenu.tsx`, `src/components/ProjectMenu.collab.test.tsx` | The menu copy 该房间体量超过服务器持久化上限，服务器重启后将无法恢复 is provably false for trimmed rooms (R6-1's near-limit test restores one with snapshot, access, and version intact), and trim is now the common degraded case — the loudest, non-dismissible warning a member sees overstates the loss and erodes trust in the banner. | Red: hook + menu today render the death copy for a `"trimmed"` outcome. Fix: client parses R7-2's `persistence.outcome` with the same strict discipline as the boolean (unknown strings → no verdict; boolean-only servers keep the current behavior); hook exposes the kind as display-only state (same reset/keep-on-undefined rules; do NOT touch the heal/terminal core); menu splits the copy — skipped keeps the current death copy; trimmed says the truth, e.g. 该房间体量超过服务器持久化上限，重启后房间会恢复，但最近的增量历史会丢失，长时间离线的成员需要重新加载完整快照，建议导出备份. Precedence terminal > offline > persistence unchanged. Metric: red test flips; per-kind DOM pins; downgrade-to-boolean compat pinned; existing R6-6 precedence tests green unmodified. Sequenced after R7-2. |
| R7-4 | `src/components/ProjectWorkbench.tsx`, `src/components/ProjectWorkbench.test.tsx`, `src/components/StorageNotice.tsx`, `src/components/StorageNotice.test.tsx`, `src/components/StudioRoutes.tsx`, `src/components/StudioRoutes.test.tsx`, NEW `src/lib/project-package-file-name.ts` (+ test) | Confirmed R6-3 item 1: `createProject` does `put` + navigate with no `refresh()` (ProjectWorkbench.tsx:155–163) while rename/delete both refresh — the degraded banner's export list omits a just-created memory project for as long as the workbench stays mounted, and R6-7's no-reload return made "workbench stays alive" a normal state. Plus the round's lint regression: `projectPackageFileName` (non-component export) in `StorageNotice.tsx` trips react-refresh (lint 6→7 warnings). | Red: degraded store, create a project without navigating (inject `navigate`) → banner lists N−1 export buttons while `store.list()` has N. Fix: `await refresh()` in `createProject` before navigating (mirror `renameProject`); move `projectPackageFileName` to the new lib module and update the three import sites (StorageNotice re-export removed, not kept). Metric: red test flips; lint back to 6 warnings; existing R6-4 notice tests green unmodified. |
| R7-5 | `src/lib/editor-project-store.ts`, NEW `src/lib/editor-project-store.test.ts` (or extend if it exists), `src/components/crash-disaster.integration.test.tsx` | Confirmed R6-3 item 2: `createIndexedDbProjectStore` accepts a `scheduleRecover` injection (project-store.ts:595/622 — no frozen-internals change needed) but `editor-project-store.ts:23` neither passes nor exposes it, so the integration test hijacks global `setInterval` and pattern-matches the 20 s period — a teardown-order landmine coupled to an implementation constant. | Deliverable (no red state — this is a seam): the singleton module passes a module-level scheduler that registers probes into an exported test-visible handle (e.g. `export const projectStoreRecoveryProbe: { run(): void }`, no-op-safe in production, documented as test-only); `crash-disaster.integration.test.tsx` drops `captureRecoverProbe` and the `RECOVER_INTERVAL_MS` copy in favor of the handle. Production behavior identical (probe still runs on the default interval — pin that the default scheduler is still installed). Metric: integration journey green without any global-timer stubbing; grep shows no `RECOVER_INTERVAL_MS` literal outside `project-store.ts`. |
| R7-6 | `scripts/perf-room-snapshot-bench.ts` | The bench's "standard 12 MiB" single-room cell silently measures the *skip path* (record > 8 MiB per-room cap with empty history → omitted from output), and no cell measures the actual new worst case created by R6-1's budget raise: a multi-room aggregate near `MAX_PERSISTED_SNAPSHOT_BYTES` = 12 MiB (~86 ms by the R5 calibration table on that machine; this machine ~50 ms extrapolated from 8 MiB = 33.19 ms). A bench that asserts budget compliance while measuring an empty output is occupancy documentation that documents nothing. | Deliverable (bench + docs, no product code): replace or annotate the dead 12 MiB cell (if kept, label it `skip path` and assert rooms retained = 0 so it can't masquerade); add an aggregate-at-budget cell (e.g. 2 rooms × ~5.9 MiB, retained = 2) measuring snapshot + stringify occupancy; add a guard that every *measurement* cell retains the expected room count (the existing trim-cell guard generalized). Record the new table in the commit message and the file header, including the worst-case aggregate number next to the documented 8 MiB miss. Metric: bench runs clean; header table updated; no cell measures an empty snapshot while claiming a size. |
| R7-7 | `src/components/UniversityEmblem.test.tsx`, `src/lib/local-workspace-entry.test.tsx` | The CI flake class `b7ed418` fixed (mounted roots outliving the case → React scheduler races jsdom teardown → unhandled `ReferenceError: window is not defined`, seen failing run 32777897030 with all files passing) has at least two more instances found by review scan: `UniversityEmblem.test.tsx` renders into `document.body` and never unmounts (its `afterEach` only unstubs globals); `local-workspace-entry.test.tsx` declares a `roots` array but its `afterEach` only clears localStorage — and it mounts `ProjectWorkbench`, which runs async store effects, the highest-risk shape. | Deliverable (test-only): apply the `b7ed418` pattern — track every root + container, `flushSync`-unmount and remove in `afterEach` — to both files; while there, remove the dead `roots` declaration confusion in `local-workspace-entry.test.tsx`. Do NOT sweep other files (cross-owned); instead note in the commit message any further createRoot-without-unmount hits from a fresh scan so the R8 briefing can assign them. Metric: both files green in isolation and under `npx vitest run src/components src/lib` locally; no unhandled teardown errors. |
| R7-8 | `server/durability.integration.test.ts` (test-only; no product files) | R6-9 proves the *skip* band end to end but never exercises the trim path over real HTTP + files: no journey covers an API-accepted room with heavy history being trimmed at shutdown, restoring with its snapshot intact, answering a stale `getOperations` with the re-snapshot conflict over HTTP, and reporting its state through R6-2's (and R7-2's) response fields. The seams R6-1 added are exactly where the next regression will hide. | Deliverable (proof task): extend the journey (or add a sibling `it`) — create a room near the cap, apply history-heavy transactions over HTTP until the record only fits trimmed; SIGTERM; assert envelope `trimmedRoomIds` + trim warn; reboot; room serves its snapshot at the right version over HTTP; `GET .../operations?after=0` (or the operations route's actual shape) answers the trimmed-interval conflict; `persistedAtLastFlush` / R7-2's `persistence.outcome` reflect `trimmed` after the next flush; `/api/health.rooms.lastFlush` carries the trimmed id. Any defect found is filed for R8 — do not fix cross-owned files. Metric: journey green on the merged tree. Sequenced after R7-2 lands (fall back to boolean asserts if R7-2 is rolled back). |
| R7-9 | `src/App.tsx`, `src/App.test.tsx`, NEW extracted `src/lib/*` module(s) (+ tests) | `App.tsx` is 2322 lines against the repo's 400-line rule. R6-5 proved the extraction playbook is safe (−329 lines, six modules, additions-only test rule, suite green); the remaining monolith still holds cohesive non-visual clusters (import/export handlers, render-settings/font persistence, workflow-stage orchestration). | Deliverable: extract ≥300 further lines into `src/lib` modules with pure seams and their own tests; **behavior-preserving only** — no copy changes this time, so no cross-owned test file may need edits (if one does, stop and file for R8 instead of editing it; the R6-5 `AppProjectMode.test.tsx` collision is NOT pre-authorized here). `App.test.tsx` additions only. Metric: line-count delta recorded in the commit; all existing App tests pass unmodified; typecheck 0. |
| R7-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律: PR [#14](https://github.com/Xhhemoing/cengfan-map-studio/pull/14) is ready-for-review with CI attached, but its description does not yet carry the Round-6 contract changes and rollback map reviewers need. | Deliverable (docs, no code): description gains (a) Round-6 evidence (181 files / 1656 passed / 2 skipped; lint 0 err / 7 warn; typecheck 0; CI green on `5b7fe2a`; review 6 ACCEPT + 4 ACCEPT-WITH-NITS, no blocker), (b) behavior changes + rollbacks: persist contract (trim-first, 12 MiB aggregate, skip band for history-less records > 8 MiB; revert `5bc3b29`), HTTP surface (`/api/health.rooms`, `persistedAtLastFlush`; revert `be82abb` + `9f93022` + `50d5755`), crash-return no-reload (revert `41d475a`), CI (delete workflow), (c) known misses stated plainly: trimmed-copy overstatement and stale-good `lastFlush` under persist failure until R7-1..3 land; durable capacity ceiling 12 MiB. PROGRESS.md Round-7 section mirrors it. |

## Sequencing

R7-1 → R7-2 → R7-3 (failure-honesty + tri-state chain; each consumes the previous task's landed seam). R7-8 runs **after** R7-2 merges (it proves the merged fields). R7-4, R7-5, R7-6, R7-7, R7-9 are order-free and can start immediately. R7-10 last.

## Saturated — leave alone this round

`src/components/AppErrorBoundary.tsx` (R6-7 completes the crash screen: navigation matrix + no second store; only R8+ evidence reopens it), `src/components/StorageNotice.tsx` beyond R7-4's helper move, `server/index.ts` boot/quarantine core beyond R7-2's additive scope, `src/lib/useCollaborationRoom.ts` heal/terminal/offline core (R7-3 touches display state + parse only), `server/collaboration.ts` snapshot/trim/eviction algorithm beyond R7-1's outcome record (the R6-1 geometry is fresh and fully pinned), `src/components/crash-disaster.integration.test.tsx` beyond R7-5's de-stubbing, the `.github/workflows/ci.yml` shape (green and serial; don't tune it while it's the only reviewer-visible gate).

## Frozen (unchanged)

`src/lib/project-store.ts` internals (R7-5 uses the existing public option only); layout stack; `src/lib/collaboration-operations.ts` (consume only); `server/ai/*`; SSE wire format (drain still must not emit `closed`; R7-2/R7-3 use HTTP response fields only); project-document rejection identity; DataWorkspace product code (bench only); payment/套餐 boundaries per `docs/开源与收费边界.md`.
