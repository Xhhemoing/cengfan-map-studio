[Model: claude-fable-5]
# Round 11 SOTA Review (fable)

Reviewed tree: `origin/agent/opt-continuous` @ `c8f74f1` (the R11-4 lint-rename follow-up; includes R11-1..R11-9 and the full-suite ledger). Review branch: `cursor/r11-fable-review-187a`, isolated worktree. Docs only; no product edits committed.

## Headline

**No merge blocker.** All nine rows land: **7 ACCEPT, 2 ACCEPT-WITH-NITS (R11-2, R11-4), 0 REJECT.** The two carried defects are genuinely closed: the AI state store's load-path failures now ride the original errno on `cause` (pinned on a real EISDIR via directory-squat of the state file itself), and a state file configured outside `dataDir` gets its own crash orphans swept at boot by a deliberately *narrow* matcher — `^<basename>.\d+.tmp$` only, with a shared-directory neighbor pinned to survive untouched. The size campaign hit every number honestly: `server/index.ts` 1146 → **979** (AI routes out behind the R10-4 deps-struct discipline, byte-verified), `server/index.test.ts` 2378 → **126** + nine domain files with a 318/318 byte-identical expect multiset, `src/App.tsx` 1066 → **988** against a ≤ 990 gate with verbatim DOM, and four more test files (983/958/696/655) split to ≤ 293 with all four multisets machine-checked in full. **The Round-10 process finding is fixed: this review replayed the ratchet's invariants against every merge commit's own first-parent tree — all nine are ratchet-green** (Round 10 had five red). The round's one new process miss is a different guard: R11-4's merge landed **lint-red** (10 `react-hooks/rules-of-hooks` errors — the shared fixture was named `useServerFixture` and called at module top level), healed two commits later by the `installServerFixture` rename that the orchestrator correctly folds into R11-4's landing. Reproduced first-hand at both trees; behavior-neutral; Round 12 adds lint-green-per-merge as an explicit rule next to the ratchet rule.

## Gates (first-hand where required)

- `./node_modules/.bin/tsc -p tsconfig.app.json --noEmit` → **exit 0** (run by this review).
- `./node_modules/.bin/tsc -p tsconfig.node.json --noEmit` → **exit 0** (run by this review).
- Full suite / lint: orchestrator numbers accepted per instructions (264 files passed / 2 skipped; 1969 passed / 2 skipped, 98.27s; lint 0 errors / 1 frozen warning). Corroboration: `npx vitest list` on this tree enumerates exactly **1969** tests across **264** files; the 2 skipped are both env-gated benches (`project-store.bench.test.ts` behind `PROJECT_LIST_BENCH`, `collaboration.test.ts`'s authorize bench behind `COLLAB_AUTH_BENCH`) — gating preserved through the splits.
- Scoped eslint, run by this review, over **every** R11-changed file (ai-state-store pair, room-snapshot-store pair, boot-tmp-sweep, index.ts, ai-routes pair, all 11 index-split files + fixture, App.tsx, both new editor components, App.shell-layout, all 21 split/harness/fixture files of R11-6..9) → **0 errors, 0 warnings**.
- Targeted vitest, run by this review: ai-state-store **8/8**; boot-tmp-sweep + room-snapshot-store **17/17**; index.ai + ai-routes **36/36**; all ten index-split files together **75/75**; App.shell-layout + editor-shells **21/21**; App.test + App.stage-workbenches **42/42**; all 21 files of the four R11-6..9 splits together **161 passed / 1 skipped**; ratchet **5/5**.
- **Ratchet replay on every merge tree** (this review's script mirrors the guard's exact `wc -l` semantics against each commit's own allowlist and tracked files): `af32785`, `6a8775b`, `243a039`, `d8df503`, `c0e320a`, `6d82b38`, `9b6ef55`, `dfbbeae`, `cc14995`, `c8f74f1` — **all ten RATCHET-GREEN**. Zero allowlist drift at HEAD: all 37 entries equal the file's newline count exactly.
- Sequencing verified by ancestry: R11-2's merge is an ancestor of R11-3's branch ✓; R11-3's merge is an ancestor of R11-4's branch ✓. R11-6..9 branches were all cut from `bbb91a3` (post-R11-2 docs) and merged order-free with disjoint allowlist line edits.
- Exclusive-file check: every merge's first-parent diff stays inside its brief's writable set; `scripts/file-size-allowlist.json` was edited in-merge by exactly the tasks that owned an entry (R11-3 lowered index.ts 1146→979; R11-5 lowered App.tsx 1066→988; R11-4/6/7/8/9 deleted their file's entry). R11-3 did not touch `server/index.test.ts` at all. R11-5's `App.shell-layout.test.tsx` edit is additions-only (0 deleted lines, verified).

## Verdict table

| ID | Merge | Verdict | Substance |
|---|---|---|---|
| R11-1 | `af32785` (`f03e64c` red + `b40cf32` fix) | **ACCEPT** | The product diff is exactly the two `{ cause }` additions the brief asked for — `:112` readFile failure and `:114` quarantine-rename failure — with external `code`/`message` byte-unchanged (verified by diff). The red is genuine and rides a real kernel errno: `mkdir` squats the state-file path itself, `readFile` rejects EISDIR (not ENOENT), and the pin demands `cause.code === "EISDIR"` plus `failure === true` — unsatisfiable before the fix because `cause` was absent. The `:114` branch rides the same explicit-`{cause}` route without an invented mock-fs red, exactly as the brief instructed. All 8 tests re-run green by this review; the R10-1 persist-path pins pass unmodified. The `server/ai/*` unfreeze is now fully closed: every `StateStoreFailure` in the module carries the underlying error. |
| R11-2 | `6a8775b` (`f54f3d0` test+fix) | **ACCEPT-WITH-NITS** | The sweep is right and righteously narrow: `sweepStaleTemporaryFilesBesideFile` matches `startsWith("<basename>.")` plus `/^\d+\.tmp$/` on the remainder — i.e. `^<basename>\.\d+\.tmp$` and nothing else — with the shared-directory rationale written in the comment as demanded. The generic any-name shape stays confined to `dataDir` (both now delegate to one `sweepTemporaryFiles` with a caller-supplied matcher; readdir-failure = first boot = quiet; deletion warn-and-continue, pinned on a non-empty directory squatting the shape). Boot wiring: `if (resolve(dirname(stateFile)) !== dataDir) await sweepStaleTemporaryFilesBesideFile(stateFile)` sits before `store.load()`, and the boot pin proves order with the R10-2 technique — the injected store's `load()` reads the state directory and must see only the neighbor's `.tmp`. Neighbor discipline is pinned twice (boot and unit), including the prefix-boundary case `ai-runtime-state.jsonx.4.tmp`. All 17 tests green. Nits: (1) **red-repro discipline not carried in its stated form** — one commit holds test and fix together, no separate red commit and no verbatim red output in the message (the pin is constructed so it must have been red pre-fix, but the rule asks for committed evidence); (2) the boot wiring was folded onto the stateFile line to hold index.ts at 1146 — declared openly in the commit, and semantically defensible here, but it is the statement-joining pattern ratchet pressure produces; an honest two-line wiring plus entry edit would also have been fine. |
| R11-3 | `243a039` (`a9ff943` move + `f17be4a` seam test) | **ACCEPT** (strongest row) | The 173-line cluster (`:834–1006` on `bbb91a3`) moved into `createAiRoutes(deps).handle(ctx)` and this review reproduced the commit's byte-identity claim **exactly**: dedent −2, then the block diffs empty modulo precisely the 13 declared mechanical lines (9× `return;`→`return true;`, 4× the `Math.min(maxJsonBodyBytes, DEFAULT_MAX_AI_BODY_BYTES)` hoist to factory-time `maxAiBodyBytes` — same operands, computed once, the R10-4 `maxRoomBodyBytes` pattern). The judgment call the brief flagged was made correctly and is documented in both files: the agent/otherAi limiters, the production token gate, and the `AI_STATE_UNAVAILABLE` 503 stay in index.ts because they act on the whole `/api/ai/` prefix *including unmatched paths* — moving them would stop counting `POST /api/ai/unknown`, a behavior change, not a move. `readJson` failures still escape to index.ts's shared 413/400 mapper (stated at the seam). `server/index.test.ts` untouched entirely; existing AI pins pass unmodified (re-run inside 36/36). The new seam test is substantive: 21 tests across routing claims, deps wiring, validation shapes, 499/502 mapping, abort listener hygiene, and the budget-receipt commit-on-finish/rollback-on-close handover. index.ts 1146→979 (est. 985±25 — inside), allowlist lowered in the same commit, `ai-routes.ts` 241 / `ai-routes.test.ts` 400, both ≤ 400. |
| R11-4 | `cc14995` (`27a3daf`) + `c8f74f1` rename | **ACCEPT-WITH-NITS** | 2378-line `index.test.ts` → 126 residual + 9 domain files (91–338) + `index-test-fixtures.ts` (297), all ≤ 400. Machine-checked by this review: **318/318** `expect(`-line multiset byte-identical (fixture included), `it(` count **73** preserved, and `vitest list` enumerates **75** runtime tests across the ten files (the two extras are loop-registered, both sides). The fixture keeps the server-close/tmp-dir `afterEach` nets as a file-level hook each domain file installs at top level, plus the shared in-memory SSE stream and process-failure capture moved verbatim. All ten files re-run green together (75/75). Allowlist entry deleted in the merge itself; merge tree ratchet-green. Nit — **the round's one process finding**: the merge landed lint-red. The fixture installer was named `useServerFixture` and called at module top level, so `react-hooks/rules-of-hooks` fired in all ten consumers — reproduced first-hand by this review: scoped eslint shows exactly **10 errors at `cc14995` and 0 at `c8f74f1`**. The rename is a pure identifier swap (verified), product tests identical, and the orchestrator's instruction to treat it as part of the landing is right. But the branch ran targeted vitest and typecheck without scoped eslint over its new files — the same class of miss as Round 10's ratchet-red merges, one guard over. Round 12 carries lint-green-per-merge as an explicit rule, and test fixtures never take `use*` names. |
| R11-5 | `d8df503` (`b620318` + `bd38107` pin) | **ACCEPT** | Both named branches extracted: `LegacyEditorTopbar` (128) and `ExportProjectDialog` (53). DOM verbatim verified by this review against the old inline `:811–910`: the dialog's JSX is byte-identical (the `{cond && X}` wrapper became an early-return `if (!...) return null` — render-identical); the topbar is byte-identical modulo the declared seam substitutions — `workflowNav`/`backButton` ReactNode slots (App passes the *same* `WorkflowStageStepper`/`WorkbenchBackButton` elements, verified at the call site; `{projectId && X}` → `projectId ? X : null` renders the same), and handler props (`onUndo={handleUndo}` etc.) threaded 1:1. All state stays on the existing `posterExport` seam; latest-ref and effect wiring stayed in App. `App.shell-layout.test.tsx` +28 with **zero deletions**; no other App test touched; new topbar pin covers brand, stage slot, and toolbar groups. App.tsx **988 ≤ 990 gate**, allowlist lowered in the same merge — the R10-5 stale-entry lapse not repeated. Re-run: 21/21 shell/seam pins, 42/42 App shards. |
| R11-6 | `c0e320a` (`46cd6ff`) | **ACCEPT** | 983-line `PosterCanvas.test.tsx` → 7 domain files (82–283) + harness (63), no residual; product untouched (merge stat has no product file). Machine-checked in full: **152/152** expect multiset byte-identical, `it` count **47** preserved (59 runtime with loop-registered cases). Harness keeps the tracked-roots `afterEach` drain with the R9-6 "reports, never stands in" rationale in-code, restores the stubbed `Worker` global, and clears `cardLayoutCache` between tests — and its installer is named `installPosterCanvasTestHarness`, the naming R11-4 got wrong. Allowlist entry deleted in-merge. |
| R11-7 | `6d82b38` (`41c2761`) | **ACCEPT** | 958-line `card-layout.test.ts` → 4 domain files (111–274) + fixtures (255), no residual; solver/worker/product frozen and untouched. Machine-checked: **70/70** expect multiset identical, static `it` 29 preserved (**47 runtime** via `it.each` expansion — matching the ledger). Bench gating semantics untouched (the 400-card median discipline lives outside these files; nothing in the merge touches it). Allowlist entry deleted in-merge. |
| R11-8 | `9b6ef55` (`65fd7ef`) | **ACCEPT** | 696-line `agent-session.test.ts` → 5 domain files (75–293) + fixtures (34); product untouched. Machine-checked: **135/135** expect multiset identical; **33 `it(` + 2 `it.each`** preserved on both sides (the ledger's "35 it" counts both forms — exact). The pre-existing `agent-session.deadline.test.ts` (Round-3 era) was correctly left alone. Allowlist entry deleted in-merge. |
| R11-9 | `dfbbeae` (`5ba2aca` + `547f47c`) | **ACCEPT** | 655-line `project-store.test.ts` → 78 residual + 4 domain files (51–158) + fixtures (157); product untouched (saturated list respected). Machine-checked: **87/87** expect multiset identical, `it` 27 preserved (26 runtime + the `PROJECT_LIST_BENCH`-gated bench moved intact into `project-store.bench.test.ts` — the suite's skip count stays honest). The follow-up `547f47c` drops a dead `IDBFactory` import from the fixtures — in-branch hygiene before the merge, not a post-merge heal. Allowlist entry deleted in-merge. |

## Answers to the eight questions

### 1. Did R11-1 actually attach cause on load-path EISDIR?

**Yes — verified at the diff level and at runtime.** The merge's whole product delta is two `{ cause }` / `{ cause: renameCause }` additions on the exact `:112`/`:114` branches the Round-10 review filed; the stable `code`/`message` strings are byte-unchanged, so this is observability, not shape. The red commit (`f03e64c`) predates the fix and pins the strongest available evidence: with a directory squatting the state-file path, `readFile` rejects with a real EISDIR, and the test demands the rejection expose `cause.code === "EISDIR"` — impossible on the old code, which dropped the fs error entirely. The quarantine-rename branch (`:114`) rides the same fix without a mock-fs red, exactly as the brief instructed (its rename target embeds `Date.now()`; forcing it on a real fs under root isn't worth inventing a fake fs for). 8/8 green re-run by this review, including all R10-1 persist-path and sidecar-cap pins unmodified. The module's `:50` contract — "原始 errno 挂在 cause 上" — is now true on every path, and the R11-1 narrow unfreeze closes with nothing left behind.

### 2. Does R11-2 only sweep `^<basename>.\d+.tmp$` in the foreign dir (not generic)?

**Yes — narrow by construction, and pinned from both directions.** The matcher is `name.startsWith("<basename>.")` && `/^\d+\.tmp$/.test(remainder)`, which composes to exactly `^<basename>\.\d+\.tmp$`; the generic `^.+\.\d+\.tmp$` shape remains confined to `dataDir`. The unit pin seeds eight neighbors — another program's `someone-elses.json.4.tmp`, the prefix-boundary trap `ai-runtime-state.jsonx.4.tmp`, a non-numeric `.abc.tmp`, a `.tmp.bak`, the live state file, and a `.corrupt-*` sidecar — and demands all survive while both `.4.tmp`/`.<pid>.tmp` orphans of *this* file are collected. The boot pin repeats the discipline end-to-end: `AI_STATE_FILE` in a second temp directory, one own-orphan plus one same-shaped foreign `.tmp`, and the injected store's `load()` observes the directory — proving both that the sweep ran **before the first reader** (the R10-2 structural technique, extended) and that the neighbor was untouched. The wiring only fires when `resolve(dirname(stateFile)) !== dataDir`, so the in-dataDir case keeps a single sweep. The comment carries the operator-shared-directory rationale the brief demanded. Nits are process, not substance (see verdict row): no committed red evidence, and the one-line fold that keeps index.ts at 1146.

### 3. R11-3: rate limiters stayed in index — correct? Any AI pin drift?

**Correct, and it's the row's best judgment call.** The agent/otherAi limiters, the production token gate, and the `AI_STATE_UNAVAILABLE` 503 all act on the `/api/ai/` **prefix** at `index.ts:511–537` — before route matching — so they count and gate `POST /api/ai/unknown` too. Moving them into `createAiRoutes` (which returns false on unmatched paths) would silently exempt unknown AI paths from limiting: a behavior change smuggled into a "move". The commit states this, the deps-struct doc comment repeats it at the seam, and the seam test pins the complementary contract (unknown AI paths hand back to index.ts and still 404). **No pin drift**: `index.test.ts` was not in the merge at all (verified by stat); the byte-identity of the moved block was reproduced by this review (173 lines, empty diff modulo the 13 declared mechanical lines); the AI pins — now living in `index.ai.test.ts` after R11-4, with a 318/318 multiset guaranteeing no assertion text changed — re-run green (36/36 with the seam test). The SSE/events-ticket block was untouched by construction (the merge's index.ts hunk range never reaches it) and the SSE split files pass unmodified.

### 4. R11-4: expect multiset preserved? The `use*` lint miss — NITS or blocker?

**Multiset preserved, machine-checked in full**: 318/318 sorted `expect(`-lines byte-identical across {residual + 9 domain files + fixture} versus the 2378-line original; `it(` count 73 both sides; 75 runtime tests enumerated by `vitest list`, matching the ledger. Every file ≤ 400 (max: `index.rooms.test.ts` 338). The nets survived the move: server close + tmp-dir cleanup live in the fixture's `installServerFixture()` as a file-level `afterEach` each domain file installs.

**The lint miss is a NIT, not a blocker — but it's the round's process finding.** Reproduced first-hand: scoped eslint at `cc14995` yields exactly 10 `react-hooks/rules-of-hooks` errors (the `useServerFixture` name puts a node-env test helper in hook-rule scope; module-top-level calls then violate it), and 0 at `c8f74f1`. The rename is a pure identifier swap across 11 files (verified — every hunk is the same substitution), tests are byte-identical, product untouched, so no behavior question exists. It stays a nit because the healed state is what ships and the orchestrator folded the fix into the landing before review. It matters anyway: the branch's own verification ran targeted vitest and `tsc` but never linted its new files, so `git bisect` across `cc14995`/`2045f93` hits lint-red trees — the same intermediate-tree hygiene failure Round 10 had with the ratchet, on a different guard. Round 12's constraints add lint-green-per-merge (scoped eslint over every file the merge touches, before pushing) and a naming rule: test fixtures/harnesses take `install*`/`create*` names, never `use*`.

### 5. R11-5: App.tsx ≤ 990 with verbatim DOM?

**988, gate met, and verbatim verified rather than assumed.** This review byte-compared both extractions against the pre-merge inline branches (`243a039:src/App.tsx:811–910`). `ExportProjectDialog`: JSX byte-identical — backdrop, role/aria-modal/aria-label, the resource-pack checkbox with its exact 文案, the conditional warning, both footer buttons; the only structural delta is `{cond && X}` → early-return `null`, render-identical. `LegacyEditorTopbar`: byte-identical modulo declared seams — `workflowNav` and `backButton` are ReactNode slots and App's call site passes the *same elements* as before (checked), handler props thread 1:1, and `{projectId && X}` → `projectId ? X : null` renders identically. Class names, roles, aria labels, brand spans, and toolbar grouping all byte-equal. No App pin rewrite: `App.shell-layout.test.tsx` gained 28 lines with zero deletions (the new topbar pin from `bd38107`); no other App test touched; 42 assertions' worth of App shards re-run green unmodified. The allowlist entry rode the merge (1066 → 988) — the exact discipline R10-5 missed.

### 6. Splits 6–9: expect multisets; all files ≤ 400; allowlist deleted in-merge?

**Checked in full, not sampled, all four** (same standard as Round 10): sorted `expect(`-line multisets over {residual + domain files + harness/fixture} versus each pre-split file — **152/152** (PosterCanvas), **70/70** (card-layout), **135/135** (agent-session), **87/87** (project-store) — all byte-identical, zero lines in either direction. Test-registration counts preserved exactly: 47 / 29+`it.each`→47 runtime / 33`it(`+2`it.each` (the ledger's "35") / 27. Every split file ≤ 400 — the largest is `agent-session.landing.test.ts` at 293. **All four merges deleted their own allowlist entry in the merge itself** (verified in each merge stat and by the per-tree ratchet replay). Extras worth recording: R11-6's harness restores the `Worker` stub and clears the layout cache per test, keeping the drain discipline intact; R11-7 left the frozen solver stack and bench gating untouched; R11-9 moved the `PROJECT_LIST_BENCH` skip-gate intact into its own bench file so the suite's "2 skipped" stays honest; all 21 files re-run green together (161 passed / 1 env-skipped).

### 7. Honest next extraction for Round 12: index.ts 979 and App.tsx 988?

**`server/index.ts` (979) is at its honest floor for route extraction — report, don't task.** Measured on HEAD: module helper belt `:58–236` (~180 lines: clientIp, auth trio, body/byte parsing, boot-fact readers — individually small, cohesion mixed, no cluster ≥ ~50 lines that doesn't already have one consumer); factory setup `:237–502` (266 lines, sharing closures with the frozen SSE bookkeeping — ticket table, stream registry, serialization memoization); health/live/ready `:538–579` (42) + workspace GET/PUT `:580–620` (41), still entangled with the frozen persist-failure health surface the R10 review deliberately left; the frozen SSE block `:623–838` (216); dispatch tail and lifecycle/ready/boot `:841–979`. Every remaining candidate either touches frozen surface or relocates < 90 lines of closure-heavy code for no safety yield. The test side is done (126 residual).

**`src/App.tsx` (988) has one last honest carve, measured**: the canvas stage `:879–923` (45 lines — zoom shell math + the 22-prop PosterCanvas site) and the inspector aside `:925–959` (35 lines — InspectorPanel + the project-summary `<details>`). Both are self-contained named regions; extraction threads ~20 props each, so the net yield is ≈ 55–65 lines → **gate ≤ 940** (App.tsx has no queue predecessor; per the R9-7 lesson this is a gate). After that, App.tsx is seamed state/handlers (`:141–698`, R9-5's work) plus pure composition, and further carving would slice mid-expression — the floor is real. **The 400-rule debt now lives in nine mid-size test files** (647 → 452: MapLayer, AppErrorBoundary, AssetPanel, AgentAssistant, ProjectWorkbench, export-poster, binary-import, agent-conversation-store, project-package) — Round 12 clears the top eight with the proven split playbook; ratchet-green-per-merge remains in force (it held 10/10 this round) and lint-green-per-merge joins it.

### 8. Any merge blocker?

**No.** No behavior regression anywhere: the one product-path move (AI routes) is byte-verified modulo declared mechanics; both defect fixes are two-line-scale, red-pinned (R11-1) or double-pinned (R11-2), with all neighbor pins passing unmodified; the App extraction is byte-compared; all five test splits carry full multiset identity. Every merge tree is ratchet-green — first-hand replay, all ten commits — and the allowlist has zero drift. The two nits (R11-2's uncommitted red evidence, R11-4's two-commit lint-red window) are process hygiene on an already-healed history; the tree every gate ran on is green, typecheck is 0, and the suite is corroborated at 1969/264.

## Cross-checks beyond the questions

- **R11-2 sweep safety net re-verified**: a directory named like an orphan (`ai-runtime-state.json.42.tmp/` with a child) fails `rm` into the warn-and-continue path — boot cannot be blocked by the sweep; pinned.
- **R11-3 receipt lifecycle after the move**: `commitReceipt`/`rollbackReceipt` still hang off `response` finish/close inside the moved block (byte-equal), and the seam test adds two-sided pins (commit-once-on-finish, rollback-on-early-close) that didn't exist before — the extraction *increased* coverage on the most stateful AI path.
- **R11-4 fixture scope**: `index-test-fixtures.ts` exports only what the domain files import (startServer, raw HTTP helpers, process-failure capture, in-memory SSE pair, `installServerFixture`, workspace request kit); no test logic hides in the fixture beyond the shared `afterEach` — assertion text all lives in the multiset-checked files.
- **Docs commits are docs-only**: `bbb91a3`, `72b8e2f`, `2045f93` touch `PROGRESS.md` exclusively (verified by stat); the ledger's per-merge rollback lines (revert the merge SHA) are accurate against the graph.
- **The queue rule held under temptation**: R11-3's brief forbade editing `index.test.ts` even though its AI pins were about to move in R11-4 — and the merge indeed left it byte-untouched; R11-4 then moved them with multiset proof. Race avoided exactly as the sequencing intended.
- **R11-10 (PR-surface close-out) is deferred, not missed** — same shape as Round 10: PROGRESS.md's Round-11 section is current through the full-suite ledger and the rename note; the PR #14 description close-out lands after this review as the round's tenth row (filed as R12-10 with the Round-11 evidence enumerated).
- **Known-miss ledger unchanged**: idle SSE member (cannot HTTP-fix, documented since R9) and the 12 MiB durable ceiling — neither newly evidenced this round, neither tasked in Round 12.
- **Size debt after the round**: allowlist 37 entries (was 42). Actionable: the nine 452–647-line test files (question 7). Listed-not-actionable: `styles.css` 3798, the two `src/data` files, and the frozen products (`card-layout.ts` 1662, `PosterCanvas.tsx` 1588, `collaboration.ts` 1011, `DataWorkspace.tsx` 885).

## Verification chain (per repo discipline)

failure → cause → fix → recheck was exercised on this review's own tooling: the first ratchet replay flagged all ten trees red on three files (`card-layout.test.ts` 959 vs 958 etc.) → cause isolated: `git grep -c` counts a final unterminated line where the guard counts `\n` bytes (`wc -l` semantics), so files without trailing newlines read one high → fix: exact blob newline counts for every allowlisted/boundary file → recheck: all ten trees green, and the real guard (`scripts/file-size-ratchet.test.ts`) re-run 5/5 at HEAD with a zero-drift scan across all 37 entries. The R11-4 lint story was verified by deliberate two-sided reproduction (10 errors at `cc14995`, 0 at `c8f74f1`) rather than trusting the commit message. All other checks passed on first run; no retry-to-green anywhere. Two typechecks, scoped eslint over every changed file, eight targeted vitest slices (437 tests re-run), five full expect-multiset identities, two byte-identity comparisons (AI route block; both App extractions), the ten-tree ratchet replay, and the ancestry checks were all executed fresh in this review's isolated worktree.

**Merge blocker: none.** Round 12 briefing: `.agent_workspace/round-11-briefing.md`.
