[Model: claude-fable-5]
# Round 12 Briefing (from the Round-11 SOTA review)

## Direction

Round 11 closed both carried defects (load-path errno on `cause`; out-of-dataDir orphan sweep with a deliberately narrow matcher) and hit every size number honestly: `server/index.ts` 979, `server/index.test.ts` 126 + nine domain files, `src/App.tsx` 988, four more big test files split with full multiset proof — and, for the first time, **every merge commit was ratchet-green on its own tree** (replayed first-hand, ten for ten). This review found **no new evidenced product defect**: the AI state store now carries the underlying error on every path, the sweep's neighbor discipline is pinned from both directions, and the one flaw of the round was a guard miss, not a behavior miss — R11-4's merge landed with 10 `react-hooks/rules-of-hooks` errors because its shared fixture was named `useServerFixture`, healed two commits later by the `installServerFixture` rename. So Round 12 is a **finishing round for the 400-rule debt**: `server/index.ts` is measured at its honest floor (remaining mass is the frozen SSE block `:623–838`, factory closures that feed it, and the health/workspace routes still entangled with the frozen persist-failure surface — extraction there relocates closure-heavy code for no safety yield; **not tasked, on the record**); `src/App.tsx` has exactly one honest carve left (canvas stage `:879–923` = 45 lines, inspector aside `:925–959` = 35 lines, both self-contained, net yield ≈ 55–65 after ~20 props each); and the remaining debt is nine mid-size test files, 647 down to 452 lines, all splittable with the playbook Rounds 9–11 proved five times over. Round 12 takes the top eight plus the App carve plus the docs close-out.

Not tasked, with reasons: `server/index.ts` extraction (at floor, measured — see review question 7); `project-package.test.ts` (452 — ninth in line; take it in Round 13 if the eight land clean, or fold it in early if a split task finishes with slack); the barely-over files (`scene-document.test.ts` 401, `App.collaboration-terminal.test.tsx` 402, `project-document.test.ts` 403, `sync-china-locations.test.ts` 418) — the ratchet holds them and a split that nets three files of 140 lines is churn, not safety; the idle-SSE-member remainder (cannot be fixed over HTTP for a genuinely idle client; documented since R9, unfreeze bar unmet); the 12 MiB durable ceiling (not newly evidenced in Round 11); product files `useCollaborationRoom.ts` 847, `agent-session.ts` 799, `collaboration-client.ts` 754, `AssetPanel.tsx` 754, `AgentAssistant.tsx` 705 — no evidenced defect in any of them, and product carving without a defect or a measured seam is risk without yield; `styles.css` and `src/data/*` (listed debt; never hand-mangle).

Quality bar unchanged plus one addition. **No metric, no merge; red repro first — as a separate red commit or with the observed red state recorded verbatim in the commit message** (R11-2 carried neither: the fix was right but the rule exists so reviewers can see the red; splits and moves carry machine-check discipline instead — sorted `expect(` multiset byte-identical including the fixture/harness, `it`/`it.each` counts preserved, both quoted in the commit); **targeted vitest only** (`npx vitest run <file>`); typecheck (`tsc -p tsconfig.app.json` and `-p tsconfig.node.json`, `--noEmit`) exit 0 after every task; CI green on the merged tree. **Ratchet merge discipline stays in force** (it held 10/10 in Round 11: allowlist edits — lower R12-1's App entry, delete each split's entry — ride the merge itself; rebase before merging if entries changed under you; run `npx vitest run scripts/file-size-ratchet.test.ts` on the merge result before pushing). **New, from the R11-4 finding — lint merge discipline:** run scoped eslint over **every file the merge adds or touches** (`npx eslint <files>`) before pushing, and it must report 0 errors; test fixtures/harnesses take `install*`/`create*` names, never `use*` — a `use*`-named helper is hook-rule scope and will fail at module top level in node-env files. One task = one exclusive writable file set — no sequenced queue this round, the first time in the campaign; the shared `scripts/file-size-allowlist.json` is edited only for your own file's entry. If a fix forces an edit in a cross-owned file, stop and file it.

## Tasks (exactly 10; writable sets disjoint; own-entry allowlist edits only)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R12-1 | `src/App.tsx`, NEW `src/components/editor/*` components (+ pins in `src/App.shell-layout.test.tsx`, additions only) | App.tsx 988 after R11-5. The last two self-contained JSX regions, measured on HEAD: the canvas stage `:879–923` (45 lines — `editor-area`/`canvas-stage`/zoom-shell math and the 22-prop PosterCanvas site) and the inspector aside `:925–959` (35 lines — `editor-inspector` aside: InspectorPanel + the project-summary `<details>` with sync-status copy). | Deliverable (behavior-preserving, the R10-5/R11-5 discipline): extract both regions into `src/components/editor/*` with props typed at the seam; DOM structure, class names, ids (`editor-inspector`), roles, data attributes (`data-sync-status`), and 文案 verbatim — all existing App split-file pins pass **unmodified**; zoom math and `posterRef` threading unchanged; latest-ref/effect wiring stays in App. Metric: **App.tsx ≤ 940** (a gate — no queue predecessor; measured from 988 with the named regions summing 80 lines and ~20 props each of wiring); new files ≤ 400; existing App tests green unmodified; typecheck 0; **lower the App.tsx allowlist entry in this same merge**; scoped eslint 0 on every touched file. After this merge App.tsx is at its floor: record the residual in PROGRESS.md so Round 13 doesn't re-litigate it. |
| R12-2 | `src/components/canvas/MapLayer.test.tsx` (split), NEW `src/components/canvas/MapLayer.*.test.tsx` + NEW shared harness (names indicative) — **MapLayer/MapDataLayer product frozen; test-only** | 647 lines against the 400 rule; 16 `it` / 66 `expect(` lines; jsdom, mounts via `createRoot` (2 sites). | The R11-6 discipline: domain split + shared harness with the tracked-roots `afterEach` drain (the R9-6 guard reports a missing net, it does not stand in for one) and any global stubs restored per test; zero assertion changes machine-checked (sorted `expect(` multiset byte-identical including the harness, `it`/`it.each` counts preserved — quote both in the commit); every file ≤ 400; harness installer named `install*`. Metric: split files green in isolation and together; typecheck 0; **delete the allowlist entry in the same merge**; scoped eslint 0. |
| R12-3 | `src/components/AppErrorBoundary.test.tsx` (split), NEW `src/components/AppErrorBoundary.*.test.tsx` + NEW shared harness — **AppErrorBoundary.tsx product untouched (403 lines, its own allowlist entry stays)** | 613 lines; 24 `it` / 100 `expect(` lines across 4 describes (a natural domain seam); jsdom, mounts. | Same discipline as R12-2. Error-boundary tests often stub `console.error` and window handlers — the harness must restore every stub per test, and crash-recovery pins keep their exact DOM/copy assertions. Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-4 | `src/components/AssetPanel.test.tsx` (split), NEW `src/components/AssetPanel.*.test.tsx` + NEW shared harness — **AssetPanel.tsx product untouched** | 587 lines; 20 `it` / 62 `expect(` lines; jsdom, mounts. | Same discipline as R12-2 (upload/replace/delete flows and usage-label pins split by domain; fixture data — user assets, fonts — moves verbatim into the harness). Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-5 | `src/components/AgentAssistant.test.tsx` (split), NEW `src/components/AgentAssistant.*.test.tsx` + NEW shared harness — **AgentAssistant.tsx product untouched** | 569 lines; 23 `it` / 116 `expect(` lines; jsdom, mounts; conversation-store interplay makes these the densest assertions in the batch. | Same discipline as R12-2. The assistant tests script fetch/transport doubles — centralize them in the harness the way R10-8 did for collaboration-client so one script drives every domain file; restore fetch per test. Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-6 | `src/components/ProjectWorkbench.test.tsx` (split), NEW `src/components/ProjectWorkbench.*.test.tsx` + NEW shared harness — **ProjectWorkbench.tsx product untouched (saturated list)** | 564 lines; 31 `it` / 116 `expect(` lines across 2 describes; jsdom, mounts (3 sites). | Same discipline as R12-2 (workbench listing/rename/delete/navigation domains; IndexedDB fixture setup moves verbatim into the harness — mind the R11-9 lesson: keep env-gated or slow cases in their own file so skip counts stay honest). Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-7 | `src/lib/export-poster.test.ts` (split), NEW `src/lib/export-poster.*.test.ts` + NEW shared fixture — **export-poster.ts product untouched** | 509 lines; 22 `it` / 78 `expect(` lines across 3 describes; no React mounting (canvas/blob doubles instead). | The R11-7 discipline (fixture module, no harness needed beyond stub restore): split by domain; canvas/OffscreenCanvas/blob stubs centralized in the fixture and restored per test; zero assertion changes machine-checked. Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-8 | `src/lib/binary-import.test.ts` (split), NEW `src/lib/binary-import.*.test.ts` + NEW shared fixture — **binary-import.ts product untouched** | 471 lines; 30 `it` / 67 `expect(` lines across 4 describes; pure functions, the easiest split in the round. | Same discipline as R12-7 (xlsx/binary fixture builders move verbatim into the fixture module). Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-9 | `src/lib/agent-conversation-store.test.ts` (split), NEW `src/lib/agent-conversation-store.*.test.ts` + NEW shared fixture — **agent-conversation-store.ts product untouched** | 464 lines; 17 `it` / 49 `expect(` lines; storage doubles, no mounting. | Same discipline as R12-7. Metric: multiset + `it` counts quoted; all files ≤ 400; green in isolation and together; typecheck 0; **allowlist entry deleted in the same merge**; scoped eslint 0. |
| R12-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) — **last** | 交付纪律: Round-11 evidence must reach the PR surface (the Round-10 pattern — the close-out lands after the fable review). | Deliverable (docs, no code): description gains (a) Round-11 gates: 264 files passed / 2 skipped; 1969 passed / 2 skipped (98.27s); lint 0 err / 1 frozen warning (after the `installServerFixture` rename); typecheck 0; review 7 ACCEPT + 2 NITS, no blocker; ratchet-green 10/10 merge trees (first clean round); (b) rollbacks per merge: R11-1 revert `af32785`, R11-2 revert `6a8775b` (note the sweep deletes matching own-name `.tmp` beside an out-of-dataDir state file on next boot by design), R11-3 revert `243a039`, R11-4 revert `cc14995` **and** `c8f74f1` together (the rename rides the split), R11-5 revert `d8df503`, splits revert `c0e320a`/`6d82b38`/`9b6ef55`/`dfbbeae`; (c) known misses stated plainly: idle SSE member (SSE frozen, documented); 12 MiB ceiling (unchanged); index.ts 979 at measured floor; App.tsx 988 with R12-1 filed; nine test files 452–647 with R12-2..9 filed; (d) the **lint merge discipline** rule (scoped eslint per merge; no `use*` fixture names) written into PROGRESS.md's Round-12 notes verbatim next to the ratchet rule so the next orchestrator inherits both. |

## Sequencing

No queue this round — all writable sets are disjoint. Parallel-safe starts at round open: R12-1 through R12-9, all nine. R12-10 last. Allowlist coordination: each task edits only its own file's entry, in the merge itself, and rebases onto whatever entries changed under it — Round 11 landed ten-for-ten ratchet-green; keep the streak, and extend it to lint (scoped eslint 0 on every merge's own tree before pushing).

## Saturated — leave alone this round

`server/index.ts` at 979 — measured floor; the helper belt `:58–236`, factory setup `:237–502`, and health/workspace routes `:538–620` are documented as not-worth-moving (frozen-surface entanglement or closure-heavy relocation; review question 7); `server/index-test-fixtures.ts` and the ten index split files (fresh); `server/ai-routes.ts` + test (fresh, byte-verified); `server/room-routes.ts`, `server/static-files.ts`, `server/room-snapshot-store.ts` + `boot-tmp-sweep` (fresh, pinned); `server/collaboration.ts`; the persist-failure chain end to end (closed R7-1→R9-3, errno-proven R9-4; only a new evidenced defect reopens); `src/components/ProjectMenu.tsx`, `ProjectWorkbench.tsx`, `StorageNotice.tsx`; `src/lib/editor-project-store.ts` + `project-store.ts` product; the R9-5 seams and the R10-5/R11-5 editor components (fresh); all Round-9/10/11 test splits and their harnesses; `server/durability-*.ts` journeys (extend only with new mechanisms); the R9-6 guard and the tracked-roots pattern; `.github/workflows/ci.yml`; `project-package.test.ts` 452 and the 401–418 barely-over files (ratchet holds them; churn without yield); `src/styles.css` and `src/data/*` (listed debt).

## Frozen (campaign runs to round 20)

`server/ai/*` — **fully refrozen**: R11-1's narrow unfreeze is closed (every `StateStoreFailure` path now carries `cause`; verified at diff level this round); only a new evidenced defect in a named file reopens anything. `src/lib/collaboration-operations.ts` (consume only). SSE wire format **still frozen** — the events/events-ticket block (`server/index.ts:623–838`) stays byte-identical; drain must not emit `closed`; the idle-member remainder stays documented-not-tasked (cannot be HTTP-fixed). Layout stack internals (`card-layout.ts`, worker, solver) and the 400-card median bench gates. PosterCanvas product (`PosterCanvas.tsx` 1588) and MapLayer/MapDataLayer product — R12-2 splits only the tests. DataWorkspace product code (the exhaustive-deps warning at `DataWorkspace.tsx:257` stays the repo's one intentional lint warning). Project-package format; project-document rejection identity. The persist-failure chain unless newly evidenced. Payment/套餐/模板手续费 boundaries per `docs/开源与收费边界.md` — no payment or billing code in this repo.
