[Model: claude-fable-5]
# Round 7 SOTA Review

Reviewed tree: worktree `/tmp/wt-r7-fable`, branch `cursor/r7-fable-review-187a` cut from `origin/agent/opt-continuous` @ `362c318` (includes all of R7-1..R7-9 plus R7-3b and the closeout ledger). Round-7 scope (against R6 close `f0977d5`): ten merges, every one read in full against its first parent with per-merge file-ownership checks. File discipline is **exact on all ten** — no merge touches a file outside its briefed exclusive set. No product code was modified by this review. `npm test` / `npm run lint` were NOT run (orchestrator serializes; recorded: **189 files, 1757 passed / 2 skipped, 83.0s; lint 0 errors / 6 warnings**). This review DID run first-hand: `./node_modules/.bin/tsc -p tsconfig.app.json --noEmit` and `-p tsconfig.node.json --noEmit` — **both exit 0**; the room-snapshot bench (`tsx scripts/perf-room-snapshot-bench.ts`) — numbers reproduce (below); a scoped eslint over `src/components`, `src/lib`, `src/main.tsx` — exactly the 6 recorded warnings, files named in cross-check 5. CI independently corroborates: run 32783454636 on the push of `362c318` is green, and the branch carries a follow-up ledger commit confirming it.

## Headline

**No merge blocker.** All ten rows accepted: 8 ACCEPT, 2 ACCEPT-WITH-NITS, 0 REJECT. The Round-6 flagship — the false death warning for trimmed rooms — is genuinely closed end to end: `lastPersistOutcome` now records failures (R7-1), the tri-state `persistence: { outcome, at }` rides create/join/snapshot with `at: 0 → null` (R7-2), the client parses it with the same strict discipline as the boolean, the hook exposes it display-only, the menu splits the copy (R7-3), and R7-3b's one-line App wiring plus end-to-end test proves the trimmed copy actually reaches the DOM from a real create response — with boolean-only servers still pinned to the death copy at menu, hook, and App layers ("宁可把裁剪说重,不能把死亡说轻"). R7-8 then proves the whole trim band over real HTTP + real files: near-limit room, history over the cap, SIGTERM, envelope `trimmedRoomIds`, reboot restores at the right version, stale `operations?afterVersion=0` answers 409 `VERSION_CONFLICT` with the re-snapshot copy, up-to-date client unaffected, next flush reports `persistence.outcome: "trimmed"` and health carries the id. Both R6-3 confirmed items are fixed (R7-4 refresh, R7-5 seam), the bench's dead cell is dead no more (R7-6, re-run by this review), the flake class got its two known instances fixed plus a 36-file scan for R8 (R7-7), and App.tsx dropped 2322→1988 with a faithful extraction (R7-9).

The largest finding of this review is once more one seam beyond the round's chain: **the member-facing surfaces go stale-good again exactly one level up from where R7-1 fixed it.** `roomPersistence()` in `server/index.ts` computes the tri-state from the last *successful* flush only and ignores R7-1's `lastFailure` — so during a disk-failure streak, create/join/snapshot keep answering `persisted`/`trimmed` with the old timestamp, and the only surfaces that show the streak are `/api/health` and the deduped console.error. The people whose rooms are silently not being written are, again, the only ones not told. That heads Round 8, together with the warn-template legend defect this review reproduced live (below).

## Per-task verdicts

### R7-1 persist failure on lastPersistOutcome (`1664494`) — ACCEPT
- Red-first held as separate commits (`84f37aa` red → `012bc9c` fix). Additive `lastFailure?: { at, message }` is set in the shared `startPersist` rejection handler — so both interval and explicit `flush()` paths record it — and cleared on the next success; the last successful `skippedIds`/`trimmedIds` stay readable throughout a streak (pinned across success → two distinct failures → success-clears). `at: 0` documented in-code as "never succeeded" and pinned via a failure-before-any-success test that also covers non-`Error` rejections (`"disk on fire"` string through `describePersistError`). The interval test pins the outcome record *and* that the deduped console.error behavior is untouched (1 error call for 2 failures). Defensive copies preserved; `lastPersistOutcome()` now also answers when only a failure exists (empty ids, `at: 0`). Test file additions-only (130+/0−); existing persist tests unmodified.

### R7-2 persistence tri-state on room HTTP responses (`b959478`) — ACCEPT-WITH-NITS
- Substance exact. `roomPersistence()` returns `{ outcome: "persisted" | "trimmed" | "skipped", at: number | null }`; skip checked before trim; case-insensitive id match preserved; **`at: 0` maps to `null`** with the reason in-code ("那不是 1970 年"); store without the accessor degrades to `persisted` + `null` (pinned by stripping the accessor, on create and snapshot); the compat boolean is now derived from the same single `lastPersistOutcome()` read (`512d493`), so the two fields cannot contradict within one response. All three outcomes pinned on all three endpoints over real HTTP with lowercase outcome ids against uppercase room ids. `/api/health.rooms.lastFlush` gains R7-1's failure record by raw pass-through, and — the strongest test — a real success → failure flush cycle pins that health shows `lastFailure` with the *old* success timestamp ("磁盘坏掉的整段时间里，健康检查只报上一次成功就是把事故说成正常"). Existing R6-2 tests unmodified.
- Nits: (1) **red-first discipline not held**: the branch is `55e4e7f` (feat, tests + fix bundled) → `512d493` (refactor); no separate red commit and no observed-red record in the message — the only R7 task where the red state is documented nowhere. Substance is fully pinned, so this is a process nit. (2) Health's raw pass-through still publishes `at: 0` un-mapped on `lastFlush` for a never-succeeded store — interpretable because `lastFailure` sits next to it, but a health consumer parsing `at` as a timestamp gets 1970; observation only, ops-facing by design since R6-2.

### R7-3 skip vs trim menu copy (`8d5f046`) — ACCEPT
- Red-first held (`e30b30a` red → `637f8d9` fix). Client: `persistenceOutcomeOf` accepts exactly the three known literals — case variants (`"TRIMMED"`), future outcomes (`"purged"`), string-typed objects, `{ at }`-only, and `null` all yield *no verdict* (each pinned); a known outcome with an unusable `at` keeps the outcome and nulls the timestamp ("结论本身不跟着一起丢", pinned twice); both response shapes parsed. Hook: `roomPersistenceKind` is display-only — the diff touches no heal/terminal/offline logic; tri-state responses update kind + degraded together, boolean-only responses update degraded only and *keep* the previous kind (documented, and harmless: the note renders only while degraded); pinned transitions include boolean-only → null verdict, later-response-keeps-verdict, newer-snapshot-overrules-handshake, and reset on leave + next room, each with negative asserts that offline/terminal/status are untouched. Menu: `PERSISTENCE_NOTE_COPY` splits skipped (death copy unchanged) from trimmed (survives-restart copy per the brief, verbatim); unknown/`persisted`+degraded contradictions and boolean-only servers fall back to the death copy; `data-collaboration-persist-kind` added for DOM pins; the trimmed pin includes a negative match on 服务器重启后将无法恢复; precedence terminal > offline > persistence re-pinned per kind; non-dismissibility re-pinned. Existing R6-6 precedence tests unmodified.

### R7-3b App wires roomPersistenceKind (`a306069`) — ACCEPT
- Not in the Round-6 briefing — an orchestrator-added chain-completion task, and a necessary one: R7-3's briefed file set excluded `App.tsx`, so without this the menu would never receive the kind prop and every degraded room would keep showing the death copy in the real app. One product line (`roomPersistenceKind={collaboration.roomPersistenceKind}`) plus an end-to-end App test: real create response carrying `persistence: { outcome: "trimmed", … }` → rendered note with `data-collaboration-persist-kind="trimmed"`, negative match on the death copy, positive on 重启后房间会恢复. Shares `App.tsx`/`App.test.tsx` with R7-9; the orchestrator sequenced it after R7-9's merge, so no live collision — noted so future briefings assign shared-file follow-ons explicitly. Test+fix in one commit; for a one-line wiring whose red is the added test itself, acceptable.

### R7-4 createProject refresh + filename helper move (`533f4a1`) — ACCEPT
- The red repro is exactly the briefed one and its observed red state is recorded with numbers in `31ac470`'s message (banner 1 export button while `store.list()` has 2): degraded store, injected `navigate` (so the workbench stays mounted — R6-7's no-reload return made that a normal state), create → banner export-button ids must equal `store.list()` ids. Fix mirrors `renameProject`: `await refresh()` before `openProject`, with the rationale in-code. Helper move complete: `projectPackageFileName` lives in `src/lib/project-package-file-name.ts` with its own test (plus a same-day-collision pin), the `StorageNotice` re-export is **removed** (not kept), and all import sites updated (`ProjectWorkbench`, `StudioRoutes`, test moved out of `StorageNotice.test.tsx`). Lint 7→6 confirmed by this review's scoped eslint — the StorageNotice react-refresh warning is gone.

### R7-5 recover-probe seam on the shared store (`89dcb33`) — ACCEPT
- Brief said "no red state — this is a seam" and the seam is right: `editor-project-store.ts` passes a module-level `scheduleRecover` (same shape as the default — `setInterval` + `unref`, **interval received from the store**, no constant copied) that side-registers the probe into the exported `projectStoreRecoveryProbe` handle (`run()` no-op-safe, `scheduled`, `intervalMs`), documented test-only; the dispose path releases the handle only if the registered probe matches — so recovery returns the handle to unregistered (pinned). `crash-disaster.integration.test.tsx` drops `captureRecoverProbe` and the `RECOVER_INTERVAL_MS` copy; **grep confirms the literal now exists only in `project-store.ts`**. The new unit test is the right kind of paranoid: it derives the default interval by *asking a throwaway instance* instead of copying the constant, pins that the degraded singleton installs a `setInterval` at exactly that default (production behavior unchanged), pins that `run()` drives the real recovery path with the write-back verified on a fresh store against the same IDB factory, and pins the stuck-in-memory case (repeated `run()`s, no spurious `recoverError`). `project-store.ts` untouched (frozen respected; `RecoverScheduler` was already exported).

### R7-6 honest snapshot occupancy bench (`f7a1ae5`) — ACCEPT
- The dead cell can no longer masquerade: the 12 MiB single-room cell is labeled `skip path (> 8 MiB room)` and *asserts* retained = 0 / skipped = 1; the new `aggregate at budget` cell (2 × 5.9 MiB, retained = 2) measures the actual worst case R6-1's budget raise created; the trim-cell guard is generalized — **every** cell asserts retained/trimmed/skipped counts and budget compliance. Header + commit message carry the full reference table with the worst-case aggregate (48.34 ms / 12,366,463 bytes) next to the documented 8 MiB miss history. **This review re-ran the bench**: aggregate 48.11 ms / 12,366,463 bytes, 8 MiB cell 31.92 ms, skip cell 0.06 ms / 73 bytes / retained 0 — the recorded numbers reproduce and all guards pass. Bonus finding from the live output: the flush warn's fixed legend prints "skipped rooms remain in memory…" even on the trim-only cell and "Trimmed rooms remain restorable…" even on the skip-only cell — see cross-check 3.

### R7-7 unmount tracked roots in two leaking test files (`38a632a`) — ACCEPT
- Both files now track `{ root, container }` per mount and `flushSync`-unmount + remove in `afterEach`; `local-workspace-entry.test.tsx`'s dead `roots` array (pushed, never drained) is replaced by a `mount()` helper feeding the tracked list. The commit message is the round's best artifact: a real red measurement (temporary probe counting still-mounted `__reactContainer$` roots: 1..5 growing in UniversityEmblem — `document.body.innerHTML = ""` only *detaches*, the roots stay mounted with observers armed; 1 mounted ProjectWorkbench root with async store effects), the fix, the metric (0 leaked roots, 10/10 green, typecheck 0), and the briefed fresh scan: **no file lacks unmount entirely, but 36 files unmount only inline at the end of each `it`** — an assertion throwing before the unmount line leaks the root for the rest of the run — enumerated by path with the highest-risk named (PosterCanvas.test.tsx 47 roots, MapLayer 17, CardsInspector 14, useCollaborationRoom async). Correctly not swept (cross-owned); assigned in the R8 briefing.

### R7-8 trim durability journey over real HTTP + files (`24bcd02`) — ACCEPT
- One new `it` in the exclusive file, no product edits. Every briefed leg holds against a real `dataDir`: snapshot built 320 KiB under the per-room cap so only accumulated history pushes the record over (persistence must *choose* trim over skip); five real HTTP transactions with `Prefer: return=minimal`; SIGTERM → envelope has `trimmedRoomIds: [id]`, the room retained with `operationHistory: []` at the edited version, `skippedRoomIds` absent, and the trim warn names the id while asserting the *skip outcome clause* ("room(s) from persistence") absent; reboot → "restored 1", no 未恢复 warn, room serves the byte-identical snapshot at the right version over HTTP with the old token; `operations?afterVersion=0` → 409 `VERSION_CONFLICT` "增量历史已被裁剪，请重新获取完整快照" with `currentVersion`, while an up-to-date client gets 200 + empty ops; a sharp honesty detail — the fresh process reports `persistence: { outcome: "persisted", at: null }` before its first flush (no inherited verdict from the dead process); resumed edits → next flush → `persistence.outcome: "trimmed"`, boolean `false`, `at > 0`, health `lastFlush.trimmedIds` carries the id, envelope re-asserted. **No product defect found on the trim path — confirmed by this review.** The one wording defect it had to dodge is the warn legend (cross-check 3), correctly filed rather than fixed cross-owned. File now at 399 lines — split before the next addition (assigned R8).

### R7-9 extract 334 lines from App into six lib seams (`70c8cb6`) — ACCEPT-WITH-NITS
- App.tsx 2322 → 1988 (−334, ≥300 as briefed; commit records it), six cohesive modules with tests: `canvas-edit-transactions` (all transaction factories — **ids, labels, and sources verified identical**, deterministic ids like `tx-text-delete-${id}` stayed deterministic, `createId` ones stayed `createId`), `editor-history` (labels + shortcut matcher, input/textarea/contentEditable exclusion preserved), `editor-navigation` (checked against the old branching including the subtle cases: layout issue with unfound target still switches stage/panel without touching selection; unresolvable delivery target still early-returns before any stage change), `editor-chrome` (render-settings load is behavior-identical and the font-face style-node reuse is preserved), `rendered-template` (the `[renderProject, style, template]` → `[renderProject]` dep narrowing is sound — both were derived from `renderProject`), `resource-library` (messages preserved verbatim, including the dedupe and empty-library copy). No cross-owned test file edited; `App.test.tsx` additions-only (+78/0) with real DOM pins: topbar history labels name the pending step, keyboard undo/redo drives the live canvas, focused input fields keep the browser's own undo, template application is one undoable step. Typecheck re-verified 0 by this review.
- Nits: (1) the extraction commit `ff5ad03` carries "WIP: module tests to follow" in a *merged* message — the tests did land in `ac1b1e1` before merge, but WIP markers don't belong in integration history. (2) App.tsx at 1988 (1989 after R7-3b) is still ~5× the repo's 400-line rule — playbook proven twice now, keep going (R8).

## Cross-task checks

### 1. The tri-state honesty chain — reaches the DOM, every joint pinned, compat preserved at every layer
- Store records skip/trim/failure (R7-1) → HTTP publishes `persistence: { outcome, at }` beside the compat boolean, single-read consistent (R7-2) → client parses three-literal-strict, decorative `at` (R7-3) → hook holds display-only kind with reset/keep rules (R7-3) → App threads one prop (R7-3b) → menu picks the copy per kind with the death copy as the boolean-only fallback (R7-3). Proven end to end by R7-3b's App test and, over real files and processes, by R7-8. The R6 headline — "the most common warning a member sees overstates the loss" — is closed: a trimmed room's members now read that the room survives restart and what they actually lose.
- Answering R6's headline question directly: yes, boolean-only servers still show the death copy — pinned at the menu (`persistenceNoteKind ?? "skipped"` with a DOM test), at the hook (kind stays `null`), and implicitly at App (absent prop = old behavior).

### 2. Failure visibility — fixed at the store, stale-good one level up (the R8 headline)
- `lastFailure` appears on `lastPersistOutcome()` (R7-1, pinned) and on `/api/health.rooms.lastFlush` (R7-2, pinned over a real success→failure cycle). `at: 0` maps to `persistence.at: null` on room responses (pinned).
- The gap: `roomPersistence()` (index.ts:528–538) reads only `skippedIds`/`trimmedIds`/`at` and **ignores `lastFailure`** — so create/join/snapshot answer `persisted`/`trimmed` with the last success timestamp throughout a failure streak, and the client/hook/menu chain (which only consumes those responses) can't know the disk is failing. Health and one deduped console.error are the only streak-visible surfaces. This is the same shape as R6-1's nit 1, one level up. Additive fix is cheap (`persistence.lastFailureAt` from the same single read); filed as R8-2/R8-3.

### 3. The trim journey's one wording defect — confirmed and reproduced live
- `warnDegradedRooms` (collaboration.ts:876–894) builds the outcome clauses correctly per group, but the explanatory legend is a **fixed two-clause suffix**: a trim-only flush still prints "skipped rooms remain in memory but will not be restored after restart", and a skip-only flush still prints "Trimmed rooms remain restorable…". This review reproduced it live in the R7-6 bench run: the skip-path cell's warn carries the trim legend and the trim cell's warn carries the skip legend. R7-8's journey had to assert around it (its in-test comment documents exactly this). An operator grepping logs after a trim-only shutdown reads a sentence about skipped rooms that never existed. Filed as R8-1.

### 4. Round-6 leftover scorecard — all assigned items closed, one unassigned nit still open
- R6-1 nit 1 (stale-good `lastPersistOutcome`) → **closed** by R7-1, verified. R6-1 nit 2 (bench dead cell / missing aggregate) → **closed** by R7-6, verified by re-run. R6-3 item 1 (`createProject` no refresh) → **closed** by R7-4, red numbers in the commit. R6-3 item 2 (no `scheduleRecover` seam) → **closed** by R7-5, grep-verified. R6-4 nit 1 (lint 7 warnings) → **closed** by R7-4, scoped eslint shows 6. R6-6 nit (trimmed death copy) → **closed** by R7-3 + R7-3b. R6-9 note (trim path unproven over HTTP + files) → **closed** by R7-8.
- R6-4 nit 2 — workbench banner export failures land in the generic error section, not the banner's alert row — was noted-only in R6, never assigned, and is **confirmed still open**: `exportProject` failures go through `reportFailure` → `setError` → the detached `workbench-error` section (ProjectWorkbench.tsx:207–216, 266), while the editor route surfaces the same failure inside the banner via `StorageNoticeActionError`. Assigned R8-4.
- R7-10 (PR #14 description + ledger polish) had not landed at review time (`PROGRESS.md` marks it pending; `/tmp/opt-merge` has moved past `362c318` with what appears to be ledger work). Outside this review's scope; its content requirements roll into R8-10 with Round-7 numbers.

### 5. Test-hygiene and size debt, precisely located
- 36 test files unmount only inline in `it` (R7-7's scan, enumerated in `b32ddc7`'s message) — split across R8-5/R8-6/R8-3.
- The 6 lint warnings, located by this review's scoped eslint: `DataWorkspace.tsx` exhaustive-deps (product frozen — leave), and five react-refresh: `StudioMuiProvider.tsx` (`studioTheme`, self-consumed only), `global-data/GlobalDataNavigation.tsx` (`globalDataViewLabel`, one importer: `GlobalDataScreen.tsx`), `src/lib/app-initialization.tsx` ×3 (three functions beside the `WorkbenchBackButton` component; all importers in `App.tsx`). Assigned R8-9 (two component files) and R8-7 (app-initialization, since it owns App.tsx).
- `server/durability.integration.test.ts` at 399 lines against the 400-line rule — split before the next journey (R8-8).

## Gates

- Typecheck: run by this review, both projects, exit 0. Full suite + lint: orchestrator numbers (189 files / 1757 / 2 skipped; 0 errors / 6 warnings) — corroborated by green CI on `362c318` (run 32783454636) checked directly, and the warning count independently reproduced by this review's scoped eslint. Bench: re-run, numbers reproduce.
- Red-first discipline: held with separate red commits on R7-1 and R7-3; held-with-documented-red on R7-4 and R7-7 (observed red state recorded in the message); R7-5/R7-6/R7-8/R7-9 are seam/doc/proof/refactor tasks with no red demanded; **R7-2 is the one miss** (no separate red commit, no observed-red record) — process nit, substance fully pinned.
- **Merge blocker: none.**

## Leftovers going into Round 8 (all evidenced above)

1. Member-facing surfaces stale-good during a persist-failure streak (`roomPersistence()` ignores `lastFailure`) — cross-check 2.
2. Flush warn's fixed legend asserts facts about outcome groups that didn't occur — cross-check 3, reproduced live.
3. 36 files with inline-only unmounts — R7-7's scan.
4. Workbench banner export failures not in the banner's alert row — R6-4 nit 2, still open.
5. `durability.integration.test.ts` at 399 lines; App.tsx at 1989; five react-refresh warnings in three files.
6. Durable capacity ceiling 12 MiB aggregate (by design; unchanged, on the PR description's known-misses list).
