[Model: claude-fable-5]
# Round 8 Briefing (from the Round-7 SOTA review)

## Direction

Round 7 finished the trim/skip honesty chain and proved it over real HTTP + files. Round 8 closes the two defects that round exposed one level up, then pays down precisely-located debt — we are at round 8 of 20, and every task below carries evidence from the Round-7 review (`.agent_workspace/round-7-review.md`). The headline is a two-step chain (R8-2 → R8-3): **`roomPersistence()` ignores R7-1's `lastFailure`, so during a disk-failure streak create/join/snapshot keep answering `persisted`/`trimmed` with the last success timestamp** — the member-facing chain R7 built can't see that the disk is failing; only `/api/health` and one deduped console.error can. Second: the flush warn's fixed two-clause legend asserts facts about outcome groups that didn't occur (reproduced live in this review's bench run — a trim-only flush prints "skipped rooms remain in memory…"). Third theme: the R7-7 scan's 36 inline-unmount-only test files, the still-open R6-4 export-failure-row nit, the durability suite at 399 lines, App at 1989 lines, and the five located react-refresh warnings.

Quality bar unchanged: **no metric, no merge; red repro first — as a separate red commit or with the observed red state recorded verbatim in the commit message (R7-2 was the round's one miss; don't repeat it); targeted vitest only** (`npx vitest run <file>`); `npm run typecheck` must stay exit 0 after every task; CI must be green on the merged tree. One task = one exclusive writable file set; the sets below are disjoint — if your fix forces an edit in a cross-owned file, stop and file it unless this table pre-authorizes the collision.

## Tasks (exactly 10, disjoint writable sets)

| ID | Writable files | Defect (evidence) | Required red repro → fix → metric |
|---|---|---|---|
| R8-1 | `server/collaboration.ts`, `server/collaboration.test.ts` | `warnDegradedRooms` (collaboration.ts:876–894) builds per-group outcome clauses correctly but always appends the fixed legend "Trimmed rooms remain restorable but stale clients must re-snapshot; skipped rooms remain in memory but will not be restored after restart" — a trim-only flush warns about skipped rooms that don't exist and vice versa (reproduced live in the R7 review's bench run; R7-8's journey had to assert around it, see its in-test comment at durability.integration.test.ts:332). An operator grepping logs after a trim-only shutdown reads a false sentence about skips. | Red: trim-only flush → warn line contains "skipped rooms remain in memory"; skip-only flush → contains "Trimmed rooms remain restorable". Fix: make each legend clause conditional on its group being non-empty (both present → both clauses, same order). Do NOT touch the trim/eviction algorithm, the outcome clauses, or `lastPersistOutcome`. Metric: red flips both directions; both-groups case pinned; existing warn pins ("trimmed operation history", ids) green unmodified; R7-8's cross-owned asserts (`toContain(roomId)`, `not.toContain("room(s) from persistence")`) stay green untouched. |
| R8-2 | `server/index.ts`, `server/index.test.ts` | `roomPersistence()` (index.ts:528–538) computes the tri-state from `skippedIds`/`trimmedIds`/`at` only and ignores R7-1's `lastFailure` — during a persist-failure streak, create/join/snapshot report the stale-good verdict with the old timestamp; the streak is visible only on `/api/health` and the deduped console.error. Same shape as R6-1's nit 1, one level up. | Red: inject a store outcome with `lastFailure` → create/join/snapshot responses today carry no trace of it (pin the absence, then flip). Fix (additive, from the same single `lastPersistOutcome()` read R7-2 unified): `persistence` gains `lastFailureAt: number \| null` (null when no active streak); keep `outcome`/`at` semantics and the compat boolean untouched. R7-3's client parse reads only `outcome`/`at`, so the extra key is compat-safe by construction — verify against `persistenceOutcomeOf`, don't modify it (cross-owned). No SSE changes. Metric: red flips on all three endpoints; success-clears pinned over a real flush cycle (failure → `lastFailureAt` set, success → null); stripped-store and boolean-only compat pins green unmodified; existing R7-2 tests green unmodified. |
| R8-3 | `src/lib/collaboration-client.ts`, `src/lib/collaboration-client.test.ts`, `src/lib/useCollaborationRoom.ts`, `src/lib/useCollaborationRoom.test.tsx`, `src/components/ProjectMenu.tsx`, `src/components/ProjectMenu.collab.test.tsx` | The member-facing chain ends at `outcome`/`at`: even after R8-2, no client surface tells a room's members the server currently cannot write to disk — the loudest failure mode (every room dies if the process dies during the streak) is the only one with zero member-facing copy. | Red: hook + menu today render nothing for a response carrying `persistence.lastFailureAt`. Fix: client parses `lastFailureAt` with the `at` discipline (non-finite → null; unknown extras still ignored); hook exposes display-only `roomPersistFailureAt: number \| null` (same reset-on-switch/leave and keep-on-missing rules as `roomPersistenceKind`; do NOT touch heal/terminal/offline); menu renders an additional honest line when set, e.g. 服务器暂时无法写入磁盘，此期间的改动在服务器重启后可能丢失，请及时导出备份 — distinct `data-` attribute, same non-dismissible style, precedence terminal > offline > persistence-kind > failure (a dead room's death copy still wins). Sequenced after R8-2. While here (this task owns the file): give `useCollaborationRoom.test.tsx` the R7-7 `afterEach` tracked-unmount net (it's on the 36-file scan, async transport + timers = highest risk). Metric: red flips; per-layer pins incl. old-server (no field) compat; existing R7-3 kind tests green unmodified; file's roots unmounted in `afterEach`. |
| R8-4 | `src/components/ProjectWorkbench.tsx`, `src/components/ProjectWorkbench.test.tsx` | R6-4 nit 2, confirmed still open: banner export failures go `exportProject` → `reportFailure` → `setError` → the generic `workbench-error` section (ProjectWorkbench.tsx:207–216, 266) — detached from the banner the user just clicked in — while the editor route surfaces the same failure inside the banner via `StorageNoticeActionError` (StudioRoutes). The degraded banner is the one place a memory-mode user is looking. | Red: degraded store, banner export button with a throwing download → banner contains no `role="alert"` row; failure text renders only in the detached section. Fix: track banner-export failures separately and render `StorageNoticeActionError` inside the notice (mirror StudioRoutes' pattern; `StorageNotice` already exports the row — consume it, don't modify it). Grid-menu export failures may keep the generic section. Metric: red flips; success path unchanged (one download per gesture re-pinned); R7-4's created-project listing test green unmodified. |
| R8-5 | Test-only, 21 files: `src/components/canvas/*.test.tsx` (13: DecorationLayer, MapDataLayer, MapLayer, PosterCanvas.card-sizing, PosterCanvas.performance, PosterCanvas.reference-styles, PosterCanvas.test, ProvinceTextureOverlap, ProvinceTexturePositioning, RegionalAssetLayer, ResizeHandles, TextLayer, useCardLayoutWorker), `src/components/inspector/*.test.tsx` (7), `src/components/workspaces/ReferenceCardStyleWorkspace.test.tsx` | R7-7's scan (commit `b32ddc7`): these files unmount only inline at the end of each `it` — an assertion throwing before the unmount line leaks the root for the rest of the run, the same flake class CI run 32777897030 exposed, weaker trigger. PosterCanvas.test.tsx alone mounts 47 roots; MapLayer 17; CardsInspector 14. | Deliverable (the `b7ed418`/R7-7 pattern): per file, track every `{ root, container }` and `flushSync`-unmount + remove in `afterEach`; keep inline unmounts only where a case remounts mid-test. Use R7-7's probe (count still-mounted `__reactContainer$` containers in `afterEach`) as the red measurement and record before/after counts in the commit message. Metric: probe reports 0 leaked roots per file; all 21 files green in isolation; no unhandled teardown errors. |
| R8-6 | Test-only, 14 files: `src/components/AgentAssistant.landing.test.tsx`, `AgentAssistant.test.tsx`, `AgentAssistant.transport.test.tsx`, `AssetPanel.appearance.test.tsx`, `AssetPanel.live-preview.test.tsx`, `AssetPanel.test.tsx`, `DeferredInput.test.tsx`, `FontEditor.test.tsx`, `ProvinceTextureSizeControls.test.tsx`, `RangeNumberControl.test.tsx`, `StudioMuiProvider.test.tsx`, `StudioUi.controls.test.tsx`, `StudioUi.test.tsx`, `TypographyPanel.test.tsx` | The remainder of R7-7's 36-file scan (36 = 21 in R8-5 + these 14 + `useCollaborationRoom.test.tsx` folded into R8-3). AgentAssistant mounts async transport/timers — the shape most likely to race jsdom teardown. | Same deliverable, red measurement, and metric as R8-5, applied to these 14 files. Disjoint from R8-5 by construction; do not touch `useCollaborationRoom.test.tsx` (R8-3 owns it). |
| R8-7 | `src/App.tsx`, `src/App.test.tsx` (additions only), `src/lib/app-initialization.tsx` (→ split/rename), `src/lib/app-initialization.test.ts`, NEW `src/lib/*` / `src/components/WorkbenchBackButton.tsx` modules (+ tests) | App.tsx is 1989 lines vs the 400-line rule; the R6-5/R7-9 playbook is proven twice (−329, −334, suites green). Remaining cohesive clusters: `applyRestoredWorkspace` + save-on-leave lifecycle (`saveWorkspaceNow`, `handleBackToWorkbench`, `shouldSaveOnPageLeave` wiring), project record load/save refs, custom-template capture prompts, collaboration package plumbing (`currentCollaborationPackage`/`applySharedPackage`). Plus 3 of the 5 react-refresh warnings live in `src/lib/app-initialization.tsx` (component `WorkbenchBackButton` beside three functions; all importers in App.tsx — this task owns both sides, so the fix belongs here, not R8-9). | Deliverable: extract ≥250 further lines into pure-seam modules with their own tests; move `WorkbenchBackButton` into its own component file and make `app-initialization` component-free (rename to `.ts`); **behavior-preserving only** — transaction ids/labels/sources verbatim (R7-9 set the bar); no cross-owned test file may need edits (stop and file if one does); no merged WIP commit messages (R7-9 nit). Metric: line-count delta in the commit; App.tsx ≤ 1740; react-refresh warnings 6→3 from this task alone (verify with a scoped eslint on the touched files); all existing App tests pass unmodified; typecheck 0. |
| R8-8 | `server/durability.integration.test.ts` (split), NEW `server/durability-helpers.ts` (test helper) + NEW `server/durability-flush-failure.integration.test.ts` (names indicative) | Two evidenced items: (a) the file is at 399 lines against the 400-line rule with two journeys already in it — the next journey cannot be added without a split; (b) R7-1's failure record has no proof over real files — `lastFailure` is only pinned with injected throwing persists (index.test.ts), never with a genuinely unwritable disk, and no journey covers the R4-5 shutdown-flush-error containment together with the next boot restoring the last good envelope. | Deliverable (proof task, test-only): split the existing file into shared helpers + journey files, each ≤ 400 lines, zero assertion changes in the move (verify by diff review); add the flush-failure journey: boot with real `dataDir`, one clean flush; make the snapshot path unwritable (chmod the dir read-only or replace the file with a directory — document the chosen mechanism and restore it in `finally`); edit a room over HTTP; explicit flush rejects → `/api/health.rooms.lastFlush.lastFailure` carries the real errno message with the old success `at`; SIGTERM completes without crash (R4-5 containment); reboot → last good envelope restores, no quarantine sidecar. Any product defect found is filed for R9 — do not fix cross-owned files. Metric: all journeys green on the merged tree; every durability file ≤ 400 lines. |
| R8-9 | `src/components/StudioMuiProvider.tsx`, `src/components/global-data/GlobalDataNavigation.tsx`, `src/components/GlobalDataScreen.tsx` (import lines only), NEW `src/lib/*` module(s) (+ tests) | The remaining 2 of the 5 react-refresh warnings (located by the R7 review's scoped eslint): `studioTheme` in `StudioMuiProvider.tsx` (self-consumed only) and `globalDataViewLabel` in `GlobalDataNavigation.tsx` (one importer: `GlobalDataScreen.tsx`). Same defect class R7-4 fixed for StorageNotice — non-component exports in component files break Fast Refresh. | Red: scoped eslint on the two files reports the two warnings. Fix (the R7-4 pattern): move each export to a lib module with a test (theme construction pinned by a token or two; label fallback pinned); update the import sites; no re-exports left behind. `GlobalDataScreen.tsx` edits limited to the import statement. Metric: scoped eslint on touched files → 0 react-refresh warnings (repo total 6→4 from this task; →1 combined with R8-7, leaving only frozen DataWorkspace); behavior pins green; typecheck 0. |
| R8-10 | `.agent_workspace/PROGRESS.md`, PR #14 description (via PR tooling) | 交付纪律: R7-10 had not landed at review time — PR [#14](https://github.com/Xhhemoing/cengfan-map-studio/pull/14)'s description still lacks the Round-6 *and* Round-7 contract changes and rollback map. | Deliverable (docs, no code): description gains (a) Round-7 evidence (189 files / 1757 passed / 2 skipped; lint 0 err / 6 warn; typecheck 0; CI green on `362c318` run 32783454636; review 8 ACCEPT + 2 NITS, no blocker), (b) behavior changes + rollbacks: persist failure record (revert `1664494`), tri-state HTTP surface (revert `b959478`), skip/trim member copy (revert `8d5f046` + `a306069`), workbench refresh + helper move (revert `533f4a1`), recover seam (revert `89dcb33`), App extraction (revert `70c8cb6`), plus the Round-6 items listed in round-6-briefing R7-10, (c) known misses stated plainly: member-facing surfaces stale-good under persist-failure streak until R8-2/3; warn legend defect until R8-1; durable capacity ceiling 12 MiB. PROGRESS.md Round-8 section mirrors it. |

## Sequencing

R8-2 → R8-3 (failure-visibility chain; R8-3 consumes R8-2's landed field). R8-1, R8-4, R8-5, R8-6, R8-7, R8-8, R8-9 are order-free and can start immediately (R8-8's split should land before any other task considers touching durability files — none do this round). R8-10 last.

## Saturated — leave alone this round

`server/collaboration.ts` snapshot/trim/eviction geometry beyond R8-1's warn text (R6-1 pinned, R7-8 proven end to end), `src/lib/editor-project-store.ts` + `src/lib/project-store.ts` (R7-5 completed the seam; only new evidence reopens), `scripts/perf-room-snapshot-bench.ts` (R7-6 honest, re-run verified; next bench work needs a new workload, not a new cell), `src/components/AppErrorBoundary.tsx`, `src/components/StorageNotice.tsx` (R8-4 consumes its exports, doesn't edit it), `src/components/crash-disaster.integration.test.tsx` (de-stubbed, green), `.github/workflows/ci.yml`, `useCollaborationRoom` heal/terminal/offline core (R8-3 touches display state + parse only).

## Frozen (unchanged; campaign runs to round 20)

`server/ai/*`; `src/lib/collaboration-operations.ts` (consume only); layout stack internals (`card-layout.ts`, layout worker, solver) unless a new evidenced defect is found; SSE wire format (drain still must not emit `closed`; R8-2/R8-3 use HTTP response fields only); project-package format; project-document rejection identity; DataWorkspace product code (its exhaustive-deps warning stays — a dependency-array change is a behavior change, not a lint fix); payment/套餐/模板手续费 boundaries per `docs/开源与收费边界.md` — no payment or billing code in this repo.
