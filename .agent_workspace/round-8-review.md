[Model: claude-fable-5]
# Round 8 SOTA Review

Reviewed tree: worktree `/tmp/wt-r8-fable`, branch `cursor/r8-fable-review-187a` cut from `origin/agent/opt-continuous` @ `553d5c1` (all of R8-1..R8-9 plus R8-3b and the closeout ledger). Round-8 scope (against R7 close `362c318`): ten merges, every one read in full against its first parent with per-merge file-ownership checks. File discipline is **exact on all ten** — no merge touches a file outside its briefed exclusive set (R8-3b shares `App.tsx`/`App.test.tsx` with R8-7 and was orchestrator-sequenced after it, the same handled-collision shape as R7-3b; R8-6 edits `StudioMuiProvider.test.tsx` while R8-9 edits `StudioMuiProvider.tsx` — disjoint by file). No product code was modified by this review. `npm test` / `npm run lint` were NOT run (orchestrator serializes; recorded: **197 files, 1846 passed / 2 skipped, 89.3s; lint 0 errors / 1 warning**). This review DID run first-hand: `./node_modules/.bin/tsc -p tsconfig.app.json --noEmit` and `-p tsconfig.node.json --noEmit` — **both exit 0**; a scoped eslint over `src/components`, `src/lib`, `src/main.tsx`, `src/App.tsx` — **exactly 1 warning, the frozen `DataWorkspace.tsx` exhaustive-deps; react-refresh is at zero**. CI independently corroborates: the push of `553d5c1` is green (run 32787959232) and so is the PR run (32787965224). Refactor fidelity was machine-checked, not eyeballed: for R8-5, R8-6 and the R8-8 split, a `-U0` diff over each merge shows **zero `expect(` lines changed** (R8-8's two removals are the helper-internal `toBe(201)`s, re-found verbatim in `durability-helpers.ts`), and `it(`-declaration counts are identical before/after (160 and 72).

## Headline

**No merge blocker.** All ten rows accepted: 9 ACCEPT, 1 ACCEPT-WITH-NITS, 0 REJECT. Both Round-7 headline defects are genuinely closed. The stale-good member surface during a persist-failure streak is gone end to end: `roomPersistence()` now reads `lastFailure.at` from the same single `lastPersistOutcome()` read (R8-2), the key riding create/join/snapshot only while a streak is active; the client parses it with the `at` discipline where absence and non-finite are the same non-verdict (R8-3); the hook holds display-only `roomPersistFailureAt` with the crucial correct reading that a tri-state response *without* the key is the server saying the streak ended — otherwise the banner would never heal (R8-3); the menu renders the honest additive line 服务器暂时无法写入磁盘… under terminal/offline precedence (R8-3); and App threads the one prop with a real end-to-end DOM pin (R8-3b). The warn legend now only explains outcome groups that occurred, both directions flipped from red and the both-groups order pinned (R8-1). Round 8 also holds the strongest process line of the campaign so far: **red-first discipline held on every row that demanded it** — six separate red commits (R8-1/2/3/3b/4/9), two probe-measured before/after tables for the test sweeps (165→0 across 22 files; 66→0 worst-case across 14 files in three failure-injection modes), and R8-8's mechanism check (comment out the `mkdir` → the rejects assertion goes red) with the kernel's real errno recorded verbatim.

The largest finding of this review is — for the third round running — one seam past the round's chain: **the member most likely to be losing work still can't be told about the streak.** R8-2 put `lastFailureAt` on create/join/snapshot, but the transactions POST (`server/index.ts:892–920`) answers with the raw `roomStore.apply` result and no persistence fields — and an actively-editing member's only regular server contact *is* the transaction ack (the SSE wire is frozen and carries no persistence). A streak that starts mid-session reaches a member only when something forces a snapshot refetch (heal, version conflict). The person continuously writing changes that aren't durable is the one person whose responses never mention it. Additive fix from the same `roomPersistenceFields` seam; heads Round 9 together with the `.tmp` rename leak R8-8 filed — which this review confirmed in the product code and found a **second instance of**: the workspace PUT writer (`server/index.ts:773–776`) has the identical write-tmp-then-rename pattern with no cleanup, and `pruneRoomSnapshotSidecars` (`:1502–1506`) only ever prunes `.bad`.

## Per-task verdicts

### R8-1 conditional flush warn legend (`a72bf30`) — ACCEPT
- Red-first held (`a88fc5e` red with both observed failures verbatim → `e41aeaa` fix). The fix is exactly the briefed shape: each legend clause emitted only when its group is non-empty, both-present keeps the original order (trim first), outcome clauses / trim algorithm / `lastPersistOutcome` untouched — the product diff is 12 lines. Tests seed *real* trim and skip states (history over the cap vs oversized snapshot) rather than mocking outcomes, pin both single-group directions with negative matches, and pin both-groups ordering by index comparison. Test file additions-only. R7-8's cross-owned asserts (`toContain(roomId)`, `not.toContain("room(s) from persistence")`) verified intact at `durability.integration.test.ts:267–270` on the merged tree — including through the later R8-8 split.

### R8-2 persistence.lastFailureAt on room HTTP responses (`d9d77ef`) — ACCEPT
- Red-first held (`06edd8d`, three observed failures quoted). `roomPersistence()` reads `lastFailure?.at` from the same single `lastPersistOutcome()` read R7-2 unified; non-finite/`0` → null; `outcome`/`at`/boolean semantics untouched (boolean explicitly pinned `true` during a streak). All three endpoints pinned with exact-shape `toEqual` (create, snapshot GET, join over real HTTP); success-clears pinned over a real flush cycle (success → key gone, failure → key present with `lastFailureAt >= at`, next success → gone). No SSE changes; health untouched.
- Two documented deviations, both judged correct: (1) the brief said `lastFailureAt: number | null` — the implementation **omits the key when there is no streak**, with the reason in-code and in tests (a `null` would have changed the response shape under every existing exact-equality pin; readers treat absent and non-finite identically, verified against R8-3's parse). Strictly better than the brief. (2) The brief demanded "existing R7-2 tests green unmodified", but R7-2's `at: 0` test *itself injects `lastFailure`* and asserts exact shape — under any correct fix that assertion must change. The red commit names this precisely and uses the flip as evidence ("它的精确断言正是当前缺失的证据"). That is a brief-authoring collision, not a task miss; the change strengthens the pin (adds `lastFailureAt: 1_700_000_000_900`).

### R8-3 persist-failure member copy across client, hook, menu (`4f1b13c`) — ACCEPT
- Red-first held (`4bd7dcb` red → `d1625fa` fix). Client: `lastFailureAt` parsed inside `persistenceOutcomeOf` with the `at` discipline; string/`null`/boolean/absent all yield key-absent with exact-shape + `not.toHaveProperty` pins; unknown extras still dropped; all three response shapes covered. Hook: display-only `roomPersistFailureAt`; the update rule is the sharpest call of the round — **a tri-state response without the key clears the state** (the server omits the key exactly when the streak ended; keeping stale state would leave the banner up forever after the disk heals), while a boolean-only response keeps it (that server said nothing); both distinctions pinned, plus reset-on-leave, next-room isolation, and negative asserts on offline/terminal/status. Also delivered the briefed R7-7 `afterEach` tracked-unmount net for `useCollaborationRoom.test.tsx` with idempotent `unmountRoot` and the unmount-before-unstub ordering documented. Menu: copy verbatim from the brief; distinct `data-collaboration-persist-failure` attribute; `role="status"`, no dismiss control (pinned); terminal (closed *and* expired) and offline suppress it (pinned); disconnected join form excluded (pinned); absent prop can't announce anything (pinned).
- One interpretive call, endorsed: the brief's "precedence … persistence-kind > failure" was implemented as **display order, not suppression** — a trimmed room in a streak shows both facts (pinned in "states both facts…"). The two notes describe independent truths (this room is too big; the whole server can't write right now); suppressing the failure line behind capacity copy would hide the graver fact. The in-code comment argues exactly this.

### R8-3b App wires roomPersistFailureAt (`4f70e66`) — ACCEPT
- The R7-3b shape repeated: one product line threading `collaboration.roomPersistFailureAt` into `ProjectMenu`, plus an end-to-end App test — a real create response carrying `persistence: { outcome: "persisted", at, lastFailureAt }` renders the failure note in the DOM with negative pins on terminal/offline. Red-first held as separate commits (`0685e66` → `a68a230`). `App.test.tsx` additions-only (+42/0). Orchestrator-sequenced after R8-7's App rewrite; no live collision.

### R8-4 banner export failures inside the storage notice (`8fefafc`) — ACCEPT
- Red-first held (`7496c57`, observed red quoted: 2 failed / 29 passed, with the third new case — grid-menu failures stay in the generic section — green *before* the fix, locking the fix's blast radius). Fix splits the paths exactly as briefed: notice exports go through `exportFromNotice` → dedicated `noticeExportError` → `StorageNoticeActionError` rendered inside the notice (consumed, not modified — `StorageNotice.tsx` untouched); card-menu exports keep `reportFailure` → generic section; the shared download is one helper. Success clears the error and the one-download-per-gesture pin re-verified (`files).toEqual([...])` after retry). The empty-list guard keeps the error row renderable even with zero projects. R6-4 nit 2 — open since Round 6 — is closed.

### R8-5 tracked-unmount net in 21 canvas/inspector/workspace suites (`2320e1e`) — ACCEPT
- Four sweep commits plus a dedicated metric commit (`5de889e`) that supersedes the earlier weaker probe with a full before/after table under a hostile-mode probe (first `flushSync` after mount throws — exactly the defect condition): **165 → 0 leaked roots** across the 21 owned files (PosterCanvas 47→0, MapLayer 17→0, CardsInspector 14→0), with `AssetInspector` as a 0→0 control proving the probe measures the net rather than run noise. Mount boilerplate replaced by per-file `trackedRoot()` helpers draining in `afterEach`; inline unmounts kept only where cases remount mid-test. Fidelity machine-verified by this review: zero `expect(` lines changed in the entire merge; `it` count 160 before and after; 23 files / 169 tests green in isolation per the commit; typecheck re-verified 0 by this review.

### R8-6 tracked-unmount net in 14 component suites (`a27c5db`) — ACCEPT
- The rest of the 36-file scan. The single commit (`32103f3`) carries the round's best red measurement: a three-mode injection probe (chai assert throw / jest-style matcher throw / `vi.waitFor` rejection — the realistic CI timeout) with a per-file before→after table, **66 leaked roots worst-case → 0 in all three modes**, and the AgentAssistant files' unmount ordered *before* `useRealTimers`/`unstubAllGlobals` so a still-mounted assistant tears down against the transport and timers it ran with (rationale in-code). Inline unmounts that are themselves assertions (debounced-persistence-after-unmount, cancel-on-unmount, hydrate-after-remount pairs) correctly kept. Zero `expect(` lines changed; `it` count 72 before/after. Test-only.

### R8-7 extract 261 lines from App into four seams + WorkbenchBackButton (`56c2d7b`) — ACCEPT-WITH-NITS
- App.tsx **1989 → 1728** (≤ 1740 as briefed; 1729 at HEAD after R8-3b's one line). Five extraction commits + one App-level pin commit, no WIP messages (R7-9's nit not repeated). Fidelity spot-checked line-by-line by this review: `patchScene`/`freezeCardPositionsForMapChange`/`refreshDisplayFramePositions` (confirm copy verbatim), `tx-decoration`/`tx-landmark` ids and 添加装饰/添加地标 labels, drag dedupe in `moveAsset`, `collaborationPackage`'s history strip (`past: [], future: []`), `mergeSharedProject`'s `version + 1`, session-selection precedence (province first), and 已更新素材/已上传字体 copy — all identical to the old inline code; the four-times-duplicated font upload is now one action. `WorkbenchBackButton` moved verbatim to its own file with behavior tests; `app-initialization` is component-free `.ts` (imports extension-less, so no importer edits needed). Module tests are substantial (388/235/159/140 lines) and pin the briefed subtleties: hand-placed card positions never overwritten, same-point drags stay out of the undo stack, decline-refresh does nothing, data stage as side entrance remembering its origin, elements overview action stays put. `App.test.tsx` additions-only (+39/0) with three live-editor pins. react-refresh: the 3 `app-initialization` warnings gone (this review's scoped eslint shows zero react-refresh repo-wide together with R8-9).
- Nits: (1) `db4abae` added a new export `loadBrowserState` to `app-initialization.ts` (SSR guard + `loadBrowserValue`, replacing three inline ternaries) but `app-initialization.test.ts` — in this task's writable set — was not extended: the `typeof window === "undefined"` branch is unpinned anywhere (jsdom always has `window`). Three trivial lines, but the brief's bar was modules-with-tests. Assigned to R9's App task. (2) App at 1729 is still ~4.3× the 400-line rule — playbook now proven three times, keep going (R9).

### R8-8 durability split + real-disk flush-failure journey (`30aef52`) — ACCEPT
- Split (`adf948c`): eight fixtures moved verbatim to `durability-helpers.ts`; the machine check agrees with the commit's own claim — the only `expect(` lines removed are the two helper-internal `toBe(201)`s, present verbatim in the new file; journeys 399 → 333 lines, helpers 79, all ≤ 400.
- Journey (`edab090`): every briefed leg over a real `dataDir`, with an unwritability mechanism *better* than the brief's two suggestions and argued in-test: a directory squatting on the writer's `<file>.<pid>.tmp` path gives a kernel-real `EISDIR` for any uid (chmod is a no-op under root — common in containers), leaves the last good envelope untouched (so restore is product behavior, not test write-back), and blocks only the room-snapshot path. Coupling to the writer's tmp naming is called out as fail-loud. Legs verified: mid-incident HTTP edit 200; explicit flush rejects with `EISDIR`; health carries `lastFailure` with the real errno message *including the path* beside the old success `at`; good bytes untouched on disk; SIGTERM completes with the `[shutdown] 状态落盘失败` record and no `.bad` (R4-5 containment); reboot restores the last good envelope at the created version with the incident-era edit correctly absent; the new process's next flush succeeds and drops `lastFailure`. Red mechanism-check (comment out the `mkdir` → red) and the verbatim errno recorded in the message.
- Answering the round's question directly: **no product defect found on the flush-failure path beyond the filed one** — the `.<pid>.tmp` rename leak (`index.ts:1568–1575`, `pruneRoomSnapshotSidecars` only prunes `.bad`), correctly filed rather than fixed cross-owned. This review confirmed the claim in the product code and adds a second instance: the workspace PUT writer (`index.ts:773–776`) has the identical pattern. Both to R9. One scope note, not a miss: the journey pins the streak on `/api/health` only — `persistence.lastFailureAt` on room responses over a genuinely failing disk is pinned in `index.test.ts` via an injected throwing persist, not in a journey (the brief predates R8-2's landing); folded into R9's journey task.

### R8-9 studioTheme / globalDataViewLabel → src/lib (`4d2a887`) — ACCEPT
- Red-first held at both levels (`1c3099b`: the two scoped-eslint warnings quoted verbatim *and* the two behavior tests failing on unresolved imports). The R7-4 pattern executed exactly: both exports moved verbatim (theme palette byte-identical; navigation table + label fallback), **no re-exports left behind**, `GlobalDataScreen.tsx` edits limited to import lines, `GlobalDataNavigation` consumes the shared table. Tests pin palette tokens on the studio CSS custom properties, `cssVariables` enabled, navigation order, every label, and the 数据总览 fallback. This review's scoped eslint confirms the metric: react-refresh warnings zero repo-wide (6 → 1 total, combined with R8-7; only frozen DataWorkspace remains). Observation only: `global-data-views.ts` imports the `GlobalDataView` type from a component file — type-only, harmless to Fast Refresh, noted for symmetry.

## Cross-task checks

### 1. The failure-visibility chain — closed to the DOM, one surface still dark (the R9 headline)
- Store records the streak (R7-1) → create/join/snapshot carry `lastFailureAt` while active, key-omitted otherwise (R8-2) → client parses with absence-as-no-streak (R8-3) → hook holds display state with clear-on-explicit-verdict / keep-on-silent-boolean (R8-3) → App threads one prop (R8-3b) → menu shows the additive honest line under terminal/offline precedence (R8-3). Every joint pinned; end-to-end proven by R8-3b's DOM test; the server end proven over a real dying disk by R8-8 (health level).
- The gap: the transactions POST (`index.ts:892–920`) returns `roomStore.apply` raw — no `roomPersistenceFields`. An actively-editing member's steady-state contact with the server is exactly this ack (SSE is frozen and silent on persistence), so a streak beginning mid-session is invisible to the member with the most unsaved work until a heal or conflict forces a refetch. Same defect class as R6-1 nit 1 and R7's cross-check 2, one surface further out. Additive fix from the existing seam; client side lands in `collaboration-client` (ack currently returned unparsed), `collaboration-send` (ack consumer, not frozen), and the hook (`notePersistence` already has the rules). Filed as R9-2/R9-3.

### 2. The legend fix and the trim journey stay consistent through the split
- R8-1's conditional legend means a trim-only flush no longer utters the skip sentence — R7-8's journey asserts (`trimWarning` contains the id, does not contain "room(s) from persistence") remain green and are now doubly true (outcome clause *and* legend absent). Verified intact at `durability.integration.test.ts:267–270` after R8-8's split moved the fixtures out. No durability assertion was weakened in the move (machine-checked).

### 3. Real-disk truth, and the two writers that can litter
- R8-8's journey retires the last purely-injected claim about `lastFailure`: the errno on health is the kernel's own, with the failing path in the message, sitting beside the previous success `at`. What it deliberately did not do is fix the leak it read out of the writer: on a rename failure, `<file>.<pid>.tmp` is orphaned forever (`pruneRoomSnapshotSidecars` matches only `.bad`). This review confirms both the room-snapshot writer (`index.ts:1568–1575`) and — new finding — the workspace writer (`index.ts:773–776`) share the pattern. A rename-failure red is constructible (directory squatting on the *final* path). R9-1.

### 4. Round-7 leftover scorecard — every assigned item closed
- Stale-good member surface → **closed** by R8-2/R8-3/R8-3b, verified layer by layer. Warn legend → **closed** by R8-1, both directions. 36 inline-unmount files → **closed**: 21 (R8-5) + 14 (R8-6) + 1 (`useCollaborationRoom.test.tsx`, R8-3) = 36; this review's fresh scan of every mounting test file found no file without an `afterEach` net (sampled patterns: tracked-roots arrays, cleanups arrays — all drain in `afterEach`). Banner export failures → **closed** by R8-4 (open since R6-4). Durability file size → **closed** (333/148/79, all ≤ 400). react-refresh five → **zero** (R8-7 + R8-9, re-measured by this review). R7-2 nit 2 (health `lastFlush.at: 0` raw pass-through, 1970 to a naive parser) — still open, third round on the books; folded into R9-2.
- R8-10 (PR #14 description + ledger) is by-design sequenced after this review and is not part of the reviewed scope; its content requirements roll into R9-10 with Round-8 numbers.

### 5. Hygiene debt has moved from unmount discipline to file size — precisely located
- Unmount discipline is done (scan clean; permanent regression guard proposed as R9-6 — the R8-5 probe showed setup-file `afterEach` hooks run outermost, which is exactly the mechanism a guard needs, and `vite.config.ts` has no `setupFiles` today).
- Over the 400-line rule and unowned by any current freeze: `src/App.test.tsx` **2978** (18 describes), `server/index.test.ts` **2463**, `server/collaboration.test.ts` **1532**, `src/lib/useCollaborationRoom.test.tsx` 1148, `src/components/canvas/PosterCanvas.test.tsx` 983, `src/lib/collaboration-client.test.ts` 894. Product: `src/App.tsx` 1729, `server/index.ts` **1637** (baseline was 1076 — it has grown a round-on-round persistence cluster that is cohesive and extractable), `PosterCanvas.tsx` 1588, `server/collaboration.ts` 1011, `useCollaborationRoom.ts` 825.

## Gates

- Typecheck: run by this review, both projects, exit 0. Scoped eslint: run by this review — 0 errors / 1 warning (frozen DataWorkspace exhaustive-deps), react-refresh count zero, independently confirming the orchestrator's lint line. Full suite: orchestrator numbers (197 files / 1846 / 2 skipped, 89.3s) — corroborated by green CI on the push of `553d5c1` (run 32787959232) and the PR run (32787965224), both checked directly.
- Red-first discipline: **held on all rows** — separate red commits on R8-1, R8-2, R8-3, R8-3b, R8-4, R8-9; probe-measured before/after tables for R8-5 (165→0, hostile mode, with a control file) and R8-6 (66→0 worst case, three injection modes); R8-8's red mechanism-check with the verbatim kernel errno; R8-7 is a behavior-preserving refactor with pins (no red demanded). The R7-2-style miss did not recur.
- **Merge blocker: none.**

## Leftovers going into Round 9 (all evidenced above)

1. Transactions ack carries no persistence fields — the actively-editing member is the last to learn about a streak (cross-check 1).
2. `.tmp` rename leak in **two** writers: room snapshot (`index.ts:1568–1575`, filed by R8-8) and workspace PUT (`index.ts:773–776`, found by this review); sidecar pruner only handles `.bad` (cross-check 3).
3. Health `lastFlush.at: 0` published raw (R7-2 nit 2, still open).
4. Room-response `lastFailureAt` unproven over a genuinely failing disk (injected-persist pin only; journey pins health only).
5. `loadBrowserState` SSR fallback branch unpinned (R8-7 nit 1).
6. File-size debt: App.tsx 1729; server/index.ts 1637; test files up to 2978 lines (cross-check 5). No permanent guard against leaked-root regressions.
7. Durable capacity ceiling 12 MiB aggregate (by design; unchanged, stays on the PR description's known-misses list).
