# 更新日志

本文件记录蹭饭地图工作室每个版本面向用户可见的变化。

## 记录规则（本节自包含，不依赖其他文档）

- 版本条目按「新增 / 修复 / 明确不做（附原因）/ 感谢」组织；周期性的意见处理另行记录「本期收到意见 N 条」，没有真实数据时不虚构数字。
- 感谢一律使用 GitHub ID 或本人自选昵称，不写真实姓名，不出现「学校全名 + 个人」的组合；转述意见时不得引用其中的真实学生名单内容。
- 「已改」与「感谢」条目尽量附 Issue / PR 链接留痕。
- 「明确不做」必须写原因——这不是冷漠，是为了让提意见的人下次还愿意提。
- 本项目按 **AGPL-3.0-only** 授权（见 [LICENSE](LICENSE)）。本仓库不包含也不会包含支付、套餐、付费解锁等收费功能，边界说明见 [docs/开源与收费边界.md](docs/开源与收费边界.md)。

## [Unreleased]

### 新增

- **数据框排布方式与两个「禁止遮挡」开关**：「版式」阶段右栏的排布方式改用中文名并新增「省份近」「分列整齐」两种算法（原有四周整齐 / 极角环绕 / 右侧单列 / 边缘网格保留，默认仍是四周整齐，老海报不会跳版）。原「允许卡片覆盖地图」换成「禁止遮挡地图」「禁止遮挡其他元素」两个默认勾选的开关：前者让卡片与连接线避开省份轮廓，后者让卡片避开嘉宾面板、文本框与画布上可见的装饰素材——装饰素材此前完全没有被自动排布考虑，卡片会直接压上去。两个开关同样作用于手动拖拽；拖动一张卡片时被挤开的邻居会一起让位，并且只占一步撤销。

- **内容阶段「整体模板与交换」**：「内容与排版」阶段右侧栏新增可折叠的「整体模板与交换」区块，在主编辑路径上即可切换内置模板、应用与保存「我的模板」，并直接导入 / 导出社区模板文件——此前社区模板交换只能绕道全局设置。导入超过 20 个上限时替换最旧的模板并明确提示。社区模板格式与校验规则不变：结构性排除学生数据，永远免费交换。
- **导出成功结果条**：「最终导出」阶段导出成功后显示结果条，标明实际写出的文件名（无法得知文件名时提示到浏览器下载目录查看），并提供「再次导出」按钮；PNG / SVG / 工程包导出均生效。此前导出成功没有任何界面反馈。
- **工程包导出文件名包含项目名**：编辑器与工作台导出工程包现均为「项目名-工程包-日期.json」，与 PNG / SVG 命名对齐；未命名项目回退「我的毕业去向图」。后缀保持 `.json`，历史 `cengfan-project-*.json` 与 `.cengfan` 文件继续可导入。文件名不含房间号、邀请凭证或任何学生字段。
- **帮助菜单直达更新日志并显示版本号**：帮助菜单「先自己看看」新增「更新日志」链接（指向 GitHub 上的本文件）；环境区显示当前版本号。版本号为只读常量，不把任何工程数据放进跳转 URL。

### 修复

- **空名单不再误判「名单数据健康」**：阶段概览卡片在名单为 0 人时不再显示「名单数据健康」，改为警告卡「还没有名单」，引导先导入或录入毕业去向名单。
- **导入结果读屏可感知**：「数据与素材」阶段成功与过程消息走常驻 `role="status"`；失败 / 阻断消息改走常驻 `role="alert"` 即时播报。两个区域都不随消息卸载，空态视觉隐藏但仍留在读屏树。
- **项目菜单标明协作入口**：顶栏「项目」菜单的无障碍名称改为「打开项目与协作菜单」。小屏下在线协作只能从该菜单进入，此前名称未提及协作，读屏与小屏用户难以发现。
- **导出 PNG 在任意导出进行中禁用**：顶栏「导出 PNG」在 SVG 或工程包导出进行中也会禁用并显示「导出中...」，避免与交付栏并发写出。
- **文档纠偏（不涉及代码行为）**：修正 `AGENTS.md`、`DEVELOPER.md` 与数据导入技能文档中残留的 `src/server` 路径（服务端实际位于仓库根目录 `server/`）；数据导入技能文档改为指向前端本地模板生成（`src/lib/binary-import.ts` 的 `createImportTemplateSheets()` 与 `DataWorkspace` 的下载按钮）；`function.md` 技术栈由 React 18 更正为 React 19；`function.md` 与 `DEPLOY-SERVER.md` 中已下线的 `/admin` 管理后台与 `GET /api/admin/visits` 访问统计描述改为「已移除」。

## [0.1.0] - 2026-08-24

首个记录版本。以下为本版本面向用户的功能快照。

### 新增

- **应用内「提意见 / 帮助」入口**：工作台头部与编辑器均提供帮助菜单，可跳转 GitHub Issue 模板（提意见）与用户指南。跳转链接只携带环境信息（操作系统 / 浏览器 / 运行方式），不携带名单、项目名、房间号或任何本地存储内容；菜单渲染与点击全程零网络上报。（纠偏：本版本的帮助菜单尚无更新日志链接与版本号显示；两者已在后续周期补齐，见上方 Unreleased。）
- **社区模板交换格式**（`cengfan-community-template` v1）：自定义模板可导出为独立的 `.cengfan-template.json` 文件，在另一台设备导入后进入「我的模板」。格式结构性排除学生数据——文件任意层级出现 `students` 键、或出现价格 / 套餐类字段，一律整体拒收；导出时嘉宾名单强制置空。社区模板永远免费交换。
- **协作本地昵称与角色文字标签**：协作弹层可设置本机昵称（仅存本机 localStorage，不超过 20 字符，不进入工程包与导出文件），成员列表每行显示「创建者 / 编辑者 / 仅查看」文字角色。服务器 API 与房间协议零改动；他人昵称暂不可见，属已知差距。
- **PNG / SVG 导出文件名包含项目名**：SVG 按「项目名.svg」、PNG 按「项目名-倍率x.png」（如 2 倍导出为 `项目名-2x.png`）命名，未命名项目回退「我的毕业去向图」；工程包文件名保持 `cengfan-project-日期.json`，不含项目名。所有导出文件名均不含房间号、邀请凭证或任何学生字段。
- **数据阶段恢复 XLSX 模板下载**：导入主路径重新提供「下载学生数据 XLSX 模板」按钮。模板仅含表头（学生姓名 / 录取院校 / 城市 / 去向类型）与虚构示例，本地生成、零网络。
- **工作台「载入示例项目」**：已有项目时也可一键新增一份示例项目，每次点击都是新建，绝不覆盖或删除既有项目；示例名单全部为虚构数据。
- **贡献者致谢规则**：新增 [docs/社区/贡献者致谢.md](docs/社区/贡献者致谢.md)——文档、模板、脱敏数据、答疑与代码同等致谢，社区模板支持可选署名。

### 明确不做（附原因）

- **世界地图 / 海外地图模板**：当前仅支持中国省级地图；海外去向通过导入时标记「去向类型 = 海外去向」，以无锚点卡片形式在中国地图版式中展示。
- **导出 PDF、小程序 / 移动 App、账号系统与云端保存**：不在当前能力范围内；SVG 导出后可自行转 PDF 或印刷。
- **导出计数等任何形式的遥测上报**：「名单默认不出浏览器」是本项目的信任基石，导出 / 点击统计一律不加。
- **VIP、会员、专属付费模板**：与 AGPL-3.0-only 及收费边界冲突，见 [docs/开源与收费边界.md](docs/开源与收费边界.md)。

### 感谢

- 暂无外部贡献者条目——规则先行、不放空表。第一位贡献者出现后，将按 [docs/社区/贡献者致谢.md](docs/社区/贡献者致谢.md) 的规则在此点名并附链接。
