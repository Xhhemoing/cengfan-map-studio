# 蹭饭图用户指南

> 目标：让班主任、班委、毕业生快速上手，3 分钟做出毕业去向地图。

---

## 一、3 分钟快速上手

### 场景：班主任要做毕业典礼去向墙

**步骤**：
1. 本机运行 `npm run dev`，打开 http://localhost:5173。首次进入工作台会自动放入「示例：2026届毕业去向」，点开即可对照 [README 样例图](README.md)。
2. 要用自己的名单：点击「新建项目」→ 输入班级名称
3. 进入项目后打开「数据与素材」→ 导入 Excel/CSV（或工作台「导入」`.cengfan` 工程包）
4. 选择学生名单（姓名、去向、省份）
5. 等待自动匹配省份 + 智能布局
6. 在画布上微调重叠卡片（可选）
7. 打开「最终导出」→ PNG / SVG / 工程包

**完成**：高清毕业地图已生成，可直接打印或做成展板。

---

## 二、功能详解

### 1. 导入学生名单
- 支持格式：Excel (.xlsx) / CSV
- 必填字段：姓名、去向、省份/城市
- 自动匹配：系统内置 3000+ 学校/城市映射
- 匹配失败时：手动选择省份（在「地图映射」里点「指定省份」后，屏幕阅读器会即时播报「已为 某某 指定省份」）
- 不提供图片 OCR：「识别 OCR 文本」只解析你粘贴的文字。名单在图片里时，请先用其他 OCR 工具转成文字再粘贴。

### 2. 地图编辑
- **点击省份**：选中并在右侧编辑卡片
- **拖拽卡片**：自由调整位置，智能避让重叠
- **锚点连线**：卡片与省份自动连接，可隐藏
- **缩放/平移**：滚轮缩放，拖拽平移
- **键盘直达画布**：打开编辑器后按 Tab，第一个焦点就是「跳到主要内容」链接，回车即可跳过顶栏直接进入画布工作区。
- **键盘选择地图/省份/图钉**：在画布里继续按 Tab，焦点会依次落到地图、学生图钉与各省份上，回车或空格即可选中，与鼠标点击等效。
- **撤销/重做播报**：地图样式页右栏按下「撤销/重做」后，屏幕阅读器会即时播报「已撤销/已重做」的具体步骤。顶栏「历史与缩放」组的撤销/重做（含经典界面编辑器）同样会即时播报。

### 3. 素材库
- **字体**：上传 TTF/OTF，自定义班级字体
- **贴图**：上传 PNG/JPG，按省份绑定（如校徽）
- **颜色**：自定义卡片背景、文字、边框
- **模板**：10+ 内置模板（紧凑/宽松/极简/卡通）

### 4. 导出与分享
- **PNG**：高清位图，适合打印/展板
- **SVG**：矢量格式，可再转 PDF 或印刷
- **项目包**：.cengfan 格式，可导入其他设备复用
- **印刷出血**：画布属性里的「印刷出血(mm)」，范围 0–20。默认 0 = 不加出血，导出结果里也没有任何裁切标记，屏幕分享、自行打印展板用默认值即可。要送印刷厂裁切时按印厂要求填写（常见 3mm），导出的 PNG/SVG 会在成品尺寸四周扩出出血区、把背景铺满出血框，并在成品框外画出裁切标记。
- **出血与安全边距的关系**：方向相反、配合使用。出血管画布**外侧**——背景多印出一圈，裁刀偏移也不会在纸边露白；安全边距管画布**内侧**——把卡片、文字收进版心，裁刀偏移也切不到内容。送印建议：出血按印厂要求设置，同时确认重要内容都留在安全边距以内。CMYK 分色等颜色转换请在印前软件（如 Acrobat / Scribus）中完成。
- **交付页尺寸**：未设出血时只显示「最终像素尺寸」（画布 × PNG 倍率）。设了出血后会同时给出「成品尺寸（裁切后）」和「导出尺寸（含出血与裁切标记）」——后者才是实际 PNG 文件的像素。设了出血后交付预览还会在成品四周画出出血区与裁切标记示意，真正的导出像素仍以右侧「导出设置」为准。窄屏或手机上该示意会整体缩小以完整显示成品与裁切标记，无需左右滚动。
- **印前体检**：交付页「印刷检查」会提示出血区内的对象、位图有效分辨率不足、透明背景撞出血、以及 PNG 倍率达不到约 300dpi。缺字体仍在「字体问题」里，不会重复。量不到原图像素的远程图会提示人工确认。浏览器 PNG 仍是 sRGB，不能替代印厂的 CMYK/ICC。其中「PNG 倍率不足」这类针对导出参数本身的提示没有画布对象可定位，按钮显示为「见导出设置」——直接在下方导出设置里调高倍率或改导 SVG 即可。

### 5. 协作
- 点击「创建房间」→ 生成房间码和一次性邀请凭证
- 可邀请「可编辑」或「仅查看」成员
- 通过服务器做增量同步；房间保存在内存中，约 30 分钟无活动后过期

---

## 三、示例案例

仓库内置示例为虚构姓名。完整说明见 [docs/案例模板/](docs/案例模板/)，成品截图见 [README](README.md)。

### 案例 A：985 高校附属中学（47 人）
- **制作时长**：3 小时（含手动微调）
- **成果**：按省份/城市看清全班去向，本省与京沪深分布一眼可辨
- **反馈**：「第一次把全班去向摊在一张图上」

### 案例 B：普通高中（68 人）
- **制作时长**：5 分钟（初稿）
- **成果**：导入名单 → 智能布局 → 导出高清图
- **反馈**：「家长终于看清孩子去向了」

### 案例 C：国际部（12 人小班）
- **制作时长**：2 小时（海外去向以无锚点卡片展示）
- **成果**：3 种风格供家长投票
- **反馈**：「最有纪念意义的毕业礼物」

完整案例集 → [docs/案例模板/](docs/案例模板/)

---

## 四、常见问题

**Q: Excel 导入失败？**
A: 检查字段名是否为「姓名」「去向」「省份」，或使用 CSV 格式。

**Q: 卡片重叠怎么办？**
A: 拖拽卡片到空白区域，或点击「智能布局」重新排列。

**Q: 如何添加校徽？**
A: 左侧「素材库」→ 上传 PNG → 拖到对应省份卡片。

**Q: 导出图片模糊？**
A: 使用「高清 PNG」选项，或导出 SVG 后再转图片/PDF。

**Q: 要送印刷厂，出血怎么设？**
A: 画布属性 →「印刷出血(mm)」按印厂要求填写（常见 3mm），导出 PNG/SVG 自动带出血区和裁切标记。默认 0 表示不加出血、也不画裁切标记。交付页会分开显示成品尺寸和含出血的导出尺寸；「印刷检查」会列出分辨率、透明出血等送印风险。PNG 3× 约 288dpi，仍低于 300dpi 印刷线时建议改导 SVG 交给印前放大。

**Q: 多设备同步？**
A: 导出 .cengfan 项目包 → 另一台设备导入。

---

## 五、毕业季 Checklist

### 班主任版
- [ ] 收集最终名单（Excel/CSV）
- [ ] 导入蹭饭图 → 3 分钟出初稿
- [ ] 上传校徽 + 字体
- [ ] 手动微调重叠卡片
- [ ] 导出 PNG（展板）或 SVG（再转印刷稿）
- [ ] 分享到班级群/家长群

### 班委版
- [ ] 收到名单 → 2 天内出初稿
- [ ] 导入名单 → 选择模板
- [ ] 导出 3 种风格供投票
- [ ] 归档项目包（下次复用）

### 毕业生/家长版
- [ ] 下载原项目包
- [ ] 更新个人去向
- [ ] 导出更新版地图
- [ ] 分享朋友圈/校友群

完整 Checklist → [docs/私域/毕业季-checklist.md](docs/私域/毕业季-checklist.md)

---

## 六、反馈与支持

- **GitHub Issues**：选模板提交 Bug / 建议 / 意见
- **用户群**：导入和出图（见 `docs/私域/用户群运营手册.md`）
- **开发者**：先读 [CONTRIBUTING.md](CONTRIBUTING.md)

---

**让每张毕业去向地图，都成为班级故事的起点。**