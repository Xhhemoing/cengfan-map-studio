# 蹭饭地图工作室前端界面设计规划 2.0

> 基线文档：`function.md`
>
> 规划目标：在不删减现有功能、不改变工程数据模型和核心算法的前提下，以**使用流程顺畅、界面整洁、简单易上手**为共同约束，重新定义主编辑器的信息架构、组件职责、显隐层级和交互规则。
>
> 适用范围：主编辑器 `/`。独立管理页 `/admin` 不进入本规划。

---

## 1. 设计结论

### 1.1 核心判断

`function.md` 将功能重组为「名单、地图、版式、内容、素材、交付」六个工作区，方向正确，但仅完成六区归类还不够。若把每区所有功能同时铺开，用户仍会面对以下问题：

- 六个工作区看似平级，但实际重要性不同：名单、地图、版式、交付是主路径，内容和素材多为按需补充；
- 左侧工作区、右侧属性检查器和全局设置可能同时出现相同能力，入口重复；
- 模板、地图框、卡片、字体、贴图等对象同时存在“整体设置”和“局部设置”，新用户难以判断该去哪里；
- 名单表格、素材库、复杂属性都需要较大空间，若全部塞进固定侧栏，会显得拥挤且难以操作；
- AI、协作、资源包、渲染性能等低频能力容易挤占主任务空间。

因此 2.0 规划不再只回答“功能属于哪一区”，还必须回答：

1. 用户当前只需要看到什么；
2. 哪些能力应该通过画布对象直达；
3. 哪些能力必须延后展示；
4. 什么时候使用侧栏，什么时候使用全屏工作台；
5. 完成当前任务后，系统如何自然引导下一步。

### 1.2 最终结构

保留六个业务工作区，但将它们重新定义为一条有主次关系的制作流程：

```text
核心必经：名单 → 地图 → 版式 → 交付
按需补充：              内容 → 素材
```

界面仍按顺序展示六步：

```text
1 名单  →  2 地图  →  3 版式  →  4 内容  →  5 素材  →  6 交付
```

区别在于：

- **名单、地图、版式、交付**承担明确的完成状态和下一步引导；
- **内容、素材**允许跳过，不使用“未完成”制造压力；
- 用户可随时点击任意步骤，不被向导锁死；
- 工作区负责“完成一类任务”，检查器只负责“编辑当前对象”；
- 全屏模式只用于名单大表格和交付检查，不再建设第二套全局设置中心。

### 1.3 三维设计准则

| 维度 | 设计标准 | 判断问题 |
|---|---|---|
| 使用流程 | 控件按完成海报的自然顺序出现 | 用户完成当前动作后，下一步是否明确？ |
| 界面整洁 | 同一时间只突出一个主任务，避免重复入口 | 当前屏幕是否有两个以上同权重主按钮？ |
| 简单易上手 | 默认值可直接产出，专业能力渐进披露 | 不理解术语的用户能否在 10 分钟内完成海报？ |

所有后续界面决策都必须同时满足三个维度，而不能只满足其中一个。

---

## 2. 用户与任务模型

### 2.1 主要用户

1. **首次制作的班委/学生**
   - 手上通常只有微信群名单、Excel 或零散文本；
   - 不熟悉地图表达、图层和排版术语；
   - 目标是尽快生成一张可分享的毕业去向图。

2. **需要美化的熟练用户**
   - 会调整模板、贴图、字体、卡片和连接线；
   - 需要画布实时反馈、拖拽和局部精调；
   - 不希望被向导限制操作顺序。

3. **继续修改旧工程的用户**
   - 目标通常是改名单、换一张贴图、调整一个标题或重新导出；
   - 不应该被强制重新走完整流程。

### 2.2 产品必须优先支持的四条任务路径

#### 路径 A：第一次制作

```text
新建工程
→ 粘贴/上传名单
→ 修正未匹配城市
→ 选择地图表达
→ 应用模板并智能排版
→ 修改标题
→ 交付检查
→ 导出 2× PNG
```

#### 路径 B：快速改名单

```text
打开旧工程
→ 名单
→ 搜索学生并修改
→ 画布即时更新
→ 交付检查
→ 重新导出
```

#### 路径 C：美化某个省份

```text
在画布点击省份
→ 右侧显示省份快捷属性
→ 点击“选择贴图”
→ 素材区自动筛选该省
→ 点击缩略图应用
→ 画布即时预览
```

#### 路径 D：调整局部排版

```text
在画布点击地图/卡片/文本/嘉宾面板
→ 右侧显示对应属性
→ 拖动或修改数值
→ 撤销/重做验证
→ 交付检查
```

任何高频任务如果需要跨越两个以上无关页面，均视为规划失败。

---

## 3. 整体界面骨架

### 3.1 桌面布局

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 项目 ▾  海报名称/保存状态   撤销 重做   缩放 适应画布   AI   预览   导出     │ 56px
├──────────────────────────────────────────────────────────────────────────────┤
│ 1 名单 │ 2 地图 │ 3 版式 │ 4 内容 │ 5 素材 │ 6 交付                     │ 52px
├──────────────────┬──────────────────────────────────────┬────────────────────┤
│ 当前任务面板      │                                      │ 当前对象属性        │
│ 280–320px         │          始终可见的海报画布          │ 272–304px           │
│                  │                                      │ 可折叠              │
│ 主任务            │                                      │                    │
│ 次要设置          │                                      │                    │
│ 下一步            │                                      │                    │
└──────────────────┴──────────────────────────────────────┴────────────────────┘
```

#### 顶栏只保留全局动作

- 项目菜单；
- 可编辑海报名称；
- 保存状态；
- 撤销、重做；
- 缩放、适应画布；
- AI 助手入口；
- 预览模式；
- 主导出按钮。

以下功能不得直接占用顶栏：工程包细节、资源包、协作房间设置、网格尺寸、渲染帧率、模板另存。这些能力进入对应工作区或项目菜单的二级层。

#### 流程条承担导航与状态，不承担设置

每一步只显示：

- 序号；
- 名称；
- 状态图标；
- 必要时一个短警告数量。

示例：

```text
✓ 名单    ✓ 地图    ● 版式    内容    素材    ! 交付 2
```

流程条不显示长说明，不放二级标签，不重复放“下一步”按钮。

#### 左栏只承担当前任务

左栏不是组件仓库。每个工作区首屏只允许：

- 一个清晰标题；
- 一句当前状态；
- 一个主动作；
- 最多三个直接设置分组；
- 底部一个“下一步”动作。

#### 中央画布始终是主角

- 除名单全屏管理和交付全屏检查外，画布始终可见；
- 修改设置后立即反映到画布；
- 选中对象时显示边界、拖拽柄和必要辅助线；
- 未选中对象时不显示多余框线；
- 网格、吸附、渲染模式属于画布工具，不属于某个业务工作区。

#### 右侧只编辑当前对象

右侧检查器严格遵守“所选即所得”：

| 选中对象 | 右侧内容 |
|---|---|
| 未选中 | 当前工作区摘要、推荐下一动作 |
| 画布 | 尺寸、背景、安全边距 |
| 地图 | 位置、尺寸、缩放、整体样式 |
| 省份 | 颜色、贴图、恢复默认 |
| 数据卡片 | 卡片样式、字段、连接线、位置 |
| 文本 | 内容、字体、字号、颜色、对齐 |
| 嘉宾面板 | 布局、背景、人员内容 |
| 素材实例 | 位置、尺寸、透明度、图层、删除 |

右侧不得出现与当前对象无关的全局功能。

### 3.2 响应式策略

#### 大屏：`≥ 1280px`

- 三栏同时显示；
- 左栏 280–320px；
- 右栏 272–304px；
- 中央画布占剩余空间；
- 流程条完整显示名称和状态。

#### 中屏：`768–1279px`

- 左栏保持显示；
- 右侧检查器改为按需抽屉，选中对象后从右侧出现；
- 流程条仅显示序号、名称和状态；
- 顶栏低频动作收进“更多”菜单。

#### 小屏：`< 768px`

手机端定位为“查看、改数据、换素材、轻量调整和导出”，不承担复杂自由排版：

- 顶部为可横向滚动的六步导航；
- 主体一次只显示任务面板或画布，通过「设置 / 画布」分段开关切换；
- 右侧属性改为底部抽屉；
- 名单使用卡片行而不是宽表格；
- 隐藏高精度数值布局和复杂连接线编辑，保留拖拽、模板和智能排版；
- 导出前仍可打开全屏预览。

不允许通过缩小字体或压缩按钮来硬塞桌面布局。

---

## 4. 信息层级与显隐规则

### 4.1 四层信息模型

#### L0：始终可见

- 六步流程；
- 画布；
- 当前保存状态；
- 撤销/重做；
- 当前工作区主动作；
- 导出入口。

#### L1：当前工作区直接操作

- 名单导入和问题修复；
- 地图表达选择；
- 模板和智能排版；
- 标题、文字、嘉宾；
- 省份贴图、背景、字体；
- 交付检查和 PNG 导出。

#### L2：选中对象属性

- 位置、尺寸、颜色、字体、透明度、图层；
- 单省贴图参数；
- 卡片字段和连接线；
- 嘉宾条目细节。

#### L3：高级/低频能力

- 十种地图边界纹理；
- 自定义表达式和姓名脱敏模板；
- 热力范围、上传地图精确对齐；
- 渲染帧率、网格间距；
- 资源包、协作冲突详情；
- 工程包细粒度选项。

L3 只能通过“高级设置”或“更多”展开，不得默认展开。

### 4.2 控件唯一归属

| 功能 | 唯一主入口 | 快捷入口 |
|---|---|---|
| 学生增删改、导入 | 名单 | 交付警告跳转到具体学生 |
| 数据表达方式 | 地图 | 名单导入完成后的下一步推荐 |
| 模板 | 版式 | 新建工程起始选择 |
| 画布尺寸和背景 | 版式 | 选中画布后的右侧属性 |
| 地图整体外观 | 地图 | 选中地图后的右侧属性 |
| 卡片结构和自动布局 | 版式 | 选中卡片后的右侧属性 |
| 标题、文字、嘉宾 | 内容 | 点击画布对象后的右侧属性 |
| 省份贴图 | 素材 | 点击省份后的“选择贴图” |
| 字体上传和管理 | 素材 | 文本属性中的字体选择器 |
| 保存、导出、协作 | 交付 | 顶栏主导出按钮 |
| AI 建议 | 全局 AI 入口 | 当前工作区的建议按钮 |

“唯一主入口 + 上下文快捷入口”允许存在；两个平级主入口不允许存在。

### 4.3 设置生效规则

- 颜色、开关、选择器、滑杆：立即生效并写入撤销历史；
- 文本和数值输入：失焦或 Enter 提交，输入期间显示“未应用”；
- 拖拽：实时轻量预览，松手后形成一个事务；
- 模板、智能排版、AI、替换名单、工程导入：先展示影响摘要，再应用为一个可撤销事务；
- 素材上传：上传成功即入库，但不默认应用，除非用户从“替换当前对象”动作发起上传；
- 删除素材：先显示引用位置和影响，再确认删除。

---

## 5. 六个工作区的首屏规划

## 5.1 名单

### 首屏目标

让用户在不理解数据模型的情况下完成“导入、看问题、修正”三件事。

### 空工程状态

左栏只显示三种起点：

1. **粘贴名单**（主按钮，默认推荐）；
2. 上传 Excel/CSV；
3. 手动添加。

下方提供一行真实格式示例：

```text
张三  浙江大学  杭州
```

AI 智能识别作为粘贴区中的辅助开关，不单独占据主按钮。

### 已有数据状态

顶部摘要：

```text
32 人 · 28 已显示 · 3 条需修正
```

直接显示三个分组：

1. **待处理问题**：未匹配城市、缺少字段、重复记录；
2. **最近修改**：最近 5 条记录；
3. **显示范围**：全部显示、全部隐藏、搜索。

主按钮变为“管理全部名单”，打开全屏名单工作台。

### 全屏名单工作台

```text
┌ 返回画布 | 学生名单 32 | 搜索 | 筛选：全部/问题/隐藏 | 添加/导入 ┐
├──────────────────────────────────────────────────────────────┤
│ 姓名 │ 院校 │ 城市 │ 解析省份 │ 类型 │ 显示 │ 操作          │
└──────────────────────────────────────────────────────────────┘
```

- 支持行内编辑；
- 问题行在具体字段旁给出原因和修复动作；
- 表格上方固定批量操作；
- 不在此处显示地图样式、卡片样式或模板。

### 完成条件

- 有至少一条可显示记录；
- 未匹配问题可以存在，但必须有明确数量；
- 点击“选择地图表达”进入地图工作区。

---

## 5.2 地图

### 首屏目标

先回答“数据如何被读懂”，再处理颜色和贴图。

### 表达方式

五种方式使用紧凑单选列表，不使用五张大型装饰卡片：

| 方式 | 一句话说明 |
|---|---|
| 省份卡片 | 按省汇总，适合大多数班级 |
| 城市卡片 | 突出具体目的城市 |
| 院校卡片 | 突出录取院校 |
| 地图图钉 | 人数较少，直接落点 |
| 人数热力 | 人数较多，观察分布趋势 |

系统根据名单规模给一个“推荐”标记，但不自动替用户切换。

### 首屏直接设置

仅保留：

1. 表达方式；
2. 主色/空省色；
3. 省名显示；
4. 南海小框开关。

其余边界纹理、热力范围、地图图片对齐进入高级设置。

### 省份上下文

点击省份后，右侧固定显示：

```text
浙江省
8 人 · 3 个城市
[改颜色] [选择贴图] [恢复默认]
```

选择贴图后进入素材工作区，并自动携带筛选条件 `province=浙江省`。返回地图时保留刚才的画布选中状态。

### 完成条件

- 已选择一种表达方式；
- 地图可正常显示有效数据；
- 点击“安排整张海报”进入版式。

---

## 5.3 版式

### 首屏目标

不用学习专业排版，也能得到一张完整、均衡、可继续编辑的海报。

### 首屏结构

1. **整体模板**：横向缩略图列表，显示 5 个内置模板和“我的模板”；
2. **画布规格**：常用横版、竖版、方形，自定义尺寸进入更多；
3. **卡片风格**：标准、紧凑、票券、照片，使用图形预览；
4. **智能排版**：主按钮，旁边显示结果说明，例如“将 8 张卡片分布到地图四周”。

### 不在左栏直接展示

- 地图 X/Y/宽/高；
- 卡片精确间距；
- 连接线宽度和纹理；
- 允许重叠；
- 渲染帧率；
- 所有文本字体。

这些设置分别进入画布选中对象属性或高级设置。

### 画布直接操作

- 拖动地图框、卡片、文字、嘉宾面板；
- 选中后右侧显示精确值；
- 多张卡片默认作为数据板块整体选中，二次点击才选择单卡片；
- 智能排版后允许手动覆盖；
- 手动移动过的卡片显示一个小型“已手调”标记，可单独恢复自动位置。

### 完成条件

- 画布、地图和数据板块均在安全区域内；
- 没有明显内容溢出；
- 点击“完善海报内容”进入内容。

---

## 5.4 内容

### 首屏目标

将散落的文字和嘉宾能力组织为可扫描的内容大纲。

### 内容大纲

左栏使用列表，不使用混合表单：

```text
标题区
  眉题        2026 GRADUATION
  主标题      我们的毕业去向图
  副标题      山海有期，未来可见

附加内容
  特别备注    1 项
  自定义文字  2 项
  嘉宾/老师   4 人
```

点击列表项后：

- 画布自动定位并选中对象；
- 右侧显示该对象属性；
- 双击画布文字可直接编辑文本；
- 每种内容旁只保留新增、显隐和删除三个直接动作。

### 内容预设

允许提供少量文案结构预设，例如“毕业去向”“录取榜”“班级蹭饭图”，但应用前必须显示将修改哪些默认文本，不能覆盖用户已改内容。

### AI 的归属

AI 不作为内容区的固定大面板。内容区只显示一个轻量动作：

```text
用 AI 优化标题和说明
```

点击后从右侧打开提案抽屉，逐项勾选、预览后应用。

### 完成条件

内容为可选步骤，不显示“未完成”。至少存在主标题时标记为“已完善”，否则标记为“可跳过”。

---

## 5.5 素材

### 首屏目标

让素材先按用途被找到，再按文件被管理。

### 顶部上下文条

若从省份、背景或文本属性进入，顶部显示当前目标：

```text
正在为：浙江省 选择贴图    [取消目标]
```

若无上下文，默认进入素材总览。

### 一级分类

只保留四个分类标签：

1. 省份贴图；
2. 背景与地图；
3. 画布图片；
4. 字体。

资源包和使用关系管理放入右上“管理”菜单。

### 素材网格规则

- 缩略图保持统一比例；
- 当前使用项显示“使用中”文字与勾选图标，不能仅靠边框颜色；
- 点击缩略图立即应用到当前目标；
- 无目标时点击显示素材详情，主按钮为“应用到……”；
- 上传按钮固定在分类标题旁，不单独占据大卡片；
- 上传后立即出现在列表首位；
- 加载、处理中、失败都在对应素材格内表达，不使用全页遮罩。

### 素材详情

选中素材后右侧显示：

- 名称、类型、尺寸、来源；
- 使用位置；
- 自动抠图；
- 提取配色；
- 替换、下载、删除。

省份贴图的缩放、透明度、位置属于“当前省份属性”，不属于素材文件属性。

### 完成条件

素材是可跳过步骤。使用任意用户素材或自定义字体后显示“已个性化”，否则显示“可跳过”。

---

## 5.6 交付

### 首屏目标

先告诉用户作品是否完整，再提供最可能需要的导出方式。

### 交付健康度

顶部不用抽象百分比，而使用可行动状态：

```text
可以导出
32 人已纳入海报 · 8 个目的省市 · 已保存到本机
```

若有问题：

```text
导出前建议处理 2 项
- 2 个城市未匹配          [去修正]
- 3 名学生已隐藏          [查看名单]
```

警告不强制阻断导出，但每项必须能跳转到具体处理位置。

### 默认导出

主按钮固定为：

```text
导出 2× PNG
```

旁边提供下拉选项：

- PNG 1× / 2× / 3×；
- 透明背景；
- SVG。

导出前显示尺寸和预计结果，不让用户先理解倍率概念。

### 保存和继续编辑

第二分组只显示：

- 保存到本机；
- 导出完整工程；
- 导入工程。

### 协作与迁移

协作、资源包放入“协作与迁移”折叠区：

- 创建/加入房间；
- 当前同步状态；
- 导入/导出资源包；
- 冲突处理。

除非用户已经加入协作房间，否则不在首屏持续显示房间和版本信息。

### 导出完成状态

导出成功后保留当前编辑上下文，不跳转空白成功页。显示轻量结果条：

```text
已导出 我的毕业去向图-2x.png    [再次导出] [打开交付文件夹]
```

---

## 6. 导航、选择与状态同步

### 6.1 工作区切换

- 用户主动切换工作区时，保留画布缩放和选中对象；
- 若所选对象不属于新工作区，不清空选择，只折叠无关属性；
- 从快捷入口跳转时，记录来源，并提供“返回地图/返回交付检查”；
- 刷新或重新打开工程时恢复上次工作区，但新工程始终从名单开始。

### 6.2 画布选择驱动界面

| 画布动作 | 界面响应 |
|---|---|
| 单击省份 | 保持当前工作区，右侧打开省份属性 |
| 双击省份 | 进入地图工作区并定位单省设置 |
| 单击标题 | 右侧打开文字属性，内容区高亮对应项 |
| 单击数据卡片 | 右侧打开卡片属性，版式区高亮卡片设置 |
| 单击素材实例 | 右侧打开素材实例属性，不自动跳素材库 |
| 点击空白 | 选中画布，显示画布快捷属性 |
| Esc | 清除当前选择/关闭抽屉 |

自动跳转应谨慎使用。除双击或用户点击明确动作外，单击画布对象不强制切换一级工作区。

### 6.3 步骤状态

仅使用四种状态：

| 状态 | 含义 | 表现 |
|---|---|---|
| 待开始 | 尚无可用内容 | 空心圆 + 中性文字 |
| 可继续 | 已满足最低要求 | 实心点 + “可继续” |
| 需处理 | 存在可修复问题 | 警告图标 + 数量 |
| 已完成 | 用户已确认或执行主要动作 | 勾选图标 |

内容和素材允许显示“可跳过”，不得显示红色未完成。

---

## 7. 组件规划

### 7.1 页面组件树

```text
StudioShell
├── StudioHeader
│   ├── ProjectMenu
│   ├── DocumentTitle
│   ├── SaveStatus
│   ├── HistoryControls
│   ├── ViewControls
│   ├── AiTrigger
│   └── ExportTrigger
├── WorkflowStepper
├── StudioBody
│   ├── TaskPanel
│   │   └── ActiveWorkspacePanel
│   ├── CanvasWorkspace
│   │   ├── CanvasToolbar
│   │   ├── PosterCanvas
│   │   └── SelectionOverlay
│   └── ContextInspector
├── ContextDrawer
├── FullscreenWorkspace
└── ToastRegion
```

### 7.2 工作区组件

```text
RosterWorkspace
├── RosterQuickStart
├── RosterSummary
├── RosterIssues
└── OpenRosterManager

MapWorkspace
├── MapExpressionPicker
├── MapQuickStyle
└── MapAdvancedSettings

LayoutWorkspace
├── TemplateStrip
├── CanvasPresetPicker
├── CardPresetPicker
└── SmartLayoutAction

ContentWorkspace
├── ContentOutline
├── ContentItem
└── ContentPresetAction

AssetWorkspace
├── AssetContextBar
├── AssetCategoryTabs
├── AssetToolbar
├── AssetGrid
└── AssetDetail

DeliveryWorkspace
├── DeliveryHealth
├── ExportPreset
├── SaveAndProjectActions
└── CollaborationDisclosure
```

### 7.3 复用边界

- 工作区组件只组合任务和调用现有领域回调，不复制 `ProjectDocument`；
- 属性表单继续复用现有 Inspector 组件，但拆出可复用 section；
- `DataWorkspace` 拆为快速摘要和全屏管理两种布局，共用数据逻辑；
- `AssetPanel` 拆为分类、网格、上下文应用和详情，不再由一个组件同时承担全部流程；
- AI 提案继续使用现有命令预览机制，只改变入口和展示容器；
- Workflow 状态从项目内容派生，不单独保存一套易失真的完成标记。

---

## 8. 视觉系统

### 8.1 设计方向

视觉主题定义为：**地图制图台 + 班级纪念册**。

产品是高频编辑工具，不做营销页，不使用巨大标题、渐变球、悬浮装饰卡片和大面积单色背景。视觉重点放在纸张画布、流程状态和数据可读性。

### 8.2 色彩角色

建议使用多角色而非单一蓝色或紫色体系：

| Token | 建议色 | 用途 |
|---|---|---|
| `ink-900` | `#183247` | 主文字、顶栏 |
| `sea-700` | `#176F68` | 当前步骤、主操作、选中态 |
| `coral-600` | `#C95C49` | 导出、重要提醒、海报强调 |
| `gold-500` | `#C5A253` | 完成状态、纪念感点缀 |
| `paper-50` | `#FFFDF7` | 海报纸张 |
| `surface-100` | `#F2F5F4` | 工具区域背景 |
| `line-300` | `#CDD8D9` | 边界和分隔 |
| `danger-600` | `#B64545` | 错误和破坏性动作 |

- 不使用渐变作为主界面背景；
- 状态不得只通过颜色区分；
- 主色只用于当前任务和主按钮，不染满所有面板。

### 8.3 排版

- 界面字体：`Noto Sans SC` 或系统中文无衬线；
- 数字、倍率、尺寸：等宽字体；
- 海报内容字体由用户配置，不反向影响工具界面；
- 顶栏标题 14–16px，工作区标题 16px，正文 13–14px，辅助信息 11–12px；
- 不使用随视口宽度缩放字体；
- 所有字距为 0。

### 8.4 尺寸与形态

- 基础间距：4px；常用间距：8/12/16/24px；
- 普通按钮高度 32–36px；主按钮 38–40px；
- 图标按钮保持正方形并提供 tooltip；
- 卡片圆角不超过 6px；
- 工作区 section 使用分隔线和留白，不嵌套卡片；
- 阴影只用于画布纸张、抽屉和模态框；
- 选择态不改变控件尺寸，避免布局抖动。

### 8.5 图标与文案

- 使用现有 Lucide 图标；
- 图标按钮必须有 `aria-label` 和悬浮提示；
- 主命令使用动词：粘贴名单、修正城市、应用模板、智能排版、选择贴图、导出 PNG；
- 避免“提交”“处理”“配置”“全局设置”等抽象词；
- 错误文案说明发生了什么以及如何修复；
- 空状态只提供一个明确起点，不展示功能说明清单。

---

## 9. AI 助手设计

### 9.1 定位

AI 是跨流程的副驾驶，不是一级工作区，也不能直接修改工程。

### 9.2 入口

- 顶栏固定 AI 图标；
- 当前工作区可显示一条上下文建议，例如“根据 32 人名单推荐卡片布局”；
- 名单粘贴区可启用智能识别；
- 内容区可请求文案优化。

### 9.3 交互流程

```text
输入需求
→ 生成可理解的变更清单
→ 用户逐项勾选
→ 画布临时预览
→ 应用为一个可撤销事务
```

AI 面板必须显示：

- 将改变什么；
- 哪些对象受影响；
- 是否覆盖现有内容；
- 预览、应用、取消。

不允许 AI 静默切换模板、替换名单或覆盖文案。

---

## 10. 易用性与无障碍要求

### 10.1 键盘

- Tab 顺序遵循顶栏 → 流程条 → 左栏 → 画布工具 → 右栏；
- 方向键可在流程步骤、分段控件和素材网格中移动；
- Enter/Space 激活按钮；
- Esc 关闭抽屉、菜单和选区；
- Ctrl/Cmd+Z 撤销，Ctrl/Cmd+Shift+Z 或 Ctrl+Y 重做；
- 所有快捷键只在 tooltip 或帮助中展示，不占据主界面。

### 10.2 焦点

- 工作区切换后，焦点进入工作区标题；
- 打开全屏名单后，焦点进入搜索或首个错误；
- 关闭抽屉/模态框后，焦点返回触发按钮；
- 焦点样式必须在浅色和深色区域均清晰可见。

### 10.3 状态与反馈

- 保存、导入、导出、上传使用明确的进行中、成功和失败状态；
- 不用只转圈且无说明的加载状态；
- 大型内容加载优先骨架，不遮挡整个编辑器；
- Toast 只报告结果，不承载必须操作的错误；需要修复的错误留在对应工作区。

### 10.4 可读性

- 普通文字对比度至少 4.5:1；
- 触控目标至少 44×44px（移动端）；
- 状态使用图标 + 文字，不仅使用红绿颜色；
- 表单输入有可见标签；
- 自定义下拉和素材格支持屏幕阅读器识别当前选择。

---

## 11. 必须删除或合并的重复界面

### 11.1 不再保留独立“全局设置”中心

现有全局设置能力拆回业务位置：

- 画布 → 版式；
- 地图 → 地图；
- 数据卡片 → 版式；
- 嘉宾与文字 → 内容；
- 字体管理 → 素材；
- 高级表现 → 对应工作区高级设置。

允许保留底层复用组件，但不能继续向用户暴露第二套平行导航。

### 11.2 不再保留独立 AI 页面

AI 变为顶栏抽屉和当前任务建议。

### 11.3 不重复提供所有导出入口

- 顶栏“导出”是快速入口；
- 交付是完整入口；
- 项目菜单只放工程级打开/新建/导入，不再重复 PNG/SVG。

### 11.4 不把属性表单复制进左栏

左栏提供任务级选择和批量动作；精确数值只在右侧检查器出现。

---

## 12. 实施顺序

### 阶段 1：统一壳层与导航

- 建立 `StudioShell`、`WorkflowStepper`、`TaskPanel`、`ContextInspector`；
- 六区成为唯一业务导航；
- 删除用户可见的“设计 / 素材 / AI”和“全局设置”平行入口；
- 保持原有工程状态、事务和 API 不变。

**验收：** 用户不进入任何旧设置页即可完成导入、地图表达、模板排版和 PNG 导出。

### 阶段 2：名单、地图、交付三个关键闭环

- 名单快速摘要 + 全屏管理；
- 地图表达 + 单省快捷动作；
- 交付健康检查 + 可跳转警告；
- 工作区状态从项目内容实时派生。

**验收：** 新用户可沿四个必经步骤完成标准海报。

### 阶段 3：版式与上下文检查器

- 模板、规格、卡片预设和智能排版收敛到版式首屏；
- 画布选择与右侧属性同步；
- 高级设置折叠；
- 手动位置覆盖与恢复自动位置状态明确。

**验收：** 首屏不出现精确布局参数，但专业用户仍能从选中对象进入完整能力。

### 阶段 4：内容和素材直达

- 内容大纲替代混合表单；
- 省份 → 贴图、文本 → 字体、画布 → 背景形成上下文直达；
- 素材上传、处理、应用和引用状态实时一致。

**验收：** 从点击省份到应用贴图不超过 3 次点击，且画布立即更新。

### 阶段 5：响应式、无障碍和性能

- 中屏检查器抽屉；
- 手机设置/画布切换；
- 键盘导航、焦点恢复、ARIA；
- 大名单和素材网格虚拟化/延迟渲染；
- 导出与重布局期间保持界面可反馈。

---

## 13. 验收标准

### 13.1 使用流程

- 新用户从新建到导出标准 PNG 不超过 10 分钟；
- 主路径不要求打开“全局设置”；
- 导入名单后一次点击即可进入地图表达；
- 点击省份后一次点击即可进入该省贴图；
- 交付警告可直接跳到具体记录或对象；
- 用户可跳过内容和素材直接导出。

### 13.2 界面整洁

- 每个工作区首屏只有一个主按钮；
- 左栏首屏不超过三个直接设置分组；
- 顶栏不超过七组可见动作，低频项进入菜单；
- 同一功能不存在两个平级主入口；
- 右侧检查器只显示当前对象相关属性；
- 高级能力默认折叠；
- 不出现 section 套 card、card 再套 card 的视觉结构。

### 13.3 简单易上手

- 空工程首屏明确提供粘贴名单、上传表格和手动添加；
- 五种地图表达均有一句适用说明和一个推荐项；
- 智能排版可直接产生可用结果；
- 导出默认值为 2× PNG；
- 所有危险操作说明影响范围并可撤销或确认；
- 状态文案使用用户语言，不暴露内部类型名和 API 名。

### 13.4 技术质量

- 320、390、768、1024、1440px 无页面级横向溢出；
- 工作区切换不重建海报数据或重置画布缩放；
- 所有交互可使用键盘完成；
- 正文对比度达到 WCAG 2.1 AA；
- 首屏不加载非当前工作区的大型列表和资源；
- 大名单、大素材库滚动不阻塞画布；
- 控制台无错误，现有测试、lint、build 全部通过；
- PNG/SVG/工程包、素材即时应用、本地恢复和协作行为保持兼容。

---

## 14. 明确不做

为了保证流程简单和界面整洁，以下内容不进入本次主界面规划：

- 不建设营销首页或功能介绍首屏；
- 不增加新的工程数据格式；
- 不把所有高级能力做成永久可见工具条；
- 不让 AI 自动执行未经预览的修改；
- 不在主站暴露访问统计或后台入口；
- 不让手机端承担复杂像素级排版；
- 不用新的组件库重写已有成熟表单；
- 不为了视觉效果增加大面积渐变、装饰球、重阴影或营销式卡片；
- 不删除历史工程兼容能力，但不让历史低频素材占据新项目首屏。

---

## 15. 最终体验定义

重构完成后的主编辑器应让用户形成一个稳定心智模型：

```text
我在顶部知道做到哪一步；
我在左边完成当前任务；
我在中间始终看到海报结果；
我点击什么，就在右边修改什么；
我不知道怎么做时使用推荐或 AI；
我准备完成时，在交付中知道还有什么问题。
```

这比单纯增加更多面板或重新命名菜单更重要。`frontUI2` 的成功标准不是“所有功能都被展示”，而是：**所有功能都有明确归属，高频功能在正确时机出现，低频功能不会妨碍第一次完成作品。**
