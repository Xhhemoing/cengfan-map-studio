# 蹭饭地图工作室（Cengfan Map Studio）功能与使用架构

> 项目根目录：`/home/ubuntu/work/蹭饭图`
> 包名：`cengfan-map-studio`（v0.1.0，私有）
> 一句话定位：面向班级毕业季的「毕业去向图 / 蹭饭图」在线海报编辑器。用户导入同学去向名单，在中国地图上选择可视化方式、完成排版和个性化后，导出 PNG/SVG 海报或完整工程包，并可按需在线协作。

---

## 1. 重组结论

### 1.1 当前分类的问题

现有界面按实现来源组织：左侧是「设计 / 素材 / AI」，顶部另有「全局设置 / 工程」，右侧根据选中对象显示属性。它覆盖的功能很完整，但用户制作一张海报时必须先判断：

- 导入名单属于「全局设置」而非主工作台；
- 模板、画布、地图展示框和数据卡片共同决定版式，却分散在「设计」「全局设置」「右侧属性」；
- 省份贴图既是地图表达，又只能从素材库寻找；
- AI、协作、资源包是辅助能力，却与制作动作并列；
- 新用户会先面对空画布和大量局部属性，缺乏一条默认的完成路径。

**重组原则：以用户目标和制作顺序分类，以选中对象承担局部属性，以渐进披露收纳低频能力。** 不删除现有功能，只改变入口、归属和默认显隐层级。

### 1.2 推荐一级功能架构

| 一级工作区 | 用户要完成的事 | 核心内容 | 默认优先级 |
|---|---|---|---|
| **1. 名单** | 准备可信的毕业去向数据 | 录入、导入、校验、显示范围 | 第一步，必经 |
| **2. 地图** | 决定这份数据如何被读懂 | 省份/城市/院校卡片、图钉、热力、单省外观 | 第二步，必经 |
| **3. 版式** | 建立一张完整海报的视觉结构 | 模板、画布、地图框、数据框、自动布局 | 第三步，必经 |
| **4. 内容** | 补充叙事信息与班级记忆 | 标题、统计、备注、嘉宾/老师名单 | 第四步，常用 |
| **5. 素材** | 管理并应用视觉资源 | 省份贴图、背景图、上传、抠图、字体、资源包 | 按需使用 |
| **6. 交付** | 检查、保存、分享和导出成果 | 导出、工程包、本地保存、协作、风险检查 | 最后一步，必经 |

**跨流程工具（不占一级导航）：**
- **AI 助手**：作为全局悬浮按钮/命令入口，基于当前工作区提出建议、预览并应用。
- **撤销 / 重做 / 缩放 / 网格 / 吸附**：始终放在画布顶栏。
- **右侧属性检查器**：点击画布对象后就地编辑对象属性，不需要用户主动寻找属性页面。
- **项目菜单**：新建、打开工程、恢复本地草稿、模板另存、资源包导入导出，收纳在左上「项目」菜单，避免占用制作导航。

### 1.3 默认制作路径

```
打开/新建工程
  → 名单：导入并修正数据
  → 地图：选择最适合名单的呈现方式
  → 版式：选模板并自动排版
  → 内容：完善标题、说明、老师名单
  → 素材：按省份补贴图和个性资源（可跳过）
  → 交付：检查、保存、协作或导出
```

这条路径应一直可见为紧凑步骤条。步骤状态只表达用户是否可以继续：`待开始`、`需处理`、`可继续`、`已完成`，而不是暴露内部模块名。

---

## 2. 推荐界面规划

### 2.1 全局骨架

```
┌ 项目 ▾ | 海报名称 | 名单 · 地图 · 版式 · 内容 · 素材 · 交付 | 撤销 重做 | 缩放 | 导出 ┐
├──────────────────────────────────────────────────────────────────────────────┤
│ 左侧：当前工作区操作        │ 中央：始终可见、可编辑的海报画布 │ 右侧：当前对象属性 │
│                             │                                  │                   │
└──────────────────────────────────────────────────────────────────────────────┘
                 AI 助手（浮动入口，提案预览后才提交）
```

- **顶栏**只保留全局且高频的项目、历史、预览与导出动作。
- **左栏**承载当前步骤的任务清单和批量操作。
- **中央画布**始终可见，所有设置即刻反馈到海报，不把用户带离当前上下文。
- **右栏**不再充当“万能设置中心”，仅显示当前选中对象的细节；没有选中对象时显示当前工作区的最常用设置和下一步。
- **全屏模式**仅用于「名单」的大型表格编辑与「交付」的检查/导出确认；其他设置在画布旁完成。

### 2.2 两种进入方式

| 进入场景 | 首屏 | 系统建议 |
|---|---|---|
| **第一次制作** | 模板选择 + 名单导入 | 先选择「粘贴名单 / 上传 Excel / 手动录入」，导入后自动进入地图呈现选择 |
| **从旧工程继续** | 上次打开的工作区和画布 | 显示未保存状态与「继续编辑 / 查看交付检查」 |
| **仅改一处** | 画布 + 对应对象检查器 | 点击省份/卡片/文字即可直接修改，不强制走步骤 |
| **从模板起步** | 模板预览 + 数据预览 | 应用模板后保留名单，提示下一步“检查地图表达” |

---

## 3. 一级工作区详细规划

### 3.1 名单：先保证数据正确

**用户目标：** 把散乱的姓名、院校、城市信息变成可用于地图的可信名单。

**主界面分区**
1. **导入**：粘贴文本、上传 Excel/CSV、OCR 式文本、AI 智能识别。
2. **审查**：候选列表逐条接受/拒绝，显示未识别行和导入数量。
3. **名单表格**：搜索、行内编辑、删除、显示/隐藏、批量显示/隐藏。
4. **问题清单**：缺少字段、重复姓名、未匹配城市、海外去向；每条问题可跳转到对应学生。
5. **数据范围**：显示人数、隐藏人数、有效省市数；这些指标同步给后续地图和交付检查。

**现有功能归属**
- 手动新增学生、城市/院校联想搜索。
- CSV/制表符/中文逗号/分号/空格格式识别与表头跳过。
- Excel/CSV 工作簿导入、OCR 文本导入、AI 名单解析。
- 导入候选审查、追加或替换。
- 城市→省份解析、别名解析、必填/重复/未解析城市校验。
- 海外去向标记、搜索、编辑、删除、显隐和批量显隐。

**体验规则**
- 有未匹配城市时不阻断用户进入下一步，但在步骤条、地图和交付阶段持续显示可点击警告。
- 导入后的默认动作不是打开全局设置，而是展示「选择地图表达方式」。
- 列表大屏时进入全屏数据工作台；小规模修订保持侧栏/抽屉，以免打断画布观察。

### 3.2 地图：先选择读图方式，再处理单省个性化

**用户目标：** 用最适合当前班级人数和信息密度的方式表达“大家去了哪里”。

**一级选择：地图表达方式**

| 表达方式 | 适合情况 | 当前能力 |
|---|---|---|
| **省份卡片** | 最常见的毕业去向总览 | 按省份聚合，同省同校合并；默认方式 |
| **城市卡片** | 目的地分散或城市信息更重要 | 按城市聚合 |
| **院校卡片** | 需要突出录取学校 | 按院校聚合 |
| **地图图钉** | 人数较少、强调地理位置 | 按省份落图钉 |
| **人数热力** | 人数较多、强调分布趋势 | 按人数映射省份深浅 |

**二级：地图整体外观**
- 底图、强调色、空省填充、地图透明度。
- 省界颜色、宽度和纹理（实线、虚线、点线、双线、柔光、缝线、栏杆、波浪、纹饰、墨线）。
- 省名标签显示、字体、颜色与字号。
- 南海诸岛展开/折叠为右下角小框。
- 热力色阶：人数范围、低值/高值颜色和预览。
- 矢量地图与上传地图图片的切换；图片可替换底色或叠加、裁剪到省界、调整透明度和对齐。

**三级：单省处理（从地图直接进入）**
- 点击省份后，右侧显示该省数据摘要与三个直接动作：**改颜色**、**应用贴图**、**恢复默认**。
- 「应用贴图」直接打开素材工作区中已筛选的该省素材，不再要求用户自行寻找归属。
- 可编辑贴图的填充方式、缩放、透明度、溢出、尺寸模式、统一尺寸和位置偏移；画布中可拖动贴图。
- 自动从贴图取色为当前省份配色；可一键优化全部有数据省份的相邻配色。

**现有功能归属**
- 五种 DataView、热力色阶、图钉、地图图层和省份统计。
- MapInspector、ProvinceInspector、MapLayer、MapDataLayer 的所有行为。
- 南海诸岛折叠、地图图片仿射对齐、每省纯色/贴图/恢复默认。
- 省份贴图碰撞处理、溢出放置和省份标签字体。

### 3.3 版式：建立可读、均衡、可导出的整张海报

**用户目标：** 不学习排版规则，也能快速得到一张不拥挤、重点明确的完整海报。

**主操作顺序**
1. **选择起点**：应用内置模板或我的模板。
2. **调整画布**：尺寸预设/自定义尺寸、安全边距、背景色或背景图。
3. **配置地图框**：地图位置、尺寸、缩放；画布拖拽和右侧数值输入双向同步。
4. **配置数据板块**：卡片预设、分组、字段、宽度、留白、间距、背景、文字、连接线。
5. **一键智能排版**：依据地图实际可用区域计算卡片位置，再允许局部手动拖动覆盖。
6. **可选精调**：网格、吸附、地图与卡片允许重叠、布局策略（四边均分/放射/右侧堆叠/网格）。

**卡片表达能力**
- 预设：标准、紧凑、票券、照片。
- 分组：省份、城市、院校；字段：姓名、院校、城市。
- 自定义卡片表达式：标题、城市标题、行内容模板。
- 姓名脱敏/格式：完整姓名、姓xx、Xxx、姓*某、首字**、大小写首字母、姓+末字掩码；支持复姓。
- 连接线：直线、折线、曲线；独立设置颜色、粗细、纹理。
- 省份贴图缩略图、城市分组小标题、卡片透明度和字体。

**自动布局底线**
- 优先避开真实省份内容区、标题文字和已放置卡片。
- 四边分流优先于单列堆叠；卡片区域不足时才启用允许地图重叠或降级策略。
- 自动结果始终可被手动移动覆盖，用户移动后不被下次局部刷新擅自重置。

**现有功能归属**
- 模板系统、CanvasInspector、CardsInspector、BlockStylePanel、CardPresentationSettings。
- 四种卡片布局求解、连接线几何、卡片文本换行、地图内容边界避让。
- 网格/吸附/缩放/适应画布/渲染帧率设置。

### 3.4 内容：把数据海报变成班级纪念海报

**用户目标：** 让海报表达班级身份、祝福和故事，而不干扰地图与数据制作。

**内容类型**
- **标题区**：眉题、标题、副标题、统计、水印、特别备注。
- **自定义文字**：新增、删除、拖动、对齐、最大宽度、字号、颜色、字重、字体和显隐。
- **嘉宾/老师名单**：标题、位置、宽度、留白、背景、透明度、文字样式。
- **嘉宾条目**：姓名、身份/职务、头像、备注/寄语、字体、显隐。
- **展示模式**：文本列表或头像卡片；自由备注会自动折行，最多显示 14 行。

**体验规则**
- 画布直接点击文本/嘉宾面板即可选中，右侧只显示对应属性。
- 「新增文字」「新增备注」「新增嘉宾」是显式动作；不把用户带入通用素材库。
- 标题区提供一组简短的内容预设，但不自动覆盖已有文案。

**现有功能归属**
- TextLayer、TextInspector、GuestsInspector、TypographyPanel 中与文字内容直接相关的功能。
- 内置文本角色、拖动、网格吸附、自定义文本、嘉宾列表和头像卡片。

### 3.5 素材：集中管理资源，但从使用位置直达

**用户目标：** 上传、复用和整理图片/字体，同时确保任何素材应用后立即反映在画布。

**素材库的分类**
1. **按省份贴图**：系统省份特色贴图 + 用户贴图；选中省份时默认过滤到该省。
2. **背景与整图地图**：背景图、上传地图图片。
3. **画布装饰**：保留历史工程兼容与现有实例编辑；新项目只在有明确、可用的系统素材时开放创建。
4. **字体**：内置字体、用户上传 TTF/OTF/WOFF/WOFF2、删除和全局应用。
5. **资源包**：素材与字体的导入/导出，跨工程迁移。

**资产操作**
- 拖拽或文件选择上传，创建后进入素材库，不需要刷新页面。
- 可选自动抠图：以四角背景色为依据清理背景并柔化边缘。
- 缩略图点击即应用，应用后高亮当前使用项并在画布即时预览。
- 替换、删除前显示资源引用位置；正在被背景、地图、某省或素材实例使用时，说明影响范围。
- 图像主题分析：提取主色/识别色/辅助色/背景色/描边/光晕，用于单省和邻省配色优化。

**现有功能归属**
- AssetPanel、FileDropzone、FontEditor、资源包、用户素材/字体持久化。
- 34 个省份特色 SVG 贴图、宣纸底纹背景；历史工程的装饰和地域剪影仍可读取迁移，但不作为新项目的默认创建入口。
- 自动抠图、图像取色、统一省份贴图尺寸、素材使用关系检查。

### 3.6 交付：让用户明确知道作品是否可用，并提供所有保存与分享出口

**用户目标：** 知道当前海报是否完整，安全地保存，并输出适合使用场景的文件。

**第一屏：交付检查**
- 名单总数、显示人数、目的省市数。
- 未匹配城市、隐藏学生、可能过密的卡片/画布提示。
- 每条问题可跳转回名单或对应画布区域；警告不强制阻断导出。

**第二屏：输出与保存**
- 导出 PNG：1×/2×/3×，可选透明背景。
- 导出 SVG：自动剔除选区、网格等编辑辅助层，可选透明背景。
- 导出工程：项目 JSON，可选择包含素材、字体和自定义模板。
- 导入工程：整体恢复工程与资源，并按 id/src 修复资源引用。
- 保存到本机：localStorage 镜像 + IndexedDB 持久工作区；清晰显示「未保存 / 保存中 / 已保存 / 保存失败」。

**第三屏：协作与资源迁移（按需展开）**
- 创建协作房间、复制房间码、加入房间、断开。
- 显示增量同步状态、房间版本、冲突原因和重新加入动作。
- 导入/导出资源包（素材和字体）用于跨项目复用。

**现有功能归属**
- PNG/SVG 导出、工程包/资源包导入导出、本地双轨存储、服务端工作区快照。
- 协作房间、SSE 增量同步、操作级 rebase、版本冲突提示。
- 工作流警告、项目摘要、同步状态显示。

---

## 4. 功能迁移映射

| 现有入口/模块 | 新归属 | 调整理由 |
|---|---|---|
| `DataWorkspace`（原全局设置内） | 名单 | 名单是海报制作的起点，不应藏在设置中 |
| WorkflowGuide | 顶部步骤条 + 当前工作区“下一步” | 从导航兼操作面板拆为进度与行动，减少重复入口 |
| 设计 > 模板 | 版式 > 选择起点 | 模板影响整张海报，属于全局版式 |
| 设计 > 元素 | 内容 + 版式 + 素材 | 文本/嘉宾归内容，结构对象归版式，资源归素材 |
| 全局设置 > 画布/地图/卡片 | 版式和地图 | 按用户目标重新分流，保留对象检查器的局部精调 |
| AssetPanel 中的省份应用 | 地图 > 单省处理（素材库承载资源） | 用户从地图点击省份后应直达可用贴图 |
| 字体工具 | 内容中的文本属性 + 素材 > 字体 | 选中文字时就地设置；上传和管理集中到资源库 |
| AI 助手独立一级页 | 全局悬浮入口 / 当前步骤助手 | AI 是辅助决策，不是独立制作阶段 |
| 工程菜单 + 项目摘要 | 项目菜单 + 交付 | 新建/恢复属于项目级，输出/保存/协作属于交付 |
| 渲染性能设置 | 版式 > 精调 > 高级 | 对大多数毕业海报用户低频，不干扰主路径 |
| `/admin` 访问统计 | 独立受保护管理页 | 保持不出现在制作主站中 |

---

## 5. 渐进披露规则

### 5.1 默认直接展示

| 工作区 | 直接展示的动作 |
|---|---|
| 名单 | 导入、修复问题、编辑名单、选择显示范围 |
| 地图 | 选择表达方式、整体配色、点击省份改颜色/贴图 |
| 版式 | 模板、画布尺寸、地图框、卡片预设、一键排版 |
| 内容 | 标题、文字、嘉宾/老师名单 |
| 素材 | 上传、按省份贴图、背景、字体 |
| 交付 | 检查问题、导出 PNG、保存到本机 |

### 5.2 放入“更多/高级”的能力

- 地图边界十种纹理、热力区间、南海诸岛折叠、地图图片裁剪与精确对齐。
- 卡片表达式、姓名脱敏模板、连接线形状/纹理、布局策略和允许地图重叠。
- 网格间距、渲染帧率、统一贴图像素尺寸、图像主题诊断信息。
- 工程包细粒度选项、资源包、云端工作区 API 令牌、协作冲突细节。

### 5.3 一致的“即时生效”约定

- 颜色、字体、开关、滑杆、下拉选择均立即更新画布并进入撤销历史。
- 文本、数字输入使用失焦或 Enter 提交，避免每个按键触发沉重布局计算；显示未提交草稿状态。
- 自动布局、导入替换、模板应用、AI 应用、工程导入属于高影响动作：先提供预览/摘要或明确确认，再创建一个可撤销事务。
- 所有素材上传、替换、删除后都需要立刻更新素材库和画布引用状态，不能依赖刷新。

---

## 6. 关键用户流程

### 6.1 新用户：10 分钟完成一张标准海报

1. 在「项目」选择新建，并从 5 个内置模板中选一个起点。
2. 进入「名单」，粘贴文本或上传 Excel；审查候选并处理未匹配城市。
3. 进入「地图」，默认展示省份卡片；根据人数改为城市/院校卡片、图钉或热力。
4. 进入「版式」，点击「一键智能排版」；只在有需要时拖动个别卡片。
5. 进入「内容」，填写标题、副标题，按需添加老师名单。
6. 在「交付」查看检查项，保存本机并导出 2× PNG。

### 6.2 有审美需求：在不破坏主流程下深度定制

1. 在「版式」选择模板后调整画布、背景、地图框和卡片风格。
2. 在地图点击目标省份，直接进入筛选后的贴图库；上传、抠图、应用并自动取色。
3. 使用「素材 > 字体」上传班级字体，再在「内容」或选中对象的检查器中应用。
4. 展开地图/卡片高级设置，微调边界纹理、连接线、热力色阶、表达式和姓名格式。
5. 通过撤销/重做和缩放在画布中检查每次变更。

### 6.3 协作与复用：班委共同完成并在多设备继续

1. 在「交付 > 协作」创建房间并复制房间码。
2. 成员加入后，编辑器通过 SSE 接收增量变更；不同字段的并发修改自动合并。
3. 同一字段冲突时暂停上传，提示重新加入以载入当前版本，避免静默覆盖。
4. 导出完整工程包用于归档，导出资源包用于在其他班级工程复用素材和字体。

---

## 7. 分阶段落地建议

该规划首先是**信息架构与交互重组**，不要求改变成熟的数据模型、画布算法或 API。建议按低风险到高影响实施。

### 阶段 A：入口重排，不改变功能行为

**目标：** 用户无需理解内部模块即可找到功能。

- 建立六个一级工作区：名单、地图、版式、内容、素材、交付。
- 将 DataWorkspace 从全局设置提升为一级入口。
- 把原「全局设置」的画布/卡片移动到版式，地图设置移动到地图。
- 将 AI 改为全局入口；保留现有提案、预览与应用行为。
- 保持 InspectorPanel、工程菜单、导出函数、存储格式和所有 API 不变。

**验收：** 从新建项目开始，用户可以不打开“全局设置”完成导入→呈现→排版→导出；现有工程导入和所有测试保持通过。

### 阶段 B：上下文直达与渐进披露

**目标：** 让高频动作离画布更近，让低频能力不干扰。

- 点击省份后显示简化的省份快捷面板，并直接切换素材库到当前省。
- 根据是否选择对象调整右侧检查器默认内容。
- 将边界纹理、表达式、性能参数等移动进高级折叠区。
- 在交付中集中未匹配/隐藏名单、保存和导出状态。

**验收：** 对“换某省贴图”“改标题”“一键排版”“导出 PNG”四个高频任务，用户不需要跨越超过一个工作区。

### 阶段 C：流程质量与风险提示

**目标：** 把现有隐式能力变成可理解、可恢复的完成体验。

- 增加交付检查面板：未匹配、隐藏、排版拥挤提示与跳转。
- 在模板、AI、导入替换、工程导入前显示影响摘要；继续复用现有事务历史实现撤销。
- 以持久化状态统一显示本机保存、协作同步、导出失败原因。

**验收：** 用户始终知道“下一步是什么”“是否有未保存变更”“导出的海报是否遗漏名单”。

### 实施边界

- **不改变：** `ProjectDocument`、工程包版本、资源包格式、协作 API、服务端路径、地图渲染/自动布局算法的语义。
- **可能变化：** `App.tsx` 的工作区状态、`StudioUi` 导航、`GlobalSettingsScreen` 的拆分和复用、相关组件测试的交互选择器。
- **需要重点回归：** 本地工作区恢复、工程导入/导出、素材即时应用、地图点击→省份贴图、AI 预览/应用、协作冲突、PNG/SVG 导出。

---

## 8. 技术架构与现有功能清单

### 8.1 技术栈与运行方式

| 层 | 技术 |
|---|---|
| 前端 | React 18 + TypeScript + Vite，纯前端 SPA（无路由框架，按 `location.pathname` 分发主应用/管理页） |
| 地图渲染 | `d3-geo`（geoMercator 墨卡托投影 + geoPath），地图数据为内置 `src/assets/china.geojson` |
| 后端 | Node.js 原生 `http` 模块（无框架），TypeScript 经 `tsx` 直接运行（`server/index.ts`） |
| 数据处理 | `xlsx`（Excel 解析）、`pinyin-pro`（拼音）、原生 FileReader/Canvas |
| 测试 | Vitest + jsdom（组件与纯函数均有测试），ESLint + TypeScript |
| 存储 | 前端：localStorage + IndexedDB（双轨工作区快照）；后端：`.data/` 目录 JSON 文件（访问日志、工作区快照） |

- 开发：`npm run dev`（Vite + API server）。
- 生产：`npm run build` → `npm start`（server 同时托管 `dist` 静态文件）。
- 页面：`/` 主编辑器；`/admin` 独立管理后台（Basic Auth / 本机回环保护）。

### 8.2 模板、字体与系统资源

- 内置模板：原始地图、卡通画风、颗粒画风、Q版、风景插画版；历史 `regional` 模板可迁移。
- 自定义模板：保存为视觉样式或布局倾向，最多 20 个，随工程包/工作区保存。
- 内置字体：默认黑体、宋体/衬线、楷体、仿宋、圆体、等宽；支持上传 TTF/OTF/WOFF/WOFF2。
- 系统资源：覆盖各省的 SVG 特色贴图和宣纸底纹背景；历史装饰与地域剪影兼容旧工程，但不作为新工程默认创建资源。

### 8.3 项目、存储与历史

- `ProjectDocument`：名单、模板、数据表达、画布、地图、卡片、嘉宾、文本、素材实例、样式与历史记录。
- 快照式撤销/重做：最多 50 步，800ms 连续操作合并；支持 Ctrl/Cmd+Z、Ctrl/Cmd+Shift+Z、Ctrl+Y。
- 本机保存：localStorage 快速镜像 + IndexedDB 持久工作区双轨恢复。
- 工程包：版本 2，包含工程、用户素材、字体、自定义模板和渲染设置；导入时自动修复资产引用。
- 资源包：独立导入/导出素材与字体。
- 所有新增 id 应使用 `createId(prefix)`，不能直接调用 `crypto.randomUUID()`（非安全上下文可能静默失败）。

### 8.4 AI、协作和后台管理

**AI 助手**
- `/api/ai/parse-data`：将文本转为学生导入候选。
- `/api/ai/propose-edits`：将自然语言转为可勾选的编辑命令，并支持画布预览后应用。
- 当前 provider 是 `local-fallback` 规则实现：可切换城市/院校分组、紧凑卡片、地图缩放、风景模板、字段可见性和按人数建议；问句返回解释。

**在线协作**
- 创建/加入 12 位大写房间码，初始上传工程快照。
- 本地改动 600ms 后计算路径级增量，服务端通过 SSE 推送。
- 不冲突的并发字段自动 rebase；同路径冲突返回 409 并要求重新加入。
- 房间内存保存，100 房间上限、每房 50 订阅、30 分钟无活动清理、256 条操作历史。

**管理后台**
- `/admin` 显示总请求、独立 IP、路径排行和最近访问记录。
- `/api/admin/visits` 仅允许本机回环；配置 `ADMIN_PASSWORD` 时须 Basic Auth（用户名默认 `admin`）。
- `.data/visits.json` 最多保留 5000 条页面 GET 记录；主站没有统计入口。

### 8.5 后端 API

| 方法 | 路径 | 说明 | 鉴权 |
|---|---|---|---|
| GET | `/api/health` | 健康检查 | 无 |
| GET | `/api/admin/visits` | 访问统计 | Basic Auth / 本机 |
| GET/PUT | `/api/workspace` | 工作区快照读写 | Bearer / X-API-Key（`WORKSPACE_API_TOKEN`） |
| POST | `/api/rooms` | 创建协作房间 | 无 |
| GET | `/api/rooms/:id` | 读取协作房间 | 无 |
| POST | `/api/rooms/:id/transactions` | 提交快照或增量操作 | 无 |
| GET | `/api/rooms/:id/events` | SSE 实时事件流（20 秒心跳） | 无 |
| POST | `/api/ai/parse-data` | AI 名单解析 | 无 |
| POST | `/api/ai/propose-edits` | AI 修改建议 | 无 |
| POST | `/api/ai/explain` | AI 解释 | 无 |

服务端具备请求体限制（AI 512KB / 协作 8MB / 工作区 64MB）、gzip、哈希静态资源长缓存、路径穿越防护、安全响应头与 `CORS_ORIGINS` 白名单。

### 8.6 核心实现模块

| 模块 | 职责 |
|---|---|
| `src/App.tsx` | 编辑器总装：工程状态、历史、工作区、协作、导入导出、工作区切换 |
| `src/components/canvas/*` | SVG 画布、地图、数据层、文本层、装饰层、省份贴图、缩放控制 |
| `src/components/inspector/*` | 画布/地图/省份/卡片/嘉宾/文本/素材的对象属性编辑 |
| `src/components/DataWorkspace.tsx` | 名单录入、导入审查、搜索、编辑与显隐管理 |
| `src/components/AssetPanel.tsx` | 用户素材、系统贴图、抠图、取色、资源包与贴图设置 |
| `src/components/GlobalSettingsScreen.tsx` | 现有全局设置容器；重组时拆分为地图/版式/内容的复用面板 |
| `src/lib/project-document.ts` | 工程文档、事务、快照历史、撤销/重做 |
| `src/lib/scene-document.ts` | Canvas/Map/Cards/Guests/Text/Asset 场景模型与归一化 |
| `src/lib/card-layout.ts` | 卡片自动布局、避让、回退策略、手动位置约束 |
| `src/lib/connector-geometry.ts` | 卡片至地图连接线的端点、折线、曲线和相交处理 |
| `src/lib/map-alignment.ts` | 上传地图图片的填充、裁剪与仿射对齐 |
| `src/lib/province-texture*.ts` | 省份贴图尺寸、位置、碰撞和溢出处理 |
| `src/lib/image-color.ts` | 贴图取色与邻省配色优化（Oklab） |
| `src/lib/project-package.ts` / `resource-pack.ts` | 工程包、资源包序列化、解析、迁移和引用修复 |
| `src/lib/collaboration-operations.ts` | 协作差异、操作应用、重基合并 |
| `server/index.ts` | HTTP API、静态托管、鉴权、访问日志与安全响应头 |
| `server/collaboration.ts` | 内存协作房间、版本控制、订阅、TTL、限额 |

### 8.7 边界与已知约束

- 本机保存为手动覆盖模式：编辑后显示待保存，点击保存才写入本地工作区。
- 协作房间只存在于内存，30 分钟无活动自动清理；冲突需要重新加入房间确认最新版本。
- AI 目前不调用外部 LLM，而是本地规则回退；请求 schema 已保留真实模型接入空间。
- 导出 PNG 依赖浏览器 Canvas；超大画布或 3× 导出可能受浏览器内存限制。
- 访问统计只可通过受保护的 `/admin` 查看，不能在主站或公开 API 中暴露。
